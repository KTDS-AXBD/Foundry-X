# Prototype Builder v2 PRD

**버전:** Final (v2 기반)
**날짜:** 2026-04-07
**작성자:** AX BD팀
**착수 판정:** Conditional → PoC 선행 후 착수 (Ambiguity 0.15, 스코어카드 R1 78/100)
**상태:** ✅ 착수 준비 완료

---

## 1. 요약 (Executive Summary)

**한 줄 정의:**
Prototype Builder의 디자인 품질·판별 정확도·비용 구조를 동시에 고도화하여, PRD 입력만으로 고객 데모가 바로 가능한 수준의 프로토타입을 자동 생성하는 시스템.

**배경:**
Phase 19(Builder Evolution)에서 5차원 품질 스코어링 체계(build/ui/functional/prd/code)와 O-G-D 자동 개선 루프를 구축하고, Claude Max CLI PoC를 완료했다(96% 구현). 그러나 3가지 핵심 한계가 남아 있다:

1. **디자인 품질**: LLM 기본 생성 결과는 "AI가 만든 느낌"이 강함. 과용 폰트(Inter/Arial), 회색 계열 단조로움, 카드 중첩 등 전형적인 AI 디자인 패턴에서 벗어나지 못함
2. **판별 정확도**: Discriminator가 정적 체크리스트(h1 존재, CTA 버튼, 기능 2개+) 수준으로, 실제 품질 향상 루프가 의미 있게 작동하지 않음
3. **비용 구조**: API 호출당 ~$2.40 비용으로 반복 개선 실험이 예산 제약을 받음. max-cli PoC는 완료했으나 본격 통합 미완

**목표:**
impeccable 디자인 스킬 시스템 통합 + LLM 기반 다차원 판별 + max-cli 본격 통합으로, 추가 수정 없이 고객 데모가 가능한 프로토타입을 자동 생성한다.
**실제 데모 가능 기준(추가):**
- 자동 평가(5D 품질 점수, Vision API 점수, PRD 반영률) 외에, BD팀 자체 검수 및 실제 고객 피드백(내부 데모, 파일럿 고객 인터뷰 등)에서 "즉시 데모 가능" 판정을 받은 경우를 최종 기준으로 삼는다.
- 품질 검증 프로세스는 정량/정성 복합 지표(자동 점수+BD팀 체크리스트+사용자 피드백)를 통과해야 함.

---

## 2. 문제 정의

### 2.1 현재 상태 (As-Is)

**디자인 품질:**
- 생성 프롬프트에 디자인 가이드라인 없음 → LLM 기본 스타일 그대로 출력
- 과용 폰트(Inter, Arial, system-ui), 순수 흑색/회색, 낮은 시각적 계층구조
- 단조로운 레이아웃: 카드 반복, CTA 버튼만 강조, 섹션 간 구분 부족
- 모바일 반응형이 깨지거나 부자연스러운 경우 다수

**판별 정확도:**
- Discriminator 체크리스트: h1 존재, 기능 2개+, CTA 버튼, 반응형, 시각 계층구조 — 5개 항목
- 체크리스트 통과해도 "조잡한" 프로토타입이 80점 이상 나올 수 있음
- PRD 정합성(25%)은 키워드 매칭 수준 — 의미적 반영도 미평가
- 자동 개선 루프의 피드백이 구체적이지 않아 품질이 수렴하지 않음

**비용 구조:**
- API 모드: Sonnet $3/$15 per 1M tokens → 빌드당 ~$2.40
- 월 $20 예산으로 ~8회만 실행 가능 → 반복 실험 제약
- max-cli PoC 완료(poc-cli.md): rate limit, 응답 시간 측정 완료
- 본격 통합 미완 — WSL 로컬 실행 환경 미구축
**비용 구조에 대한 추가 문제:**
- Vision API 등 외부 서비스 호출 비용이 예측 불가하며, 실제 반복 실험에 필요한 비용 한도 및 효율적 대안(예: 캐싱, 평가 주기 조정, 내부 대체 평가 등)이 현재 부재
- max-cli의 구독 계정 rate limit 정보가 불명확하여, 무제한 반복 가정에 리스크 존재
**운영/품질 보증 문제:**
- LLM/Vision API 등 자동 점수만으로 실제 고객/BD팀 체감 품질이 보장되지 않음
- 장애(예: max-cli/ Vision API rate limit/장애/응답 지연) 발생 시 fallback, 모니터링, 알림 등 운영 대응 체계 미흡
- 내부 BD팀이나 파일럿 고객 등 실제 사용자의 품질 검증/피드백 수집 프로세스 부재

### 2.2 목표 상태 (To-Be)

| 차원 | As-Is | To-Be |
|------|-------|-------|
| 디자인 품질 | LLM 기본 스타일 | impeccable 7도메인 적용, 전문가 수준 |
| PRD 반영률 | 키워드 매칭 (체감 60~70%) | LLM 의미 비교 90%+ |
| 시각적 평가 | 정적 DOM 분석만 | Vision API 스크린샷 평가 |
| 판별 정확도 | 5항목 체크리스트 | LLM 5차원 정밀 판별 |
| 빌드 비용 | ~$2.40/회 (API) | $0/회 (max-cli 구독) |
| 반복 가능성 | 월 ~8회 | 무제한 (구독 내) |
| 데모 가능성 | 추가 수정 필요 | 바로 데모 가능 (자동+BD팀/고객 실사용 평가 통과) |
### 2.3 시급성

- Phase 22(Offering Skill v2)에서 프로토타입이 핵심 산출물 — 품질 미달 시 파이프라인 전체 효용 저하
- API 비용 누적이 반복 실험을 제약하고 있어 빠른 max-cli 전환이 필요
- impeccable이 공개된 시점에서 디자인 품질 기준이 시장에서 상향 중
- 
- Vision API/CLI 등 외부 서비스의 실제 한도/비용 실측 없이는 전체 범위/일정 산정이 불가하여, 사전 실험 및 조건부 착수가 필요함

---

## 3. 사용자 및 이해관계자

### 3.1 주 사용자
| 구분 | 설명 | 주요 니즈 |
|------|------|-----------|
| BD팀 | AX BD팀 7명 | PRD 입력 → 고객 데모 가능한 프로토타입 자동 생성, 직접 품질 검수/피드백 제공 |

| 고객 이해관계자 | BD 미팅 참석자 | 시각적으로 완성도 높은 프로토타입 확인, 실사용 피드백 제공(파일럿/POC) |

| 임원 | 의사결정권자 | 비용 대비 효과 확인, 고품질 데모 자료 |

### 3.2 이해관계자
| 구분 | 역할 | 영향도 |
|------|------|--------|
| BD팀장 | 품질 승인, 고객 미팅 주관 | 높음 |
| 개발팀 | Builder 시스템 유지보수, 장애/운영 대응 | 중간 |
### 3.3 사용 환경
- 기기: PC (생성), PC/모바일/태블릿 (데모 확인)
- 네트워크: 인터넷 (Cloudflare Pages 배포)
- 기술 수준: 비개발자 (BD팀), 개발자 (유지보수)

---

## 4. 기능 범위

### 4.1 핵심 기능 (Must Have)

| # | 기능 | 설명 | 트랙 | 우선순위 |
|---|------|------|------|----------|
| F414 | impeccable 디자인 스킬 통합 | impeccable 7도메인 참조문서 + 안티패턴을 Generator 시스템 프롬프트에 주입. 타이포그래피(폰트 페어링, 모듈러 스케일), 색상(OKLCH, tinted neutrals), 공간(스페이싱 시스템), 반응형(mobile-first, fluid layout) 도메인 우선 적용 | A | P0 |
| F415 | 디자인 안티패턴 차단 | impeccable 안티패턴 목록을 Discriminator 체크리스트에 추가. 과용 폰트(Arial, Inter, system-ui), 순수 흑색/회색, 카드 중첩, 낮은 대비 등 자동 감지 → 재생성 트리거 | A | P0 |
| F416 | PRD 정합성 LLM 판별 | prd 차원(25%)을 키워드 매칭에서 LLM 의미 비교로 교체. PRD 요구사항 목록과 생성된 프로토타입의 기능/콘텐츠를 LLM이 대조하여 반영률 산출 | B | P0 |
| F417 | 5차원 LLM 통합 판별 | build/ui/functional/prd/code 5개 차원 전체를 LLM이 평가하는 통합 판별기 구축. 기존 정적 분석은 보조 데이터로 제공, 최종 점수는 LLM 판정 | B | P0 |
| F418 | Vision API 시각 평가 | 프로토타입 스크린샷을 Vision API(Claude)로 전송하여 시각적 품질 평가. UI 차원(25%) 정확도 대폭 향상. 평가 항목: 레이아웃 균형, 색상 조화, 타이포그래피, 시각 계층구조, 반응형 | B | P0 |
| F419 | max-cli 본격 통합 | WSL 환경에서 Claude Code CLI를 Builder 파이프라인에 통합. `--bare` 모드 사용, rate limit 대응(재시도 + 백오프), API fallback 자동 전환. 비용 추적에 generation_mode 기록 | C | P0 |
| F420 | max-cli 큐 관리 | 단일 머신 제약 대응: 빌드 요청 큐잉 + 순차 실행 + 상태 모니터링. 동시 빌드 방지, 타임아웃 처리 | C | P0 |
| F423 | 운영 장애 대응·모니터링 | max-cli, Vision API 장애/지연/실패 시 fallback(자동 API 전환), 장애/비용/성능 모니터링 및 실시간 알림 체계 구축 | D | P0 |
| F424 | 품질 검증 수동 절차 | LLM/Vision API 자동 평가 외에 BD팀의 직접 체크리스트 검수, 내부 파일럿 데모, 고객 피드백 수집 등 수동 품질 검증 및 피드백 반영 프로세스 구현 | D | P0 |
| F425 | 데이터 일관성 및 기록 보장 | 빌드/평가 결과, PRD-프로토타입 매핑, 평가 로그, 피드백 이력 등 데이터 일관성 및 감사 가능 기록 보장 | D | P0 |
| F426 | 보안/개인정보 관리 | API key·토큰·PRD 데이터의 안전한 관리, 외부 전송 로그, 데이터 유출 방지 조치 및 보안 프로세스 강화 | D | P0 |
| F427 | 성능/대기시간 관리 | max-cli, Vision API 연동 시 실제 빌드 대기시간, 동시 요청시 응답지연 등 성능 모니터링 및 SLA 설정 | D | P0 |

### 4.2 부가 기능 (Should Have)

| # | 기능 | 설명 | 트랙 | 우선순위 |
|---|------|------|------|----------|
| F421 | 디자인 커맨드 파이프라인 | impeccable의 /audit → /normalize → /polish 단계별 커맨드를 Builder O-G-D 루프에 매핑. 생성 후 자동으로 audit → normalize → polish 실행 | A | P1 |
| F422 | 판별 피드백 구체화 | LLM 판별 결과를 "어떤 부분을 어떻게 고쳐야 하는지" 구체적 지시로 변환. Generator의 다음 라운드 프롬프트에 자동 주입 | B | P1 |
### 4.3 제외 범위 (Out of Scope)

- **실시간 협업**: 여러 사용자가 동시에 프로토타입을 편집하는 기능
- **프로토타입 → 프로덕션 코드 전환**: 생성된 코드를 실서비스로 직접 배포
- **원격 빌드 서버**: VPS/EC2/GitHub Actions 환경 — WSL 로컬만 지원
- **LLM 외 ML 모델**: 커스텀 학습된 디자인 품질 모델 (LLM API만 사용)
- **Lighthouse 통합**: Phase 19 로드맵에 있었으나 이번 범위에서 제외 (Vision API로 대체)

### 4.4 외부 연동

| 시스템 | 연동 방식 | 필수 여부 |
|--------|-----------|-----------|
| Claude Code CLI (max-cli) | WSL subprocess | 필수 |
| Claude Vision API | REST API (스크린샷 전송) | 필수 |
| impeccable 참조문서 | 정적 파일 (빌드 시 번들) | 필수 |
| Anthropic API | REST API (fallback) | 필수 (fallback) |
| Cloudflare Pages | CLI deploy | 기존 유지 |

---

## 5. 성공 기준

### 5.1 정량 지표 (KPI)

| 지표 | 현재값 | 목표값 | 측정 방법 |
|------|--------|--------|-----------|
| 5D 품질 점수 | 체감 30~40점 | 80점+ 자동 도달 | prototype_quality 테이블 |
| PRD 반영률 | ~60~70% (키워드) | 90%+ (LLM 비교) | prd 차원 점수 |
| 시각적 완성도 | DOM 분석만 | Vision API 0.8+ | ui 차원 점수 |
| 빌드 비용 | ~$2.40/회 | $0/회 (구독 내) | prototype_usage_logs |
| 자동 개선 루프 수렴 | 미수렴 | 5라운드 내 80점 도달 | ogd_rounds 평균 |
| 안티패턴 검출률 | 0% | 95%+ | Discriminator 로그 |
| 수동 품질 검증 통과율 | 미측정 | 90%+ (BD팀 체크리스트) | 내부 검증 로그 |
| 실고객/파일럿 피드백 반영률 | 미측정 | 80%+ 긍정 피드백 | 데모 후 설문/인터뷰 |
### 5.2 MVP 최소 기준

- [ ] impeccable 4개 핵심 도메인(타이포, 색상, 공간, 반응형)이 Generator 프롬프트에 적용
- [ ] LLM 판별기가 5차원 중 최소 prd + ui 차원에서 작동
- [ ] max-cli가 WSL에서 빌드 파이프라인에 연결되어 실제 빌드 1건 이상 성공
- [ ] 1개 이상의 PRD로 생성한 프로토타입이 고객 데모 가능 수준 (팀 내부 확인)
- [ ] BD팀 수동 검증 체크리스트 통과 및 파일럿 고객 피드백(긍정) 확보
### 5.3 실패/중단 조건

- max-cli rate limit이 하루 5건 미만으로 제한되어 실용성 없음
- Vision API 비용이 빌드당 $1 이상이어서 비용 절감 효과 상쇄
- LLM 판별 재현성이 ±20점 이상으로 불안정
- 3 Sprint 투입 후에도 80점 자동 도달이 불가능
- BD팀 수동 체크리스트 및 파일럿 고객 피드백에서 '즉시 데모 불가' 판정 지속

---

## 6. 제약 조건

### 6.1 일정

- Phase 22 범위에 포함 또는 별도 Phase로 편성
- 예상 Sprint: 5~7개 (3트랙 병렬)
- 
- Vision API/CLI 등 외부 서비스의 실증(비용, 성능, 한도) 완료 이후 본격 개발 착수

### 6.2 기술 스택

- 프론트엔드: React 18 + Vite 8 (기존 유지)
- 백엔드: Hono + Cloudflare Workers (기존 유지)
- 인프라: Cloudflare D1 + Pages (기존 유지)
- 신규: Claude Code CLI (WSL subprocess), Claude Vision API
- 참조: impeccable 정적 참조문서 (빌드 시 번들)
- 

### 6.3 인력/예산

- 투입: AI 에이전트 + BD팀 1명 (Sinclair)
- 예산: Claude Max 구독 $100/월 범위 내
- Vision API 비용: 별도 확인 필요 (Anthropic API 과금)

- 실제 리소스 부족(1인)시, 외부 파트타임 개발/QA/운영 리소스 추가 투입 검토 필수

### 6.4 컴플라이언스

- impeccable: Apache 2.0 라이선스 → 상업적 사용 가능
- Claude API 이용약관 준수
- 고객 데이터(PRD)가 외부 API로 전송됨 — 기존과 동일한 보안 수준 유지
---

## 7. 오픈 이슈

| # | 이슈 | 담당 | 마감 |
|---|------|------|------|
| 1 | Vision API 비용 — 스크린샷 1장당 토큰/비용 실측 필요 | Sinclair | Sprint 착수 전 |
| 2 | max-cli rate limit — 구독 계정 실제 일일 한도 재확인 | Sinclair | Sprint 착수 전 |
| 3 | impeccable 참조문서 크기 — 시스템 프롬프트 토큰 한도 초과 시 우선순위 기반 선별 필요 | [미정] | F414 설계 시 |
| 4 | LLM 판별 비용 — 5차원 전체 LLM 판별 시 토큰 사용량 (max-cli로 대체 가능 여부) | [미정] | F417 설계 시 |
| 5 | Phase 22와 범위 조정 — Offering Skill v2(F414~F422)와 Builder v2 F-item 번호 충돌 가능성 | Sinclair | SPEC 등록 시 |
| 6 | LLM 판별 재현성 — ±20점 범위 내 재현성 확보(프롬프트 엔지니어링, 평가 지표, 모델 선택 등 구체 전략 필요) | [미정] | F417/F418 설계 시 |
| 7 | Vision API/CLI 장애·지연·fallback 설계 — 장애 발생시 자동 복구, 실시간 모니터링/알림 체계 필요 | [미정] | 운영 설계 시 |
| 8 | 파일럿 사용자/고객 선정 및 피드백 루프 설계 — 내부 BD팀, 파일럿 고객 QA 프로세스 마련 | [미정] | MVP 직후 |
| 9 | 보안/개인정보 관리 강화 — API key, PRD 데이터, 외부 전송 기록, 유출 방지 프로세스 합의 | Sinclair | 착수 전 |
| 10 | 성능/대기시간 요구사항 — max-cli/Vision API 빌드 대기시간, 동시 요청 처리 한계 측정 | [미정] | F427 설계 시 |
| 11 | 데이터 일관성 및 감사 가능성 확보 — 빌드/평가/피드백 전체 이력 관리 | [미정] | 데이터 설계 시 |
| 12 | 평가 자동화 기준과 실사용자 품질 괴리 해소 — KPI/체크리스트/피드백 통합 기준 도출 | [미정] | MVP 검증 시 |
---

## 8. 마일스톤 구성 (안)

| 마일스톤 | F-items | 설명 |
|----------|---------|------|
| **M1: 디자인 품질 기반** | F414, F415 | impeccable 통합 + 안티패턴 차단 + 참조문서 토큰 한도 초과 대응 전략(도메인 우선순위, 요약, 동적 주입 등) 포함 |
| **M2: LLM 판별 고도화** | F416, F417, F418 | PRD/5D/Vision LLM 판별 + 평가 재현성 확보(프롬프트 엔지니어링, 지표, 모델, 자동/수동 혼합) |
| **M3: max-cli 통합** | F419, F420 | CLI 파이프라인 + 큐 관리 + rate limit/운영 장애 대응 설계(자동 fallback, 알림, SLA) 포함 |
| **M4: 루프 최적화 및 운영** | F421, F422, F423~F427 | 커맨드 파이프라인 + 피드백 구체화 + 운영/품질/보안/성능/데이터 일관성 강화, 수동 검증/피드백 루프 구현 |
> 3트랙 병렬이므로 M1~M3는 동시 진행 가능. M4는 M1+M2 완료 후.
---

## 9. 리스크 관리
### 주요 리스크 및 대응

| 리스크 | 상세 내용 | 대응 방안 |
|--------|----------|-----------|
| Vision API/CLI 비용·제한 실증 전제 | 비용·rate limit 실측 전 범위/일정 산정 불가, 예산/기능 미달 가능 | Sprint 0에서 실증 실험 후 착수 결정, 비용 초과/제한시 스코프 조정/대안 평가 |
| 인력 과소배치 | 1명+AI만으로 5~7스프린트 내 완수 비현실, 병목·품질저하 우려 | 외부 파트타임 개발/QA/운영 리소스 확보, 일정/마일스톤별 검증 인력 배분 |
| 품질 측정과 고객 체감의 괴리 | LLM/Vision API 자동 점수와 실제 고객/BD팀 평가 불일치 가능 | 수동 체크리스트·파일럿 피드백 의무화, KPI·데모 기준에 정성적 요소 포함 |
| 장애/운영 대응 미흡 | max-cli, Vision API 장애·지연시 fallback/알림/복구 체계 부재 | 운영 장애 대응 기능(F423) 선반영, SLA·알림·자동 전환 설계 |
| 보안/개인정보 관리 | API key, PRD 데이터 외부 전송·유출 위험 | 보안 관리 기능(F426), 감사 로그, 키 관리 프로세스 강화 |
| 데이터 일관성 부재 | 빌드/평가/피드백 이력 불일치시 추적·감사 곤란 | 전과정 감사 로그/DB 이력화, 일관성 자동 검증 |
| 성능/대기시간 불확실 | 빌드/평가 대기, 동시 요청시 응답지연 | 성능 모니터링·SLA(F427), 동시성 제한, 사용자 안내 |
| 평가 자동화 기준의 재현성 | LLM 평가 점수 편차·불안정시 품질 수렴 실패 | 프롬프트 엔지니어링, 평가 지표 정교화, 모델 선택, 수동 검증 병행 |
| 스펙 크리프 | impeccable/Vision 등 외부 트렌드 변화로 요구사항 확장 | 범위 관리, Out-of-scope 명확화, 빈번한 리스크 리뷰 |
| 토큰 한도 초과 | impeccable 참조문서 등 시스템 프롬프트 토큰 한도 초과 | 도메인 우선순위, 동적 요약, 필요시 부분 주입 전략 도입 |
| 로컬 CLI 의존성 | WSL CLI 환경 불안정, 확장성·이식성 저하 | 장애 대응, 이중화, 원격/클라우드 대체안 검토 |

---

## 10. Out-of-scope
- 실시간 협업
- 프로토타입→프로덕션 코드 전환
- 원격 빌드 서버(VPS/EC2/GitHub Actions)
- LLM 외 ML 모델(커스텀 학습)
- Lighthouse 통합(이번 범위 제외)
- 외부 평가 데이터셋 기반 자동 튜닝/학습
- 실시간 데모 배포(즉시 미반영)
- 스펙 크리프 방지: impeccable, Vision 등 외부 트렌드로 인한 요구사항 지속 확장은 이번 PRD 범위 밖

---

## 11. 검토 이력

| 라운드 | 날짜 | 주요 변경사항 | 스코어 |
|--------|------|--------------|--------|
| 초안 | 2026-04-07 | 최초 작성 (인터뷰 기반) | - |
| v2 | 2026-04-08 | 고객 체감 기준/KPI/운영/장애/보안/피드백 루프/리스크/인력/실증 등 AI 검토 의견 반영 | - |
---

*이 문서는 requirements-interview 스킬에 의해 자동 생성 및 관리됩니다.*