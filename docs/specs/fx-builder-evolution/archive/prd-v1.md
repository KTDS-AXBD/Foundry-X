# fx-builder-evolution PRD

**버전:** v1
**날짜:** 2026-04-06
**작성자:** AX BD팀
**상태:** 🔄 검토 중

---

## 1. 요약 (Executive Summary)

**한 줄 정의:**
Prototype Builder의 품질을 계량화·자동 향상하고, Claude Max 구독을 활용해 API 비용을 제거하여 고객 데모 가능한 수준의 프로토타입을 지속 생성하는 시스템.

**배경:**
Phase 16에서 Prototype Auto-Gen 시스템을 구축하여 PRD → 코드 생성 → Vite 빌드 → Cloudflare Pages 배포 파이프라인을 완성했다. Railway 클라우드에서 5종 PRD의 E2E 실행에 성공했으나, 생성된 프로토타입의 품질이 고객에게 보여줄 수준에 미달한다. 현재 Discriminator는 `vite build` 성공 여부만 판정(0.9/0.4)하여 "빌드는 되지만 조잡한" 결과물이 통과한다.

동시에 Railway에서 Anthropic API를 직접 호출하는 구조로 인해 토큰 비용이 지속 발생하며, Claude Max 구독($100/월)에 포함된 Claude Code CLI를 활용하면 이 비용을 제거할 수 있다.

**목표:**
1. 다차원 품질 스코어링 체계를 구축하여 프로토타입 수준을 정량적으로 측정한다
2. 품질 점수 기반 자동 개선 루프로 80점 이상 달성을 자동화한다
3. Claude Max 구독의 CLI를 Builder에 통합하여 API 비용을 제거한다

---

## 2. 문제 정의

### 2.1 현재 상태 (As-Is)

**품질 문제:**
- 5종 프로토타입 모두 "빌드 성공"이지만 고객 데모 불가 수준
- Discriminator가 1차원(빌드 성공 0.9 / 실패 0.4)만 측정 — 디자인, 기능, PRD 반영도 미평가
- 생성된 코드의 UI가 깨져 있고, 인터랙션이 미구현된 상태로 배포됨
- 품질을 객관적으로 측정할 기준이 없어 "충분히 좋은지" 판단 불가

**비용 문제:**
- Railway에서 Anthropic API 직접 호출 → 토큰당 과금 (Sonnet: $3/$15 per 1M tokens)
- 5종 PRD × 평균 2라운드 × ~16K output tokens = 약 $2.40/회
- 반복 개선 시 비용 누적 (목표 80점까지 5+ 라운드 예상 → $6~12/회)
- CostTracker 월 $20 예산이 10회 미만 실행으로 소진

**환경 문제:**
- Railway Docker 환경에서 Claude CLI가 hang (SKIP_CLI=true로 우회 중)
- API 모드와 CLI 모드가 완전히 분리되어 통합 운영 어려움

### 2.2 목표 상태 (To-Be)

| 차원 | 현재 | 목표 |
|------|------|------|
| 품질 측정 | 1차원 (빌드 성공/실패) | 5차원 스코어카드 (100점 만점) |
| 품질 수준 | 체감 30~40점 | 80점+ (고객 데모 가능) |
| 자동 개선 | 빌드 실패 시만 재시도 | 품질 점수 기반 타겟 개선 |
| 코드 생성 비용 | ~$2.40/회 (API) | $0/회 (CLI 구독) |
| 월간 예산 | $20 (≈8회) | $0 (무제한, 구독 내) |

### 2.3 시급성

- BD팀이 고객 데모에 프로토타입을 사용하려면 품질 향상이 선행되어야 함
- API 비용이 누적되면서 반복 개선 실험 자체가 예산 제약을 받음
- Phase 18 Offering Pipeline에서 Prototype Builder를 핵심 도구로 활용할 예정 — 품질 미달 시 파이프라인 전체 효용이 저하

---

## 3. 사용자 및 이해관계자

### 3.1 주 사용자
| 구분 | 설명 | 주요 니즈 |
|------|------|-----------|
| BD팀 (7명) | PRD를 입력하여 프로토타입을 생성하는 직접 사용자 | 높은 품질의 프로토타입을 빠르게 생성 |
| 고객사 담당자 | 프로토타입을 보고 피드백을 주는 소비자 | 완성도 높은 데모를 통한 제안 이해 |
| 경영진 | BD 활동 성과를 승인하는 의사결정자 | 객관적 품질 지표로 투자 판단 |

### 3.2 이해관계자
| 구분 | 역할 | 영향도 |
|------|------|--------|
| Sinclair (팀 리드) | Builder 아키텍처 결정, 구독 예산 관리 | 높음 |
| BD팀원 | 일상적 프로토타입 생성 | 높음 |
| 고객사 | 데모 수신자, 피드백 제공 | 중간 |

### 3.3 사용 환경
- 기기: PC (데스크톱 웹 브라우저)
- 네트워크: 인터넷 (Cloudflare Pages 배포)
- 기술 수준: 혼합 (BD팀: 기술 이해도 보통, 고객: 비개발자)

---

## 4. 기능 범위

### 4.1 핵심 기능 (Must Have)

| # | 기능 | 설명 | 우선순위 |
|---|------|------|----------|
| 1 | **다차원 품질 스코어링** | Prototype을 5차원으로 평가하여 100점 만점 스코어 산출 | P0 |
| 2 | **타겟 개선 피드백** | 품질 점수가 낮은 차원에 대한 구체적 개선 지시를 Generator에 전달 | P0 |
| 3 | **자동 개선 루프** | 점수 < 80일 때 자동으로 개선 라운드를 반복 (최대 N회) | P0 |
| 4 | **Claude Code CLI 통합** | 구독 계정의 CLI를 코드 생성에 활용하여 API 비용 제거 | P0 |
| 5 | **품질 대시보드** | 생성된 프로토타입별 품질 점수 + 차원별 분석 표시 | P0 |

### 4.2 부가 기능 (Should Have)

| # | 기능 | 설명 | 우선순위 |
|---|------|------|----------|
| 1 | **디자인 시스템 템플릿** | shadcn/ui + Tailwind 기반 고품질 레이아웃 템플릿 제공 | P1 |
| 2 | **Lighthouse 자동 측정** | Performance, Accessibility, Best Practices 점수 자동 수집 | P1 |
| 3 | **스크린샷 기반 비전 평가** | 생성된 페이지의 스크린샷을 Vision API로 평가 | P1 |
| 4 | **비용 비교 대시보드** | API 모드 vs CLI 모드 비용 비교, 절감 효과 시각화 | P2 |
| 5 | **프로토타입 A/B 비교** | 이전 버전과 개선 버전을 나란히 비교하는 UI | P2 |

### 4.3 제외 범위 (Out of Scope)

- **백엔드 기능 구현**: 프로토타입은 프론트엔드(React SPA)만 — 실제 API 연동은 범위 밖
- **모바일 반응형 최적화**: 데스크톱 기준 품질 확보가 우선
- **사용자 인증/권한**: 프로토타입 URL은 public (Cloudflare Pages)
- **실시간 협업 편집**: 프로토타입 코드의 실시간 수정 기능

### 4.4 외부 연동

| 시스템 | 연동 방식 | 필수 여부 |
|--------|-----------|-----------|
| Cloudflare Pages | Wrangler CLI (배포) | 필수 |
| Cloudflare D1 | REST API (작업 큐) | 필수 |
| Claude Code CLI | subprocess `--bare` 모드 | 필수 (신규) |
| Anthropic API | SDK 직접 호출 | 선택 (fallback) |
| Lighthouse CI | puppeteer/lighthouse | 선택 |

---

## 5. 성공 기준

### 5.1 정량 지표 (KPI)

| 지표 | 현재값 | 목표값 | 측정 방법 |
|------|--------|--------|-----------|
| 평균 품질 점수 | ~35점 (추정) | 80점+ | 5차원 스코어카드 자동 평가 |
| 코드 생성 비용 | ~$2.40/회 | $0/회 | CostTracker (CLI 모드) |
| 빌드 성공률 | 60% (1st round) | 90%+ (1st round) | 빌드 로그 |
| 고객 데모 가능 비율 | 0% | 80%+ | 품질 점수 80+ 비율 |
| 자동 개선 수렴율 | - | 70%+ (5라운드 이내) | O-G-D 로그 |

### 5.2 MVP 최소 기준
- [ ] 5차원 품질 스코어링이 동작하고 점수가 산출됨
- [ ] 점수 < 80일 때 자동 개선 루프가 1회 이상 실행됨
- [ ] Claude Code CLI로 코드 생성이 가능함 (API 비용 $0)
- [ ] 기존 5종 PRD 중 3종 이상이 80점+ 달성

### 5.3 실패/중단 조건
- Claude Max 구독의 CLI가 subprocess 모드(`--bare`)를 지원하지 않거나 rate limit이 실용적이지 않을 때
- 5차원 평가의 재현성이 낮아(±20점 이상 편차) 신뢰할 수 없을 때
- 5라운드 이내 80점 수렴율이 30% 미만일 때

---

## 6. 제약 조건

### 6.1 일정
- 목표 완료일: Phase 18 Offering Pipeline과 병행 (2~3주)
- 마일스톤: M1(스코어링) → M2(CLI 통합) → M3(자동 개선) → M4(대시보드)

### 6.2 기술 스택
- Generator: TypeScript (Node.js 20), Claude Code CLI `--bare` 모드
- Discriminator: Claude Sonnet API (Vision) 또는 Lighthouse
- Scorer: TypeScript, 5차원 가중 평균
- 인프라: Railway (Docker) + 로컬 WSL (CLI 모드)
- 프론트엔드: Foundry-X Web Dashboard (기존 React + React Router)

### 6.3 인력/예산
- 투입 인원: 1명 (AI 에이전트 활용)
- Claude Max 구독: $100/월 (기존)
- Anthropic API: 최소화 목표 (Discriminator용만, 월 $5 이내)
- Railway: $5/월 (Hobby plan)

### 6.4 컴플라이언스
- KT DS 내부 정책: 고객 데이터 미포함 (PRD 기반 더미 데이터만 사용)
- 보안: Cloudflare Pages public 배포 — 민감 정보 미포함 확인 필수

---

## 7. 기술 설계 핵심

### 7.1 5차원 품질 스코어카드

| 차원 | 가중치 | 측정 방법 | 설명 |
|------|:------:|-----------|------|
| **빌드 성공** | 20% | `vite build` exit code | 빌드 가능 여부 (현재 Discriminator) |
| **UI 완성도** | 25% | Vision API (스크린샷 평가) | 레이아웃, 디자인 일관성, 스타일링 |
| **기능 동작** | 20% | 정적 분석 + 빌드 워닝 | onClick 핸들러, 라우팅, 상태 관리 |
| **PRD 반영도** | 25% | LLM 평가 (PRD vs 코드 비교) | Must Have 기능 구현 여부 |
| **코드 품질** | 10% | ESLint + TypeScript strict | 타입 에러, 린트 경고 수 |

**총점 = Σ(차원 점수 × 가중치) × 100**

각 차원은 0.0~1.0으로 평가되며, 가중 평균을 100점 환산한다.

### 7.2 듀얼 모드 아키텍처

```
┌─────────────────────────────────────────────┐
│               Builder Server                 │
│                                              │
│  ┌──────────┐    ┌──────────┐               │
│  │ CLI Mode │    │ API Mode │               │
│  │ (구독)   │    │ (fallback)│               │
│  └────┬─────┘    └────┬─────┘               │
│       │               │                      │
│       ▼               ▼                      │
│  ┌─────────────────────────┐                 │
│  │    Code Generator       │                 │
│  └────────────┬────────────┘                 │
│               ▼                              │
│  ┌─────────────────────────┐                 │
│  │  5-Dimension Scorer     │                 │
│  │  ┌───┬───┬───┬───┬───┐ │                 │
│  │  │빌드│UI │기능│PRD│코드│ │                 │
│  │  └───┴───┴───┴───┴───┘ │                 │
│  └────────────┬────────────┘                 │
│               ▼                              │
│  ┌─────────────────────────┐                 │
│  │  Improvement Loop       │                 │
│  │  (score < 80 → retry)   │                 │
│  └────────────┬────────────┘                 │
│               ▼                              │
│  ┌─────────────────────────┐                 │
│  │  Deploy (Pages)         │                 │
│  └─────────────────────────┘                 │
└─────────────────────────────────────────────┘
```

**CLI Mode (Primary):** Claude Max 구독의 `claude --bare` CLI를 사용. 로컬 WSL 또는 tmux 세션에서 실행. API 비용 $0.

**API Mode (Fallback):** Railway Docker에서 Anthropic API 직접 호출. CLI 불가 시 자동 전환. CostTracker로 예산 관리.

### 7.3 자동 개선 루프 (Enhanced O-G-D)

```
Round 1: Generator → 5-Dim Score → (score < 80?)
  ↓ Yes
Round 2: Generator + [가장 낮은 차원 타겟 피드백] → 5-Dim Score → (score < 80?)
  ↓ Yes
Round N: ... (최대 5회)
  ↓ 수렴 실패
Best Score 채택 + 미달 리포트 생성
```

각 라운드에서 가장 낮은 차원을 식별하고, 해당 차원에 특화된 개선 프롬프트를 생성한다:
- **UI 낮음** → "레이아웃을 정리하고, Tailwind 유틸리티를 사용하여 일관된 간격/색상을 적용하세요"
- **기능 낮음** → "모든 버튼에 onClick 핸들러를 연결하고, 라우팅이 동작하도록 하세요"
- **PRD 낮음** → "PRD의 Must Have 기능 중 미구현 항목: {목록}. 이를 추가하세요"

---

## 8. 마일스톤 구성

### M1: 5차원 스코어링 엔진 (Sprint 1)
- 5차원 평가기 구현 (빌드 + 정적분석 + LLM 평가)
- 기존 5종 프로토타입에 대한 베이스라인 점수 측정
- 스코어카드 결과 D1 저장 + API 조회

### M2: CLI 통합 + 듀얼 모드 (Sprint 2)
- Claude Code CLI `--bare` subprocess 모드 통합
- CLI 가용성 자동 감지 → CLI/API 자동 전환
- 비용 추적: CLI ($0) vs API ($N) 분리 기록

### M3: 자동 개선 루프 (Sprint 3)
- 타겟 피드백 생성기 (차원별 개선 프롬프트)
- Enhanced O-G-D 루프 (5라운드, 80점 수렴 목표)
- 라운드별 점수 추이 기록 + 개선 효율 분석

### M4: 품질 대시보드 (Sprint 4)
- Foundry-X Web에 Builder Quality 페이지 추가
- 프로토타입별 점수 카드 + 차원 레이더 차트
- 라운드별 개선 추이 그래프
- 비용 절감 효과 요약

---

## 9. 오픈 이슈

| # | 이슈 | 담당 | 마감 |
|---|------|------|------|
| 1 | Claude Max CLI의 `--bare` 모드 rate limit 확인 필요 — 구독 플랜별 분당/시간당 제한이 있는지 | Sinclair | PRD 확정 전 |
| 2 | Vision API (스크린샷 평가) 비용 — Discriminator용 API 호출은 별도 과금, 월 $5 이내 유지 가능한지 | Sinclair | M1 착수 전 |
| 3 | Lighthouse CI를 Railway Docker에서 실행 가능한지 (headless Chrome 필요) | [미정] | M1 |
| 4 | CLI 모드에서 WSL tmux 세션의 안정성 — 장시간 실행 시 세션 유지 이슈 가능 | Sinclair | M2 |
| 5 | 5차원 평가의 재현성 검증 — 동일 코드에 대해 ±10점 이내 일관성 필요 | [미정] | M1 완료 후 |

---

## 10. 검토 이력

| 라운드 | 날짜 | 주요 변경사항 | 스코어 |
|--------|------|--------------|--------|
| 초안 | 2026-04-06 | 최초 작성 (인터뷰 기반) | - |

---

*이 문서는 req-interview 스킬에 의해 자동 생성 및 관리됩니다.*
