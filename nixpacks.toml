[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = [
  "pnpm --filter @foundry-x/shared build",
  "pnpm --filter @foundry-x/prototype-builder build"
]

[start]
cmd = "node prototype-builder/dist/index.js"
