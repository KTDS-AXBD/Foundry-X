{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp",
        "--headless",
        "--no-sandbox",
        "--viewport-size", "1280x720"
      ]
    }
  }
}
