"use client";

// ─── F530: Agent Meta 라우트 — /agent-meta (Sprint 283) ───

import { AgentMetaDashboard } from "@/components/feature/AgentMetaDashboard";

export function Component() {
  return <AgentMetaDashboard />;
}

Component.displayName = "AgentMetaPage";
