---
code: FX-SPEC-001
title: Foundry-X Project Specification
version: 6.21
status: Active
category: SPEC
system-version: Sprint 319 ✅ F572+F574 + 3 hotfix (PR #690/#691/#692/#693) + fx-modules prod LIVE (S313) — Phase 45 Batch 5 완결
created: 2026-03-16
updated: 2026-04-29
author: Sinclair Seo
sprint: 319
---

# Foundry-X Project Specification

## §1 프로젝트 개요

Foundry-X — AX 사업개발 라이프사이클을 AI 에이전트로 자동화하는 오케스트레이션 플랫폼.
핵심 철학: **"Git이 진실, Foundry-X는 렌즈"** — 명세/코드/테스트/결정 이력은 Git에, Foundry-X는 읽기/분석/동기화 레이어.

- **상세 개요 + 아키텍처**: [BLUEPRINT.md](BLUEPRINT.md)
- **Phase 로드맵 + 계획**: [ROADMAP.md](ROADMAP.md)
- **현행 PRD**: `docs/specs/FX-SPEC-PRD-V8_foundry-x.md` (v8)

## §2 현재 상태

> Sprint 1~160 이력: [docs/archive/spec-current-state-2026-04.md](archive/spec-current-state-2026-04.md)

| Sprint | 상태 |
|--------|------|
| Sprint 261 | ✅ 완료 — ax-plugin source↔cache drift 방지 + C33 eslint cleanup (PR #508/#510) |
| Sprint 262 | ✅ 완료 — Phase 35 F511 Work Management 품질 보강 (D1 0126, ~3452 tests, E2E 273) |
| Sprint 263 | ✅ 완료 — Phase 36-A 착수: SPEC 파서 테스트 A-0 (48건 PR #517) |
| Sprint 264 | ✅ 완료 — F512 Phase A 정비 (A-0~A-8) + F513 TDD (B-0~B-3) (PR #518) |
| Sprint 273 | ✅ 완료 — F516 Backlog 인입 파이프라인 + 실시간 동기화 (PR #538, +1166/-5, D1 0128) |
| Sprint 274 | ✅ 완료 — F517 메타데이터 트레이서빌리티 (PR #539, +1317/-1, D1 0129+0130, Gap 100%) |
| Sprint 275 | ✅ 완료 — F518 Work Ontology KG (PR #541, D1 0131, TDD 18 tests, Gap 91%) |
| Sprint 276 | ✅ 완료 — F519 대시보드 현행화 (PR #543, +327/-5, Match 100%) |
| Sprint 277 | ✅ 완료 — F522+F523 shared 슬리밍 + D1 격리 (PR #544, +858/-38, 17 files) |
| Sprint 278 | ✅ 완료 — F524+F525 E2E 자동 추출 + Gap-E2E 통합 점수 (PR #548, +1093/-0, TDD 18/18, Composite 97%) |
| Sprint 279 | ✅ 완료 — F526 autopilot Verify E2E 통합 (PR #550, +532/-0, 6 files) |
| Sprint 280 | ✅ 완료 — F527 Agent Runtime L2 (PR #549, +1858/-0, 18 files, Match 97%) |
| Sprint 284 | ✅ 완료 — F531 발굴 Graph 실행 연동 (PR #558, TDD 41, Gap 95%) |
| Sprint 285 | ✅ 완료 — F532 에이전트 스트리밍 E2E (PR #559, +718, TDD+E2E) |
| Sprint 286 | ✅ 완료 — F533 MetaAgent 실전 검증 (PR #560, +868, D1 0134, TDD+E2E) |
| Sprint 287 | ✅ 완료 — F534 DiagnosticCollector 훅 삽입 (PR #564+#565 hotfix, 실증: agent_run_metrics 0→9건) |
| Sprint 288 | ✅ 완료 — F535 Graph 실행 정식 API + UI (PR #569, +705, D1 0135) |
| Sprint 289 | ✅ 완료 — F536 MetaAgent 자동 진단 훅 (PR #571, +496, TDD 189줄) |

| 항목 | 상태 |
|------|------|
| Workers | foundry-x-api.ktds-axbd.workers.dev ✅ |
| Pages | fx.minu.best ✅ |
| typecheck | ✅ |
| lint | ✅ (0 error) |

> **📏 실시간 수치** — 하드코딩 금지. `/ax:daily-check` 자동 수집 또는:
> ```
> wc -l SPEC.md && find packages/api/src/db/migrations/*.sql | sort | tail -1
> ```
> **마지막 실측** (Sprint 319, 2026-04-27): ~11 routes, ~34 services, ~14 schemas, D1 0139, 14 packages — Phase 45 Batch 5 완결 (F572+F574 ✅, fx-modules prod LIVE). **S312 수확**: **Sprint 318 F570 ✅** PR #687 `4c12cd84` Match 97% — Offering 완전 이관. (a) `offeringPacksRoute`(Sprint 81 F236/F238/F240) + `core/offering/` + `__tests__/` offering 20+건 MAIN_API → fx-offering 이관, (b) fx-gateway F541 proxy 12줄(biz-items business-plan/methodology, offerings, bdp, methodologies) 제거 + catch-all 단일화, (c) `grep -rn "core/offering"` fx-*/api 0건 확증, (d) Phase Exit P1~P4 Smoke Reality 통과. +340/-12089 87 files (대량 삭제 = legacy offering 정리). autopilot 34분 완결 (08:20 CREATED → 08:54 MERGED). **S306~S312 7 세션 연속 성공**: Match 95→100→100→97→98→100→97% (F560/F561+F562/F563/F564+F569/F567+F568/F565/F570). Phase 45 남은: F572+F574 Sprint 319 🔧(design, S313 병렬) / F571 Sprint 320 📋

## §3 Phase 진행 현황

> **Milestone = Phase 완료.** v2.5 이후 프로젝트-레벨 SemVer 중단, Phase 완료가 Milestone 역할 수행.  
> 전체 Phase 계획: [ROADMAP.md](ROADMAP.md) | 용어 정의: [taxonomy.md](docs/specs/fx-work-unit-taxonomy/taxonomy.md)

| Phase | 상태 |
|-------|:----:|
| Phase 33 Work Observability (F509) | ✅ Sprint 261 |
| Phase 34 Multi-Agent Sessions (F510) | ✅ Sprint 262 |
| Phase 35 Quality Hardening (F511) | ✅ Sprint 263 |
| **Phase 36 Work Management Enhancement** (F512~F515) | ✅ Sprint 264~266 |
| ↳ Sprint 264 — Phase 36-A(F512) + Phase 36-B 착수(F513) | ✅ |
| ↳ Sprint 265 — Phase 36-B 완성 (F514) | ✅ |
| ↳ Sprint 266 — Phase 36-C (F515) | ✅ |
| **Phase 37 Work Lifecycle Platform** (F516~F518) | ✅ Sprint 273~275 |
| **Phase 38 Dashboard Overhaul** (F519) | ✅ Sprint 276 |
| **Phase 39 MSA Walking Skeleton** (F520~F523) | ✅ Sprint 268(F520~F521), 277(F522~F523) |
| **Phase 40 Agent Autonomy** (F524~F526) | ✅ Sprint 278~279 |
| **Phase 41 HyperFX Agent Stack** (F527~F530) | ✅ Sprint 280~283 |
| **Phase 42 HyperFX Deep Integration** (F531~F533) | ✅ Sprint 284~286 |
| **Phase 43 HyperFX Activation** (F534~F537) | ✅ Sprint 287~289 + F537 hotfix |
| **Phase 44 MSA 2차 분리 + Agent 품질 튜닝** (F538~F544, F539 3분할, F540/F541 고도화) | 🔧 진행 — F542/F543/F544 ✅. F538 ✅(부분). F539 3분할 전부 MERGED. **F540 ✅(부분)** (S297 PR#598 Match 96%, 13 routes + 22 services, hotfix `2efc06f3` ESLint 8 errors). P1 확증 fx-shaping/fx-gateway smoke 200 ✅. KOAMI P2는 F541 이월. **남은**: F541 Offering(Sprint 298). Master pane: C69 2차(lint pre-check 추가) / C56 D1 격리 / C57 shared 슬리밍(F541 이후). 회고: `docs/04-report/features/phase-44-f539-retrospective.md` + `docs/04-report/features/sprint-297-f540-report.md` |

## §4 성공 지표

> 상세 KPI + 측정 방법: [BLUEPRINT.md](BLUEPRINT.md)

| 지표 | 목표 | 현재 |
|------|------|------|
| docs/ 활성 파일 | ≤ 100개 | 아카이브 진행 중 |
| Work Mgmt API 테스트 | 30건+ | TDD 예정 |
| Gap Analysis Match Rate | ≥ 90% | 전 Sprint 달성 |
| `--no-verify` 우회 비율 | < 20% | 0% |

## §5 기능 항목 (F-items)

> F-item SSOT. 파서(work.service.ts)가 이 섹션에서 Sprint/status를 추출함.
> Phase 36 F-items에 세부 상태 표기: `🔧(impl)`, `🔧(review)`, `📋(plan)` 등

### Phase 1~29 F-items (Archived)

> 510건 완료. 상세: `docs/archive/spec-fitems-phase1-29.md`

### Phase 30+ (Active)
| **Phase 30: 발굴 단계 평가결과서 v2 F493** | | | | F296 전면 개편 — AX BD 2단계 발굴 9-스테이지 리치 리포트. `docs/specs/axbd-skill/CLAUDE_AXBD/references/03_AX사업개발_발굴단계완료(안).html` 샘플 구조를 JSON 스키마로 정규화 |
| F493 | 발굴 단계 평가결과서 v2 — `/discovery/report` 전면 개편: AX BD 9단계(2-1~2-9) 리치 리포트 (레퍼런스/시장/경쟁/도출/선정/고객/BM/패키징/페르소나 평가). 기존 F296 `evaluation_reports` 스키마 확장(`report_data` JSON blob 추가 + migration) + 구조화 JSON ↔ React 9탭 렌더러 (카드/메트릭/표/BMC/SWOT/insight/next-step/Chart.js). 3개 아이템 수동 fixture seed (KOAMI, XR Studio, IRIS) 동봉 + prod D1 insert 스크립트. 샘플 HTML = `references/03_AX사업개발_발굴단계완료(안).html` (1522줄, Fooding AI 예시) (FX-REQ-485, P1) | Sprint 242 | ✅ | Sprint 242 완료 (Match Rate 97%). 구현 커밋 `08a94b98` + `b11528a4` + `54d2f646` + `8e05890c`. Migration 0124 + DiscoveryReportDataSchema(9탭 Zod) + `generateFromFixture()` + 3 fixture JSON(1,442줄) + `report-v2/` 블록 6종(797줄, recharts 재사용) + API/Web test. E2E는 smoke 수준(functional 보강은 후속). 상세: `docs/04-report/features/sprint-242.report.md` |
| F496 | 발굴 9기준 체크리스트 정보형 재설계 — `DiscoveryCriteriaPanel`이 사용자 액션 없이 AI 자동 평가만 일어나는 컴포넌트인데도 9개 행 펼침/접힘 리스트로 노출되어 시각적 부담이 큼. 정보형으로 압축: (1) 상단 큰 요약 카드 — 진행률(N/9) + gate badge + "AI가 자동 평가해요" 안내 (2) 3×3 컴팩트 그리드 — 9개 미니카드(번호 + 상태 아이콘 + 짧은 이름) (3) 단일 카드 클릭 시에만 상세(조건/근거/연결단계) 인라인 펼침 — 다른 카드 자동 닫힘. 사용자 DEBUG 제보(세션 #249): "AI 자동이라면 사용자 액션 필요 없으니 굳이 목록 나열할 필요 없음" (FX-REQ-488, P2) | — | ✅ | 세션 #249, Test 모드 직접 master push (d3b82787). DiscoveryCriteriaPanel 재작성 — 요약 카드 + 3×3 그리드 + 인라인 상세 (typecheck + 10/10 tests pass) |
| F495 | 파이프라인 재구조화 — 발굴/형상화 2-stage + 세부 진행률 + 발굴 파이프라인 카드. (1) `PipelineProgressStepper`에서 OFFERING/MVP 제거(Foundry-X 관리 범위 밖) → 발굴/형상화 2단계로 축소 (2) 각 stage에 sub-progress 표시(발굴: 9기준 진척, 형상화: 4 artifacts 진척) — 형상화 단계에서 4/4 완료 시 "완료" 라벨 노출(진행률 드리프트 해소) (3) 신규 `DiscoveryPipeline.tsx` 컴포넌트 — `ShapingPipeline` 미러, 9 스테이지(2-1~2-9) 읽기전용 시각화 (4) `discovery-detail.tsx` 발굴분석 탭에 DiscoveryPipeline 추가, 기존 DiscoveryStageStepper는 실행 엔진으로 유지. 사용자 DEBUG 제보(세션 #248): "파이프라인은 형상화 진행중인데 Prototype까지 완료된 상태", "OFFERING/MVP는 Foundry-X 관리 대상 아님", "발굴 파이프라인도 시각적으로 추가" (FX-REQ-487, P1) | — | ✅ | 세션 #249, Test 모드 직접 master push (d6d3ee28). PipelineProgressStepper 재작성, DiscoveryPipeline 신규, 단위 테스트 6건 갱신 (391/391 web tests pass) |
| F494 | 발굴 파이프라인 단계 전진 구조 버그 — `confirmStage`가 9/9 완료돼도 `pipeline_stages`를 DISCOVERY→FORMALIZATION으로 전진시키지 않음. `REGISTERED→DISCOVERY` 전환은 `biz-items.ts:327` 한 곳만 존재, 후속 전환 코드 부재. **추가 발견**: 기존 REGISTERED→DISCOVERY INSERT도 `entered_by` NOT NULL 제약으로 silent fail (try/catch 스왈로우) → REGISTERED 영구 고정의 숨은 원인. 영향: `PipelineProgressStepper`가 영원히 REGISTERED(0%)에 고정, `biz_items.status='evaluated'`와 `pipeline_stages='REGISTERED'` 영구 불일치. 실제 사례: bi-koami-001 세션 #247 DEBUG 제보. 수정: (1) `stage-runner-service.ts::confirmStage`에서 `gateStatus==='ready'` 시 `pipeline_stages` DISCOVERY → FORMALIZATION 자동 advance (멱등성 체크) (2) `biz-items.ts:327` INSERT에 `entered_by='system'` 추가 (silent fail 수정) (3) `DiscoveryReportView` 빈 `bd_artifacts` fallback — "발굴 단계 다시 실행하기" CTA (4) bi-koami-001 데이터 복구 SQL (pipeline advance + 9 criteria completed) (FX-REQ-486, P1) | — | ✅ | 세션 #247 debug 트랙, Test 모드 직접 master push (4931c6c1). 테스트 4건 추가 (9/9 전진 + 부분 미전진 + 멱등성 + stop 미전진), 3447 api tests pass. bi-koami-001 prod 데이터 복구 완료 |
| **Phase 31: Task Orchestrator F497 (S-α MFS)** | | | | PRD v0.4: `docs/specs/fx-task-orchestrator/prd-draft.md` — Master pane 내 WT split + F/B/C/X 4트랙 + SSOT 분리(SPEC=등록 / Issue label=실시간 상태) + flock 동시성 + merge/deploy hard gate |
| F497 | Task Orchestrator MVP (S-α) — `/ax:task start|list|adopt|doctor` 기본 경로. 범위: (1) `~/.foundry-x/` 인프라 (task-log.ndjson + locks/ 디렉토리 + pid/heartbeat) (2) flock 기반 ID allocator + master-push lock (3) push SHA 고정 + `git worktree add <SHA>` race 방지 (4) GitHub Issue label 12종 (fx:track/status/risk/wip) 자동 부여 (5) commit body `fx-task-meta` JSON 블록 — `.task-context` 유실 대비 권위 소스 (6) F/B/C/X 4트랙 등록 + REJECTED/ABORTED/CANCELLED/FAILED_SETUP/CLOSED_LEARNED 5종 종료 상태 (7) `/ax:task doctor` 9개 검사 — SPEC↔Issue↔WT split-brain reconcile. 비수용: state.json 신설(SSOT 철학 위반), LLM 완전 배제, `.task-context` git commit, high risk 무조건 차단. 외부 AI 3종(Gemini 3.1 Pro / GPT-5.4 Pro / Claude Opus 4.6) 검토 17개 결함 반영 (EX-1~EX-17). S-β 이후: merge gate 정확성 체인(typecheck/test/build/D1 dry-run), deploy hard gate, conflict-resolver subagent (FX-REQ-489, P1) | — | ✅ | S-α MVP ✅ (start + list 9 step end-to-end 검증). 산출물: `scripts/task/{lib.sh,task-start.sh,task-list.sh}` + `.claude/skills/ax-task/SKILL.md` + `~/.foundry-x/` 인프라. 검증 중 발견 2 버그 fix: (a) slugify sed collation 실패 → `LC_ALL=C tr -c` ASCII-safe (cb2a1e0a) (b) `${3:-{}}` bash 파싱 함정 → 명시 if문 (6bae6ecb). dogfood C1 task로 GitHub Issue #419 자동 생성 + WT + tmux pane + fx-task-meta commit 모두 검증. S-β: doctor/adopt/park/quick + heartbeat + Master IPC + merge gate |
| F499 | Task Orchestrator S-β — doctor/adopt/park 서브커맨드 + Master IPC. S-α(start+list) 확장: (1) `/ax:task doctor` 9개 검사 실행 경로 구현 — SPEC↔Issue↔WT split-brain reconcile (2) `/ax:task adopt` 고아 WT 인수 (3) `/ax:task park` 작업 일시정지 + 재개 (4) Master↔Worker IPC 채널 — heartbeat 기반 생존 감시(X1) + doctor 자동 트리거(X2 패턴 적용) (FX-REQ-495, P1) | Sprint 244 | ✅ | F497 S-β 후속. X1(heartbeat), X2(self-healing) 기반 확장 |
| F500 | Sprint auto Monitor+Merge 파이프라인 — Sprint WT 완료 Signal 감시→PR review→squash merge→cleanup 자동 실행 체인. 현재: merge-monitor.sh/sprint-watch 존재하나 자동 트리거 부재. C11 승격. 해결: `/loop` Signal 폴링 + sprint-pipeline Phase 4c~8 연결 (FX-REQ-494, P1) | Sprint 244 | ✅ | C11→F500 승격. scripts/sprint-* 영역 |
| **Phase 32: Work Management System (작업 관리 체계 고도화)** | | | | Jira 수준 구조화 — 4-Layer(Intake→Planning→Execution→Tracking) 체계 구축 |
| **Phase 32-A: 기초 인프라 (M1, P0)** | | | | |
| F501 | GitHub Projects Board 구성 — Repo 레벨 Kanban 6컬럼(Inbox→Backlog→Triaged→Sprint Ready→In Progress→Done) + 기존 Issues 마이그레이션 + 자동 라벨링 Actions (FX-REQ-496, P0) | Sprint 245 | ✅ | G1,G3,G6 해소. `gh project` CLI 기반 |
| F502 | CHANGELOG.md 도입 — Keep a Changelog 형식 + `/ax:session-end` 자동 갱신 + Phase 단위 Release Notes 생성 (FX-REQ-497, P0) | Sprint 245 | ✅ | G4 해소. Phase 완료 시 자동 생성 |
| **Phase 32-B: 파이프라인 연동 (M2, P0)** | | | | |
| F503 | `/ax:todo` Board 연동 — GitHub Projects API로 Backlog 자동 수집 + Sprint Ready 항목 자동 배정 + Board↔SPEC 양방향 동기화 (FX-REQ-498, P1) | Sprint 246 | ✅ | G3 해소. `scripts/board/{board-list,board-move,board-sync-spec}.sh`. ax 스킬은 별도 관리 |
| F504 | `/ax:session-end` Board 동기화 — merge 시 Board 컬럼 자동 이동(In Progress→Done) + PR 본문에 Sprint/F-item/Match Rate 삽입 (FX-REQ-499, P1) | Sprint 246 | ✅ | G6 해소. `scripts/board/{board-on-merge,pr-body-enrich}.sh` + sprint-merge-monitor Step 6 |
| **Phase 32-C: 메트릭/구조화 (M3, P1)** | | | | |
| F505 | Velocity 추적 — Sprint 완료 시 자동 메트릭 기록(F-item 수/Match Rate/소요시간) + `/ax:gov-retro` 연동 + Phase 단위 velocity 트렌드 (FX-REQ-500, P1) | Sprint 247 | ✅ | G5 해소 |
| F506 | Epic(Phase) 메타데이터 구조화 — GitHub Milestones로 Phase 매핑 + Phase 라벨 체계 + Phase 진행률 자동 계산 (FX-REQ-501, P1) | Sprint 247 | ✅ | G2 해소 |
| **Phase 32-D: 거버넌스 완성 (M4, P2)** | | | | |
| F507 | Priority 변경 이력 자동 기록 — P0~P3 변경 시 Issue comment + SPEC 이력 자동 기록 + `/ax:req-manage` 연동 (FX-REQ-502, P2) | Sprint 248 | ✅ | G7 해소 |
| **Phase 32-E: 통합 정합성 (M5, P1)** | | | | Phase 32 post-audit (S255) — dogfood 실패 + 연결 끊김 해소 |
| F508 | Phase 32 Integration Gap 해소 — ax 스킬(`/ax:todo`,`/ax:session-end`,`/ax:gov-retro`,`/ax:req-manage`)에 Phase 32 스크립트(`scripts/{board,priority,velocity,epic}/*`) 연결 + sprint-merge-monitor 훅 통합 + CHANGELOG 자동 롤업 + priority-history backfill + velocity phase 감지 버그 수정 (FX-REQ-503, P1) | Sprint 255 | ✅ | S255 완료 — **Foundry-X 측 10건** (sprint-merge-monitor 3훅 통합, CHANGELOG [Phase 32] 롤업, priority-history F507 backfill, velocity phase 4건 수정 + Sprint 245~248 backfill, board-sync-spec --fix 구현, record-change flock race guard, record-sprint clobber guard, _common.sh gh scope check) + **ax-marketplace 측 4 스킬** (gov-retro/req-manage/session-end/todo SKILL.md 통합). Match Rate 100% (dogfood F505 phase-trend.sh → Phase 32 4 sprints/7 F-items/98.5% 검증 성공) |
| **Phase 33: Work Management Observability (P0)** | | | | Hotfix 작업 관찰성 복원 — S260 `/ax:req-interview` dogfood 산출 |
| F509 | fx-work-observability Walking Skeleton — Backlog/REQ/Task/Sprint/Epic 4-channel 통합 뷰(Web UI + JSON API + CLI + Live feed) + 자연어→REQ 자동 분류 파이프라인 (FX-REQ-526, P0) | Sprint 261 | ✅ | [PRD: docs/specs/fx-work-observability/prd-v1.md]. Walking Skeleton M1~M4 완료(PR #503, Gap 98%): M1=GET `/api/work/snapshot`(SPEC.md GitHub raw + commits + PRs), M2=`/work-management` 4컬럼 Kanban + 5s polling, M3=GET `/api/work/context`(recent commits + next_actions), M4=POST `/api/work/classify`(Claude Sonnet LLM + regex fallback). Out-of-scope: 편집UI(read-only), RBAC(혼자 모드). Phase 2 Round 1 73/100 Conditional + ChatGPT flaw high 수동 보강 (review/round-1/, review-history.md v1 revised). Sprint 261 PR #503 merged `e942b87d` (8 files +1089) |
| **Phase 34: 멀티 에이전트 세션 표준화 (P0)** | | | | Claude Squad 도입 + Foundry-X 인프라 연동 — C-track 14건 패치 누적의 근본 해소 |
| F510 | 멀티 에이전트 세션 표준화 — Claude Squad 도입 + 프로파일 3종(coder/reviewer/tester) + sprint N 훅 연동 + git-workflow 정합성 + 웹 Kanban 세션 상태 노출 (FX-REQ-533, P0) | Sprint 262 | ✅ | [PRD: docs/specs/fx-multi-agent-session/prd-final.md]. M1=cs alias 3종(bashrc), M2=wt-claude-worktree.sh cs 자동실행, M3=git-workflow.md cs 규칙, M4=PR #511 `06e8f6d3` (8 files +800, D1 0126 agent_sessions + GET/POST sessions + Sessions 탭 + collector). Gap 98%. C28 재발 방지 3중 방어선(하드코딩+보정+테스트 11 PASS) |
| **Phase 35: Work Management 품질 보강 (P1)** | | | | F509+F510 Walking Skeleton 인증·에러·테스트 품질 보강 |
| F511 | Work Management 품질 보강 — API 에러 핸들링 UI + syncSessions SQL injection 방어 + E2E edge case 보강 (FX-REQ-534, P1) | Sprint 263 | ✅ | PR #516 `a756780a`. Gap 97%. T3=prepared stmt bind, T4=CASE WHEN ORDER BY, G1=에러 UI(useRef stale guard), T1+T2=E2E 6→11건(+5). 선행 핫픽스: PR #514(401 auth), PR #515(sidebar topItems), D1 DROP+재생성(스키마 불일치) |
| **Phase 36: Work Management Enhancement (프로젝트 관리 체계 개선)** | | | | PRD: `docs/specs/fx-work-mgmt-enhancement/prd-final.md`. 3-AI 검토 R2 76/100, Ambiguity 0.087. "Jira를 만들지 않는다" — Blueprint/Roadmap 독립 + SPEC 경량화 + 기존 데이터 시각화 + TDD |
| **Phase 36-A: 문서 체계 정비 + 아카이브 (meta-only)** | | | | 코드 변경 0건, master 직접 commit |
| F512 | 문서 체계 정비 + 아카이브 — A-0 파서 테스트 보강 + BLUEPRINT.md/ROADMAP.md 도입 + SPEC.md 경량화(1377→350줄) + docs/ 아카이브(400→100파일) + F-item 세부 상태 10단계 + Entry/Exit Criteria (FX-REQ-535, P0) | Sprint 264 | ✅ | A-0 ✅ PR#517, A-1 ✅ BLUEPRINT, A-2 ✅ ROADMAP, A-3~A-8 ✅ (경량화+아카이브+괄호표기+process-lifecycle) |
| **Phase 36-B: 기존 데이터 표면화 — TDD (코드 변경)** | | | | B-0 테스트 보강 선행 → B-1~B-5 Red→Green→Refactor |
| F513 | Work Management API 테스트 보강 + 확장 — B-0 기존 snapshot/context/classify 테스트 ~15건 + B-1 GET /api/work/velocity + B-2 GET /api/work/phase-progress + B-3 GET /api/work/backlog-health (FX-REQ-536, P0) | Sprint 264 | ✅ | PR #518. TDD Red(24건)→Green. velocity/phase-progress/backlog-health API + Zod 스키마 |
| F514 | Work Management 대시보드 확장 — B-4 Pipeline Flow 뷰(Idea→Done 단계별 F-item 수) + B-5 Velocity/Phase Progress 차트 + E2E 테스트 (FX-REQ-537, P1) | Sprint 265 | ✅ | PR #524. Pipeline Flow + Velocity + Backlog Health 대시보드 |
| **Phase 36-C: 자동화 연결** | | | | Phase A/B 자동화 스크립트 연결 |
| F515 | 자동화 연결 — C-1 board-sync-spec 세부 상태 파싱 + C-2 Roadmap 자동 갱신 + C-3 Blueprint 버전 범프 + C-4 아카이브 자동화 + C-5 CHANGELOG 자동 생성 (FX-REQ-538, P1) | Sprint 266 | ✅ | PR #529. Gap 95.1%. 5종 스크립트 |

| **Phase 37: Work Lifecycle Platform (작업 라이프사이클 플랫폼)** | | | | PRD: `docs/specs/fx-work-lifecycle-platform/prd-final.md`. Backlog 인입→AI 자동분류→트레이서빌리티→Ontology |
| F516 | Backlog 인입 파이프라인 + 실시간 동기화 — (1) 웹 제출 폼 + AI 자동 분류/등록/알림 (2) GitHub Webhook→D1 캐시→SSE 실시간 파이프라인 (3) CLI task-start/session-end→API 직접 POST (4) 웹 SSE 수신으로 5초 polling 대체 (5) Marker.io webhook 연동. 지연 7~8초→1~2초 (FX-REQ-544, P0) | Sprint 273 | ✅ | PR #538. +1166/-5, 12 files. D1 0128, TDD 32 tests, SSE 실시간 |
| F517 | 메타데이터 트레이서빌리티 — REQ↔F-item↔Sprint 연결 D1 테이블 + Sprint↔PR↔Commit GitHub API 파싱 + Changelog 구조화(REQ/PR 메타태깅) + 추적 뷰 (FX-REQ-545, P0) | Sprint 274 | ✅ | PR #539. +1317/-1, 11 files. D1 0129+0130, TDD 19 tests, Gap 100% |
| F518 | Work Ontology 기반 연결 — KG 스키마(10노드타입/5엣지타입) + kg_nodes/kg_edges D1 테이블 + SPEC/GitHub→노드 자동생성 + KG 쿼리 API + 공개 Roadmap/Changelog 뷰 (FX-REQ-546, P0) | Sprint 275 | ✅ | PR #541. D1 0131, TDD 18 tests, Gap 91%. 공개 /roadmap + /changelog |

| **Phase 38: Dashboard Overhaul (대시보드 현행화)** | | | | 서비스 범위(발굴~형상화)에 맞춰 대시보드 전면 정리 |
| F519 | 대시보드 현행화 — (1) 파이프라인 6단계→2단계 축소 (2) 퀵 액션 dead link 제거 (3) 내부 위젯 4개 삭제 (4) ToDo List UI/UX (5) 업무 가이드 Wiki 대체 (FX-REQ-547, P1) | Sprint 276 | ✅ | PR #543. TodoSection dead link 수정, Match 100% |

| **Phase 39: MSA Walking Skeleton** | | | | PRD: `docs/specs/fx-msa-roadmap/prd-final.md`. 도메인별 서비스 분리 + API 게이트웨이 + D1 격리 |
| F520 | API 게이트웨이 Worker — fx-gateway Worker 신규, Service Binding 라우팅, 하위 호환 (FX-REQ-548, P0) | Sprint 268 | ✅ | PR #535. packages/fx-gateway/ 생성 |
| F521 | Discovery 도메인 분리 — core/discovery 12 routes + 18 services를 독립 Worker로 추출 (FX-REQ-549, P0) | Sprint 268 | ✅ | PR #535. packages/fx-discovery/ 생성 |
| F522 | shared 타입 슬리밍 — 도메인 전용 타입을 각 Worker 내부로 이동, 크로스도메인 계약만 shared 유지 (FX-REQ-550, P0) | Sprint 277 | ✅ | PR #544. fx-discovery 타입 이동, shared re-export 유지 |
| F523 | D1 스키마 격리 — Discovery 전용 D1 바인딩 분리, 크로스도메인 JOIN 대체 방안 확정 (FX-REQ-551, P0) | Sprint 277 | ✅ | PR #544. F522와 통합, Option B(re-export) 유지 |

| **Phase 40: Agent Autonomy — E2E 자동화 + Gap 통합** | | | | PRD: `docs/specs/fx-agent-autonomy/prd-final.md`. E2E 자동 생성 + Gap-E2E 통합 품질 점수 |
| F524 | E2E 시나리오 자동 추출 — Design 문서 §4+§5에서 Playwright spec 자동 생성, 템플릿+NLP 파싱 (FX-REQ-552, P0) | Sprint 278 | ✅ | PR #548. e2e-extractor.ts, TDD 9/9, Gap 95% |
| F525 | Gap-E2E 통합 점수 — Gap Analysis에 E2E PASS/FAIL 포함, Gap×0.6+E2E×0.4 가중 평균 (FX-REQ-553, P0) | Sprint 278 | ✅ | PR #548. gap-scorer.ts, TDD 9/9, Composite 97% |
| F526 | autopilot Verify E2E 통합 — sprint-autopilot Step 5~6에 E2E 생성+실행 자동 삽입 (FX-REQ-554, P0) | Sprint 279 | ✅ | PR #550. +532, 6 files |
| **Phase 41: HyperFX Agent Stack — 4-Layer 에이전트 재구조화** | | | | PRD: `docs/specs/fx-hyperfx-agent-stack/prd-final.md`. Walking Skeleton 접근 — 선언적 에이전트 + Graph 오케스트레이션 + 실시간 스트리밍 + 자기개선 |
| F527 | Agent Runtime (L2) — defineTool() 유틸리티 + AgentSpec YAML 스키마 + AgentRuntime 실행 엔진 + Hooks + TokenTracker + ToolRegistry + 기존 7 에이전트 YAML 마이그레이션 (FX-REQ-555, P0) | Sprint 280 | ✅ | PR #549, +1858, TDD, Match 97% |
| F528 | Graph Orchestration (L3) — GraphEngine(GraphBuilder API + 조건부 라우팅 + 병렬 실행) + Agents-as-Tools + SteeringHandler + ConversationManager + OrchestrationLoop 래핑 + AX BD 발굴 9단계 Graph 정의 (FX-REQ-556, P0) | Sprint 281 | ✅ | PR #552, +1919, TDD 5, Match 97% |
| F529 | Agent Streaming (L1) — WebSocket 에이전트 이벤트 스트리밍 + SSE 폴백 + D1 메트릭 저장 + Web 실시간 대시보드 (FX-REQ-557, P0) | Sprint 282 | ✅ | PR #553, +2112, TDD 2, D1 0132 |
| F530 | Meta Layer (L4) — DiagnosticCollector 6축 메트릭 수집 + MetaAgent 진단→개선 제안 + Human Approval UI (FX-REQ-558, P1) | Sprint 283 | ✅ | PR #555, +1755, TDD 3, D1 0133 |

| **Phase 42: HyperFX Deep Integration — Walking Skeleton → 실 데이터 연동** | | | | Phase 41 골격에 실 비즈니스 로직 연결 — 발굴 Graph 실행, 스트리밍 E2E, MetaAgent 실전 검증 |
| F531 | 발굴 Graph 실행 연동 — createDiscoveryGraph()를 stage-runner-service와 연결하여 9단계 발굴 파이프라인이 GraphEngine으로 실행되도록 통합. 기존 OrchestrationLoop 실행 경로에 Graph 분기 추가 + confirmStage→GraphEngine 노드 실행 위임 + 실제 LLM 호출 연결 + 단계별 결과 D1 저장 (FX-REQ-561, P0) | Sprint 284 | ✅ | PR #558, TDD 41 tests, Gap 95% |
| F532 | 에이전트 스트리밍 E2E — WebSocket/SSE 스트리밍 레이어의 end-to-end 검증. Agent 실행→이벤트 발행→WebSocket 전달→Web 대시보드 렌더링 전 구간 통합 테스트 + Playwright E2E + 연결 끊김/재접속 복원력 테스트 (FX-REQ-562, P0) | Sprint 285 | ✅ | PR #559, +718, TDD+E2E |
| F533 | MetaAgent 실전 검증 — DiagnosticCollector로 실제 에이전트 실행 메트릭 수집 + MetaAgent가 진단→개선안 생성→Human Approval→반영까지 full loop 검증. 테스트 시나리오: 발굴 Graph 1회 실행 후 MetaAgent 자동 진단 (FX-REQ-563, P1) | Sprint 286 | ✅ | PR #560, +868, D1 0134, TDD+E2E |

| **Phase 43: HyperFX Activation — Plumbing → Real Data Flow** | | | | Dogfood(S276, KOAMI)에서 확증된 3개 갭 해소 — 메트릭/Graph 트리거/자가개선 루프를 실제 데이터 흐름에 연결 |
| F534 | DiagnosticCollector 실행 경로 훅 삽입 — StageRunnerService.runStage() 및 OrchestrationLoop 실행 경로에 `DiagnosticCollector.record()` 호출 추가. 각 LLM 호출마다 tokens/duration/success/tool_use 등 메트릭을 agent_run_metrics에 기록. Dogfood 확증: 9-stage Graph 실행 성공에도 0건 기록 (session graph-dogfood-bi-koami-001-1776125192189) (FX-REQ-564, P0) | Sprint 287 | ✅ | PR #564, +678, TDD 2 files |
| F535 | Graph 실행 정식 API + UI — `POST /biz-items/:id/discovery-graph/run-all` 정식 API(PR #563 임시 버전 대체) + 웹 UI 'Graph 모드 실행' 버튼 + stage별 진행률 표시 + sessionId 저장/조회. 현재 `confirmStage(graphMode=true)` 옵션이 API 미노출 (FX-REQ-565, P1) | Sprint 288 | ✅ | PR #569, +705, D1 0135 graph_sessions |
| F536 | MetaAgent 자동 진단 훅 — Graph/Agent 실행 완료 시점에 MetaAgent.diagnose() 자동 트리거 (hook 기반) → 경계 점수 미달 시 proposal 생성. Dogfood 확증: 현재 모든 6축 score=50, rawValue=0 (입력 데이터 부재) (FX-REQ-566, P1) | Sprint 289 | ✅ | PR #571, +496, TDD 189줄 |
| F537 | **hotfix** MetaAgent auto-trigger session_id 불일치 해소 — F536 autoTriggerMetaAgent가 graph sessionId(`graph-*`)로 collect했으나 stage-runner는 `stage-{stage}-{bizItemId}` 패턴으로 기록해 매칭 실패. Dogfood 3차(graph-bi-koami-001-1776130774847)에서 agent_run_metrics 9→18 정상 수집 확인했으나 agent_improvement_proposals 0건 발견. 수정: `DiagnosticCollector.collectByBizItem()` 추가(LIKE `stage-%-{bizItemId}`) + autoTriggerMetaAgent에 bizItemId 인자 (FX-REQ 미배정, post-merge hotfix) | — | ✅ | PR #573 MERGED (`3ca8285e`). pane %3 Sprint 288 세션에서 작업 완료 |
| **Phase 44: MSA 2차 분리 — Discovery 완전 분리 + 도메인 Worker 확장** | | | | 근거: `docs/specs/fx-msa-roadmap/msa-transition-diagnosis.md` §3.2~§3.3. W+8+ 범위 (fx-msa-roadmap-v2 PRD §4.3에서 out-of-scope 선언). W+5 GTM 종료 후 재평가, W+6+에 REQ/Sprint/상세 범위 확정. 현재는 Idea 단계만 등록 |
| F538 | Discovery 완전 분리 — fx-discovery Worker에 12 routes / 18 services 전체 이전 + `packages/api`에서 제거. Walking Skeleton(Phase 39) 구조를 프로덕션 수준으로 완성. F543 CONDITIONAL GO 기반 착수 (Service Binding 오버헤드 +10~14ms 허용). 범위: (a) discovery 도메인 core/discovery/* 코드 + D1 schema + 테스트 일괄 이전, (b) `packages/api`에서 discovery 라우트 mount 제거 + msa-lint 룰 통과, (c) fx-gateway의 service binding 라우팅 검증, (d) E2E discovery 경로 통과 확인, (e) Stage 3 Exit D1~D3 필수 — 소비자 전수 grep(web/cli/타 도메인 import 지점), D1 cross-domain JOIN 영향도, API 계약 호환성, (f) 롤백 플랜(binding 오프 시 origin 라우트 잔존). Phase Exit P1~P4 Smoke Reality: fx-gateway 경유 Dogfood 1회 필수 (FX-REQ-575, P0) | Sprint 293 | ✅ **(부분)** | PR #588 (`49d97259`, +1662/-116, 31 files), Match 95%. **실현 범위 3/10 routes** — discovery/discovery-report/discovery-reports만 이전(clean routes). 7개 라우트(bizItems/axBdDiscovery/axBdArtifacts/discoveryPipeline/discoveryStages/discoveryShapePipeline/discoveryStageRunner)는 18개+ cross-domain 의존성으로 F539에서 Service Binding 방식 검토로 이월. Gap 해소: shaping 4파일 cross-domain → @foundry-x/shared, graphDiscovery dead code 제거. fx-discovery에 JWT auth + tenantGuard 미들웨어 추가. `discovery-contract.ts` 신설(BdArtifact/ExecuteSkillInput 등). TDD 12/12 PASS. **Phase Exit P1~P4 Smoke Reality 완결** (2026-04-15 via fx-gateway URL 직접): `/api/discovery/health` → fx-discovery JSON 응답 ✅, `/api/discovery/progress` → 401 (auth protected route 존재 확증) ✅. **배포 연쇄 문제 5건** 발견+해소: (1) F538 merge test FAIL(obsolete test 파편) → PR#589, (2) path filter로 deploy-msa skip, (3) wrangler-action workspace:* 미지원 → PR#590, (4) workflow dispatch 필요, (5) wrangler 경로 해결 실패 → PR#591 (`../api/node_modules/.bin/wrangler`). 각 원인 feedback에 기록 |
| F539a | **k6 Cloud 재측정 + Go/No-Go 판정** (F539 분할 #1, PRD `docs/specs/fx-gateway-cutover/prd-final.md` §4.1). F543 CONDITIONAL GO 조건 A2 해소. k6 Cloud Seoul 리전 4 엔드포인트(E1~E4) 측정 → Service Binding 증분 p99 판정(<50ms Go / 50~150ms 조건부 / >150ms No-Go) → `docs/04-report/features/phase-44-f539a-k6-cloud.md` 리포트 + F543 비고 역동기화. 전제: F538 1차 완료(✅) (FX-REQ-576, P0) | Sprint 294 | ✅ | PR #595 (`feat(benchmark): F539a — k6 재측정 GO 판정`). k6 로컬 1226샘플/endpoint(k6 Cloud account 미설정→Fallback 1 선택). 증분 p50=+5ms(Go 기준 <50ms 충족, S291 curl +10ms과 일관) / p99=-59ms(WSL→CF 지리 jitter 노이즈 지배, 신뢰 불가 판정). **판정: GO** — F539b 즉시 착수. **PRD drift 관찰**: k6 Cloud Seoul 리전 요구를 로컬 Fallback로 진행—A2 엄밀 해소는 미완이나 p50 유효 지표로 실용 판정. **신규 발견**: `/api/discovery/items` JWT 401(E4 미측정) → F539b Plan 최우선 점검(인증 전파). Match 92%, Test pass. `benchmarks/phase-44-latency/k6-cloud.js` + `docs/04-report/features/phase-44-f539a-k6-cloud.md` |
| F539b | **fx-gateway 프로덕션 배포 + URL 전환 + 롤백** (F539 분할 #2, PRD §4.2). fx-gateway Worker 프로덕션 routes 설정 + VITE_API_URL 전환(Web+CLI) + 롤백 리허설 1회 + KOAMI Smoke Reality(bi-koami-001 Graph proposals ≥ 1건). 선제 체크리스트 5항목(feedback_msa_deploy_pipeline_gaps.md)을 Design 단계에서 검증. 전제: F539a Go 또는 조건부 Go 판정 (FX-REQ-577, P0) | Sprint 295 | ✅ **(부분)** | PR #596 (`feat(fx-gateway): CORS + VITE_API_URL`). Match 100%, TDD 8/8 PASS. 변경: fx-gateway CORS 미들웨어(`hono/cors`), `.env.production` + `.env.example` → `https://fx-gateway.ktds-axbd.workers.dev/api`, 롤백 드릴 리포트(`docs/04-report/features/phase-44-f539b-rollback-drill.md`). 선제 체크리스트 4/5 PASS(test 파편 N/A). **Phase Exit 이월 2건**: (1) KOAMI Smoke Reality 미실행(Sprint 완료 이후 별도 실측 예정 — P1~P4 미완결), (2) **CLI URL 전환 누락**(PR 파일 8개 중 `packages/cli/*` 없음 — PRD §4.2 F539b-4의 "Web + CLI" 중 Web만). 후속: F539c 범위에 CLI 전환 흡수 또는 별도 Hotfix 판단 |
| F539c | **F538 이월 7 라우트 Service Binding 이전 + CLI URL 전환 흡수** (F539 분할 #3, PRD §4.3). 2 PR 분할 — PR1 Group A(bizItems 3개), PR2 Group B(discoveryPipeline/stages 4개). 각 PR KOAMI Smoke Reality + packages/api 관련 test 파편 삭제 + ESLint no-cross-domain-import 확장 + Phase 44 f539 회고(`docs/04-report/features/phase-44-f539-retrospective.md`) 작성. **범위 추가(S295 이월)**: CLI URL 전환(F539b 누락분) + F539b KOAMI Smoke Reality 통합 실행. 전제: F539b 프로덕션 배포 완료(✅ 부분) (FX-REQ-578, P0) | Sprint 296 | ✅ **(부분)** | PR #597 (`feat(msa): F539c — 7 routes to fx-discovery via Service Binding`), Match 95%, 30 files. 완료: Group A(biz-items CRUD 3) + Group B(discovery-stages/pipeline 4) fx-discovery 이전, packages/api 7 handlers 제거(migration comments), fx-discovery 11 new tests GREEN, fx-gateway 8 tests, typecheck 13 packages PASS, turbo test 3721+ tests. **CLI URL 전환**: `scripts/*.sh` 7개(feedback-consumer/seed/session-collector/sf-scan/skill-demo/task-daemon/usage-tracker) fx-gateway URL로 — `packages/cli/`는 API URL 하드코딩 없음(N/A). **Phase Exit drift 3건**: (1) **2 PR 분할 미이행** — 단일 PR #597로 통합(결과 동일하나 Group별 독립 Smoke 검증 포기), (2) **KOAMI Smoke Reality P2 미실측** — PR Test plan에 Graph 실행 증거 없음, `packages/web/e2e/prod/smoke.spec.ts`만 추가, (3) **Retrospective 누락** — `phase-44-f539-retrospective.md` 미생성. 후속: 별도 task로 Smoke + Retrospective 처리 |
| F540 | **Shaping 도메인 분리** — fx-shaping Worker 신규 생성(Walking Skeleton 없음), 14 routes / 23 services → 독립 Worker. 범위: (a) `core/shaping/*` 코드 + D1 schema(shaping_* 테이블) 이전, (b) fx-gateway Service Binding 통합 + CORS/JWT 전파, (c) packages/api shaping mount 제거 + msa-lint 통과, (d) cross-domain import(discovery→shaping, shaping→offering) 전수 grep + contract 분리, (e) E2E Shaping Graph 경로 확증(F535 PR/M1 시나리오 재사용), (f) 배포 파이프라인 preflight 5항목(feedback_msa_deploy_pipeline_gaps) PRD §6.1 체크, (g) Smoke Reality: KOAMI bi-koami-001 Shaping 단계 Graph 실행 proposals ≥ 1건. 전제: F539a/b/c 완료 + C69 preflight 자동화 적용 권장 (FX-REQ-579, P0) | Sprint 297 | ✅ **(부분)** | PR #598 (`feat(msa): F540 — fx-shaping Worker 신규 생성`, 93 files), Match 96%. **실제 산출**: 13 routes / 22 services / 15 schemas (계획 14/23/15 대비 1-1 축소). fx-gateway SHAPING binding + `/api/shaping/*` + `/api/ax-bd/*` 라우팅. packages/api 13 mount 제거. deploy.yml fx-shaping paths-filter + step 추가. TDD 4/4 PASS. **배포 연쇄 1건 발생+해소**: autopilot `pnpm lint` 누락 → CI test job FAIL → deploy 3 job skip → **hotfix `2efc06f3` ESLint 8 errors 일괄 해소(7 수동 + 1 auto-fix) → deploy.yml 24446036032 success**. **Phase Exit P1 확증** (hotfix 후): fx-shaping `/api/shaping/health` 200 ✅, fx-gateway `/api/shaping/health` 200 ✅ (Service Binding 동작), `/api/ax-bd/bmc` 401 (auth route 존재). P2 KOAMI Smoke는 F541 통합 이월. **교훈**: autopilot lint pre-check 누락 → C69 2차 개선 대상 |
| F541 | **Offering 도메인 분리** — fx-offering Worker 신규 생성, 12 routes / 23 services → 독립 Worker. Shaping 다음 파이프라인 단계. 범위: (a) `core/offering/*` 코드 + D1 schema(offering_* 테이블) 이전, (b) fx-gateway Service Binding 통합, (c) packages/api offering mount 제거 + msa-lint 통과, (d) cross-domain import(shaping↔offering) 전수 grep + contract 분리, (e) F540 Shaping 선례 활용으로 Sprint Plan/Design 단순화, (f) Smoke Reality: bi-koami-001 Offering 단계 Graph, (g) `docs/specs/axbd/shape/` relocation(PR #623) 사후 검증 — ax-bd-offering-agent 호출 시 broken path refs 0건 확인(`grep -rn "\.claude/skills/ax-bd/shape"` 결과 archive 제외 0건). 전제: F540 완료 (FX-REQ-580, P0) | Sprint 309 | ✅ | **PR #624 MERGED** (2026-04-18, Match 100%, autopilot). Sprint 298→299→309 2차 이월 후 S300 착수. fx-offering Worker 신규 생성, fx-gateway Service Binding 통합, F540 선례 기반 자동 완료. autopilot 진입 전 pre-check(pnpm lint + C69 preflight) 적용. Phase Exit P1~P4 적용 예정(KOAMI Smoke P2 후속). 후속 `docs/specs/fx-msa-followup/prd.md` Phase 45 제안 문서 등록 |
| F543 | Phase 44 gating — Service Binding latency 벤치마크 (C58 승격). fx-gateway → fx-discovery 호출 경로를 k6로 p99 측정 + Go/No-Go 판정 리포트. 기준: p99 < 100ms (가칭, 벤치마크 결과로 확정). Go 판정 시 F538 착수, No-Go 시 PRD 재설계 필요. 산출: `benchmarks/phase-44-latency/` 스크립트 + `docs/04-report/features/phase-44-latency-decision.md` 판정 리포트. 전제: Phase 39 Walking Skeleton (fx-gateway F520 + fx-discovery F521) (FX-REQ-573, P0) | Sprint 291 | ✅ | PR #583 (`6902ec1f`). curl 벤치마크 30샘플×2: Service Binding 오버헤드 p50 +10ms (health) / +14ms (items). WSL Korea 환경 절대값은 지리적 레이턴시 지배. 판정: **CONDITIONAL GO** — F538 착수 승인. Match 100%. 재검증: F539 전 k6 Cloud 권장 (A2). **F539a 재검증(Sprint 294)**: k6 로컬 1226샘플×3 endpoints. E1(direct) p50=180ms / E2(gateway+SB) p50=185ms. **증분 p50=+5ms, p99 증분=-59ms(노이즈). 최종 판정: ✅ GO** (A2 해소). `docs/04-report/features/phase-44-f539a-k6-cloud.md`. task-daemon 오판: e2e (4,4) 단일 shard FAILURE로 STATUS=FAILED 기록했으나 실제 MERGED (C64 패턴 재발) |
| F542 | **MetaAgent 프롬프트 품질 개선 + 모델 전환 실험** — Phase 43 Dogfood에서 agent_improvement_proposals=0건 원인 해소. (a) `meta-agent.agent.yaml` systemPrompt 강화 — rawValue=0/low-signal 입력 명시적 지침 + few-shot 2~3건 + 출력 형식 재검증, (b) Haiku 4.5 → Sonnet 4.6 A/B 실험 — config flag `META_AGENT_MODEL` 런타임 전환(기본값 **Sonnet 4.6**) + 동일 DiagnosticReport에 대한 두 모델 결과 비교 기록, (c) proposals 품질 rubric 점수화(재현성/실행가능성/근거명시), (d) Apply 경로 E2E 검증, (e) 2차 Dogfood 6축 score 방향성 이동 실측. Dogfood P2(실측 산출물 ≠ 0건) 체크리스트 PASS 필수. MVP: K1≥1건+K3=100%. 5회 반복 후 중단 룰. R6(rawValue=0 근본원인) 발견 시 F543 분리. 전제: Phase 43 F537 hotfix 완료(✅) (FX-REQ-571, P1) | Sprint 290 | ✅ | PR #579 (`a4d2734e`, +669/-32), TDD 21 tests, D1 0136+0137. **Dogfood P2 PASS** (S290, bi-koami-001/graph-…1776147547955): proposals 6건 실측 + rubric_score=100 6/6, types 5×graph+1×prompt. MVP K1≥1✅. Observed: auto-trigger(F536) 호출 경로는 proposals 저장 안 됐으나 manual `/api/meta/diagnose`는 정상 저장 → C65 분리 |
| F544 | **F536 auto-trigger 저장 경로 복구** (C65 승격). Graph 완료 시 `autoTriggerMetaAgent` hook이 호출되지만 `agent_improvement_proposals` 미저장. Dogfood 실측(S290, graph-bi-koami-001-1776147547955)에서 auto-trigger 후 0건, 동일 세션 manual `/api/meta/diagnose`는 6건 정상 저장 — 저장 경로 분기 또는 hook 입력 누락 확인됨. 작업: (a) agent-graph-run-service + autoTriggerMetaAgent에 구조 로깅(session_id, bizItemId, proposals.length) 추가, (b) hook 호출 trace — 이벤트 발화 여부 + 반환값 + DB write attempt 확인, (c) manual 경로와 auto 경로의 save 함수 단일화(중복 분기 제거), (d) 회귀 방지 integration test — Graph 완료 후 proposals > 0 보장, (e) Dogfood P3(3차 KOAMI 반복)에서 auto-trigger만으로 proposals ≥ 1건 실측 검증. Phase 43 Exit P1~P4 재검증 목적. MVP: K1(auto-trigger proposals ≥ 1건)+K2(manual==auto 경로 동일) PASS. 전제: F542 ✅ (Sonnet 4.6 + rubric 인프라 활용) (FX-REQ-574, P1) | Sprint 292 | ✅ | PR #582 (`8d3eac94`, +438/-18, 5 files). Sprint WT autopilot 10분 완료 Match 93%. 변경: `discovery-stage-runner.ts` stage 완료 hook 분기 통합(+50/-18) + integration test `meta-agent-full-loop.test.ts`(+49) + unit test `meta-agent-auto-trigger.test.ts`(+123). Plan+Design 문서 생성. **P3 Dogfood PASS** (2026-04-15, session `graph-bi-koami-001-1776206794085`, Graph 210s 완료 후 auto-trigger hook이 3 proposals 저장: discovery-stage-runner × prompt/prompt/tool, rubric_score=100×3). MVP K1(≥1) ✅ + K2(manual==auto 동일 save 경로) ✅. Phase 43 Exit P1~P4 Smoke Reality 완결. **관찰**: task-daemon STATUS=FAILED 오판(PR merged + post-merge deploy.yml 초회 실패) → C64 패턴 재발 확인. deploy.yml 실패 원인은 F544 무관 `prototype-builder/build-queue.test.ts` flaky timing test. F543 merge 트리거 재배포(24385703119)에서 flaky 통과로 prod 반영 완료 |
| F545 | **fx-ai-foundry-os: 3-Plane 랜딩 대시보드** — `/ai-foundry-os` 라우트 신설, DeepDive v0.3 Input/Control/Presentation Plane 3 카드 + "라이브 시연" 진입 버튼. 대표 보고(4/17 09:00) 랜딩 페이지. PRD: `docs/specs/fx-ai-foundry-os/prd-final.md` (FX-REQ-581, P0) | Sprint 298 | ✅ | PR #601 (`packages/web/src/routes/ai-foundry-os/index.tsx`). req-interview Round 1 Score 85/100 |
| (Sprint 298 묶음) | F545~F549 **fx-ai-foundry-os MVP** — PR #601 squash merge 2026-04-16T15:40Z (auto-merge). +2012/-0, 17 files, TDD 6 tests GREEN, Match 100%, autopilot 16분. 산출: `packages/api/src/core/decode-bridge/` (client+routes+schemas+mock) + `packages/web/src/routes/ai-foundry-os/` (index/demo/lpon/harness/ontology). **배포 전 필수**: Foundry-X API에 `DECODE_X_INTERNAL_SECRET` wrangler secret 등록 (Sinclair 직접). **Smoke Reality 미검증**: fx.minu.best/ai-foundry-os 접근 후 4/17 09:00 리허설에서 확증 필요 | — | — | — |
| F546 | **fx-ai-foundry-os: fx-decode-bridge 연동 레이어** — `packages/api/src/core/decode-bridge/` 신설, Decode-X Workers(KTDS-AXBD/Decode-X) 5 라우트 프록시: `/analyze`, `/analysis/:documentId/summary`, `/findings`, `/compare`, `/core-processes`. X-Internal-Secret 전달 + Zod 스키마 부분 미러. Service Binding 가능 시 binding 우선, 아니면 fetch. PRD: `docs/specs/fx-ai-foundry-os/prd-final.md` §9 (FX-REQ-582, P0) | Sprint 298 | ✅ | Decode-X URL/secret 확보가 Open Issue #1 (H+2 마감) |
| F547 | **fx-ai-foundry-os: LPON Type 1 시연 페이지** — `/ai-foundry-os/demo/lpon` — Decode-X 3-Pass(scoring/diagnosis/comparison) 결과 시각화 + "Type 1 반제품(Code+Test)" 다운로드 버튼. 다운로드 본체는 Decode-X `반제품-스펙/pilot-lpon-cancel/working-version/` 완성 반제품 패키징 (6종 Spec md + src/ Code + __tests__/ + migrations/0001_init.sql). 성공 KPI: 대표 보고 rehearsal walkthrough 통과. PRD: `docs/specs/fx-ai-foundry-os/prd-final.md` (FX-REQ-583, P0) | Sprint 298 | ✅ | F546 선행 필수. 도메인 선정: 온누리상품권(LPON) — 퇴직연금은 DeepDive A-4 예시이나 반제품 미구현 (2026-04-16 재판정) |
| F548 | **fx-ai-foundry-os: Harness 5종 체크리스트 UI** — `/ai-foundry-os/harness` — KT연계·사업성·리스크·AI-Ready Data·구체화 5종 자동 점검 결과 표시. 현재는 Foundry-X 자체 메트릭 기반 mock + 실시간 지표 1~2개(agent_improvement_proposals 카운트 등). DeepDive B-1 Harness Engineering 가시화. PRD: `docs/specs/fx-ai-foundry-os/prd-final.md` (FX-REQ-584, P0) | Sprint 298 | ✅ | M3 구간. 미달 시 P1로 강등 가능 |
| F549 | **fx-ai-foundry-os: KG XAI 뷰어 (read-only)** — `/ai-foundry-os/ontology` — Decode-X Neo4j 노드(SubProcess/Method/Condition/Actor/Requirement/DiagnosisFinding)를 D3 force-graph로 표시, 클릭 시 "이 결정의 근거 경로" 자동 하이라이트. Neo4j 조회 엔드포인트 없으면 mock 데이터 10+ 노드로 fallback. PRD: `docs/specs/fx-ai-foundry-os/prd-final.md` (FX-REQ-585, P0) | Sprint 298 | ✅ | Open Issue #4 (Neo4j 접근 경로) H+4 마감. 구현 복잡도 높음 → M4 버퍼 필요. **후속 B4** (2026-04-16 리허설 D-1): force-graph 중앙 밀집·의미 전달 부족 → Swimlane 6열 레이아웃 + 경로 토글 + 내러티브 패널로 개선 (PR #605, DONE) |
| F550 | **Codex CLI 설치 + 리뷰어 프로파일 확립** (FX-REQ-587, P0) | Sprint 300 | ✅ | PR #606 + F554 hotfix PR #607. install-codex.sh user-prefix 재작성 완료, Master v0.120.0 설치 |
| F551 | **autopilot Phase 5b Codex cross-review 훅 + Composite Verify Report** (FX-REQ-588, P0) | Sprint 300 | ✅ | PR #606 + F554 hotfix PR #607. autopilot SKILL.md Phase 5c 배선 완료(8건 참조). Mock dogfood PASS — **실 Codex API 호출은 OPENAI_API_KEY 설정 후 첫 Sprint에서 검증** |
| F552 | **Dual AI Review D1 테이블 + `/work-management` 대시보드 위젯** (FX-REQ-589, P1) | Sprint 303 | ✅ | PR #608 merge. D1 0138 dual_ai_reviews. core/verification/ 도메인 (3 routes, 1 service). work-management "AI 검증" 탭. TDD 20/20 GREEN. save-dual-review.sh autopilot 배선. Deploy D1 timeout→re-run SUCCESS |
| F553 | **4주 관측 회고 + 회귀율 리포트 + 모델 튜닝** (FX-REQ-590, P1) | Sprint 307 | 📋(idea) | F552 merge 후 4주 관측 데이터 기반 |
| F554 | **Phase 46 hotfix — Codex 실설치 + autopilot Phase 5b 훅 배선 + dogfood Smoke Reality** (FX-REQ-591, P0) | Sprint 302 | ✅ | PR #607 merge. Match 100%. 인프라 배선 완료(install-codex.sh + Phase 5c + codex-review.json mock PASS + 회고). 잔여: 실 API 호출은 OPENAI_API_KEY 설정 후 자동 활성화(mock→real 전환, codex-review.sh L107 분기) |
| F555 | **LLM 모델 ID 현행화 + 중앙 상수화** — (1) 구버전 모델 ID 최신화(haiku-20250714→20251001, sonnet-4-5→4-6). (2) shared/model-defaults.ts 중앙 상수(MODEL_SONNET/MODEL_HAIKU) + undated alias 전환. 다음 모델 업그레이드 시 1파일만 수정 (FX-REQ-593, P1) | Sprint 308 | ✅ | PR #617(47파일 모델 ID 갱신) + PR #618(50파일 상수 추출+undated alias). typecheck+3747 tests PASS |
| F556 | **MetaAgent Rubric 튜닝 — R6 rawValue=0 근본원인 + 6 proposals 변별력 확보** — F542/F544 관찰 데이터 기반 rubric 고도화. 범위: (a) `core/agent/meta/rubric-evaluator.ts` R6(또는 6번째 축) rawValue=0 원인 분석 — 계산 로직/데이터 누락/정규화 버그 중 식별, (b) rubric 6축 가중치·임계값 재조정으로 `rubric_score=100` 천장 현상 완화, (c) 기존 6 proposals 재채점 regression 테스트 + A/B 비교(튜닝 전후 score 분포), (d) 튜닝 기준 문서화(`docs/specs/fx-hyperfx-agent-stack/rubric-tuning-v1.md`) (FX-REQ-602, P1) | Sprint 310 | ✅ | PR #638 MERGED Match 100% (S302 autopilot). ProposalRubric v2 + Convergence 재정의. commit `78dfbfd3` |
| F560 | **Phase 45 · Discovery 완전 이관** — Gap 1 해소. F538 partial(7 routes Service Binding proxy 잔존)을 fx-discovery 순수 이관으로 완결. 범위: (a) `packages/api/src/core/discovery/routes/*` 7개 파일 fx-discovery로 이동, (b) fx-gateway Service Binding proxy 제거 + 직접 라우팅, (c) cross-domain import 전수 grep(`grep -rn "core/discovery"` 결과 fx-shaping/fx-offering 0건), (d) 배포 preflight 5항목 통과 + Phase Exit P1~P4 Smoke Reality. Phase 45 **MVP M1** (FX-REQ-603, P0) | Sprint 312 | ✅ | **PR #654 MERGED** (S306, 2026-04-21, Match 95%). Full Auto + **SCOPE LOCKED** 주입(autopilot 프롬프트에 원 범위 3항목 명시)으로 Sprint 311 scope drift 재발 차단. MVP M1 달성 — Phase 45 Gap 1 해소 |
| F561 | **Phase 45 · D1 Option A 전환 PoC (discovery_db 분리)** — Gap 2 해소. 데이터 소유권 분리 첫 단계 + 롤백 리허설. 범위: (a) 신규 D1 `foundry-x-discovery-db` 생성 + 관련 10+ migration 이동, (b) fx-discovery wrangler.toml `database_id` 교체, (c) biz_items Blue-Green/Shadow Write 검증, (d) 롤백 리허설 1회 성공, (e) FK 참조 끊김 목록화 + 마이그레이션 재번호 전략 문서화. Phase 45 **MVP M2** (FX-REQ-604, P0) | Sprint 313 | ✅ | **PR #656 MERGED** (S307, `fc9751c1`, Match 100%). Hotfix PR #658 (`ba9ba594`) — prod 계정에 foundry-x-discovery-db 생성 + 13 migration 적용 + deploy.yml 자동 적용 스텝. MVP M2 달성 |
| F562 | **Phase 45 · shared-contracts 레이어 신설** — Gap 3 해소. cross-domain DTO/Event 계약 분리 + consumer-driven contracts 패턴. 범위: (a) `packages/shared-contracts/` 신규 workspace, (b) Discovery↔Shaping Event/DTO v1.0 publish, (c) monolith화 방지 — 구현 로직 금지 (타입+스키마만), (d) 설계 가이드라인 md 동봉, (e) 기존 shared/ax-bd/* cross-domain 타입 전수 이동 (FX-REQ-605, P1) | Sprint 313 | ✅ | **PR #656 MERGED** (S307, `fc9751c1`). Hotfix PR #657 — CI workflow 4곳(deploy.yml 3 + e2e.yml + msa-lint.yml)에 `pnpm --filter @foundry-x/shared-contracts build` 선행 스텝 추가 — pnpm filter가 workspace dep 미빌드하는 구조적 gap 해소 |
| F563 | **Phase 45 · fx-shaping E2E + KOAMI P2 완결** — Gap 4 + 6 해소. F540 partial(E2E 미완료 + KOAMI P2 deferred) 완결. 범위: (a) Shaping 13 routes 순수 이관(F540 proxy 제거), (b) KOAMI P2 deferred 케이스 사전 분석 + 구현, (c) Shaping Graph E2E (bi-koami-001 proposals ≥ 1건), (d) Phase Exit P1~P4 Smoke Reality (FX-REQ-606, P0) | Sprint 314 | ✅ | **PR #663 MERGED** (S308, `6583b1fe`, Match 97%). autopilot 25분 24초. (a) 재해석: 이미 F540에서 routes/*가 fx-shaping 이동 완료 상태 → 남은 `packages/api/src/core/shaping/routes/*` ×13 **dead code 제거** + dead test 5건 삭제/4건 정리. services/schemas는 discovery/offering/agent/harness/collection cross-import 때문에 유지. (b)(c) bi-koami-001 D1 INSERT 실측 확인 (rawDb 직접 조회). fx-shaping tests 34/34 PASS (shaping-d1-insert 6 + shaping-routes-functional 17 + 기존 11). better-sqlite3 devDep 추가. 리스크 R5 대응 — Rubric 튜닝 후 재착수 조건 충족. **Hotfix PR #668** (`cd9d499a`) — deploy.yml test job에 `pnpm --filter @foundry-x/shared-contracts build` 선행 스텝 추가. F562 PR #657 커버리지 누락(test job 미포함) 보강. 증상: deploy run #24728582298 `@foundry-x/web#typecheck` TS2307 |
| F564 | **Phase 45 · CLI VITE_API_URL 전환 + Strangler 완결** — Gap 5 해소. CLI/Web 모두 fx-gateway 단일 진입점. 범위: (a) `packages/cli/src/` API_URL 기본값 전환, (b) foundry-x-api 직결 코드 grep 0건 확증, (c) SSO Hub Token 경로 호환성 E2E, (d) 환경 변수 migration 가이드 문서화. Phase 45 **MVP M3** (FX-REQ-607, P1) | Sprint 315 | ✅ | **PR #673 MERGED** (S309, `85946b34`, Match 100%). F569 번들. MVP M3 달성 — Phase 45 Gap 5 해소. CLI config-manager + api-url-migration.md + E2E `strangler-gateway.spec.ts` + `_redirects` |
| F565 | **Phase 45 · SDD Triangle 동기화 CI 게이트** — Gap 7 해소. SPEC drift 방지 자동화. 범위: (a) `.github/workflows/` SDD-drift-check job 추가, (b) SPEC.md §5 F-item vs `git log --since=last-sprint` 교차 검증, (c) drift > 0건 시 PR fail, (d) 주간 drift 리포트 자동 생성 (FX-REQ-608, P2) | Sprint 317 | ✅ | **PR #684 MERGED** (S311, `7af56897`, Match 100%, +570/-0, 7 files). `sdd-drift-check.yml` + `sdd-drift-report.yml` (주간 cron) + `scripts/sdd/check-drift.sh` + `scripts/sdd/__tests__/test-sdd-drift.sh` + `docs/dogfood/sprint-317-sdd-drift.md`. autopilot 10분 완결 (SCOPE LOCKED 효과, 기존 workflow/src 0 수정) | PRD §3-1 F565. 실제 자동화 스크립트 리소스 확보 선행. S308 Sprint 314→315 shift(F563 MVP 스코프 집중). **S309 Sprint 315→317 재shift** — F569 merge 후 workflow 충돌 해소 후 착수 |
| F566 | **Phase 45 · MSA Separation Roadmap v2** — Gap 8 해소. 6 도메인 분리 우선순위 + 일정 명문화 문서. 범위: (a) `docs/specs/fx-msa-roadmap-v2/prd-final.md` v2 발행, (b) 각 도메인 리소스/일정/롤백 시나리오, (c) Sprint 311~318 배치 근거, (d) Phase 46+ 예측 로드맵 (FX-REQ-609, P0) | Sprint 311 | ✅ | **PR #640 MERGED** (F560 병행). S302+ Sprint 311 autopilot 완결 |
| F567 | **Phase 45 · Multi-hop latency benchmark** — Gap 9 해소. 누적 latency 측정 + SLO 설정. 범위: (a) browser → fx-gateway → foundry-x-api → fx-discovery 3-hop 경로 k6 benchmark, (b) p50/p95/p99 측정 + 1-hop 대비 증분, (c) SLO p95 < 300ms 확정 + 미달 시 최적화 플랜, (d) Grafana 대시보드 등록 (FX-REQ-610, P1) | Sprint 316 | ✅ | **PR #678 MERGED** (S310, `3f426727`, Match 98%). F568 번들. k6 script + SLO p95 < 300ms 확정(F543 1-hop baseline 기반) + `docs/dogfood/sprint-316-latency.md` |
| F568 | **Phase 45 · EventBus PoC (기술스택 선정 + 1 flow)** — Gap 10 해소. 비동기 파이프라인 가능성 확보. 범위: (a) D1 Event Table vs Cloudflare Queue vs Durable Object PoC 3종 비교, (b) 기술스택 최종 선정 + 의사결정 문서, (c) Discovery→Shaping 트리거 1 flow 구현, (d) 타임박스 2주 고정. 선행: F562 shared-contracts (FX-REQ-611, P1) | Sprint 316 | ✅ | **PR #678 MERGED** (S310). **D1 Event Table 선정** (3종 비교 후). `StagePublisher` + `DiscoveryTrigger` PoC + Discovery→Shaping FORMALIZATION 트리거 1 flow + `docs/specs/fx-eventbus/decision.md`. 83/83 tests PASS (fx-discovery 35 + fx-shaping 40 + k6 validation 8) |
| F569 | **Phase 45 · harness-kit 표준화 (Workers scaffold + CI workflow turbo 경유 전환)** — Gap 11 해소. 새 Worker 생성 비용 절감 + CI drift 근본 방지. 범위: (a) `packages/harness-kit/` 공통 Workers scaffold(JWT/CORS/Error handler/EventBus client), (b) fx-gateway/fx-discovery/fx-shaping/fx-offering 4 Worker에 적용, (c) 버전관리 전략(npm publish vs workspace internal) 확정, (d) 신규 Worker 생성 템플릿 `scripts/new-worker.sh`, (e) **CI workflow turbo 경유 전환** — 6 workflow(msa-lint/deploy/deploy-gate-x/e2e 등) 17 step을 `turbo run build --filter=<pkg>` 1 step으로 정리. turbo.json `^build` dependsOn 이미 완비. (f) **Remote cache 도입 결정** — Vercel/Turbo Cloud 또는 self-host(S3). 선행 결정 필요. (g) workflow workspace dep 자동 해소로 S307 shared-contracts / S309 harness-kit 패턴 재발 차단. (FX-REQ-612, P2) | Sprint 315 | ✅(partial) | PRD §3-1 F569. 오픈 이슈 #4 해소. **S309 추가 scope** (2026-04-22): C94 msa-lint fix 후 turbo 전환 번들 결정(사용자 선택). PR #670 `pnpm --filter '...'` syntax로 증상 해소 후 ~2주 경과 관찰 → F569 본격 전환. **S309 Sprint 315 ✅(partial)** PR #673 MERGED (`85946b34`, Match 100%) — (a) `packages/harness-kit/__tests__/middleware/jwt.test.ts` 공통 JWT middleware + (b) fx-discovery/fx-shaping/fx-offering 3 Worker `src/middleware/auth.ts` 적용 + (d) `scripts/new-worker.sh` 템플릿. **잔여 scope**: (c) 버전관리 전략, (e) CI workflow turbo 전환, (f) Remote cache 도입 — 다음 Sprint에서 별도 처리 |
| F570 | **Phase 45 · Offering 완전 이관** — F541 Walking Skeleton 후속. F541 partial(proxy 잔존) → 순수 이관 완결. F560 Discovery 패턴 재사용. 범위: (a) Offering 12 routes 순수 이관 + `offeringPacksRoute`(Sprint 81 F236/F238/F240) + `core/offering/` + `__tests__/` offering 20+건 fx-offering 이동, (b) fx-gateway proxy 12줄(F541 명시 라우팅) 제거 + catch-all 단일화, (c) cross-domain import 전수 grep 0건 (`grep -rn "core/offering"` fx-*/packages/api 0건), (d) Phase Exit P1~P4 Smoke Reality (FX-REQ-613, P1) | Sprint 318 | ✅ | **PR #687 MERGED** (S312, `4c12cd84`, Match 97%, 2026-04-24 08:54 KST, +340/-12089 87 files). autopilot 34분 완결(08:20 CREATED → 08:54 MERGED). SCOPE LOCKED 효과 재확증 — MAIN_API 대량 삭제(core/offering + __tests__ 20+건 + offeringPacksRoute) + fx-gateway F541 proxy 12줄 제거 + cross-domain grep 0건. Phase 45 Batch 4 완결 |
| F571 | **Phase 45 · Agent 도메인 분리 (Walking Skeleton)** — 65 services — 가장 복잡. Phase 45 **마지막** 배치. **Plan/Design 확정** (S313, 2026-04-28 `/ax:todo plan`): cross-domain dep 실측(harness 14, discovery 3, offering 2, shaping 1 = 20건) 기반 **8 routes 1차 이관**(agent-adapters, agent-definition, command-registry, context-passthrough, execution-events, meta, task-state, workflow) + **잔여 7 routes**(agent, streaming, orchestration, captured-engine, derived-engine, skill-registry, skill-metrics) **Phase 46 deferred**. PRD §3-1 "15 routes" → 8 routes deviation 명시. 범위: (a) fx-agent Worker 신규 생성, (b) 8 routes + harness 2 service copy(custom-role-manager, transition-guard), (c) Service Binding 셋업(gateway → AGENT, fx-agent → MAIN_API binding 사전 등록), (d) fx-gateway 10줄 라우팅(선등록 우선순위 준수, S299 collision 교훈). 리스크 R2 (FX-REQ-614, P1) | Sprint 320 | 📋(plan) | PRD §3-1 F571. 최고 리스크 도메인, 최후 배치. **S309 Sprint 318→320 재shift** — Batch 6 최후 단독. **FX-PLAN-320 + FX-DESIGN-320 작성 완료** (S313 `/ax:todo plan`, `/ax:sprint 320` 진입 준비) |
| F572 | **Phase 45 · modules/portal·gate·launch 통합 분리** — modules 계열 3개(portal 19/23, gate 7/8, launch 8/14) 묶어서 한번에 분리. 범위: (a) **fx-modules 단일 Worker** 아키텍처 확정(S313 결정 — `/api/portal/*`, `/api/gate/*`, `/api/launch/*` prefix 분리, wrangler.toml 1개), (b) 총 34 routes/45 services 이관, (c) 종속관계/병렬 작업 가능성 분석 선행, (d) 공통 auth 경로 통합(harness-kit JWT middleware 재사용), (e) fx-gateway proxy 12줄 제거 + catch-all 단일화, (f) cross-domain import 전수 grep 0건, (g) Phase Exit P1~P4 Smoke Reality (FX-REQ-615, P2) | Sprint 319 | ✅ | **PR #690 MERGED** (S313, `ef4f47ad`, Match **100%** 24/24, +18690/-141, 162 files). F574 번들. `packages/fx-modules/` 신규 Hono Worker(34 routes + 45 services). fx-gateway MODULES Service Binding + `/api/portal/*` · `/api/gate/*` · `/api/launch/*` 라우팅. api/app.ts portal/gate/launch routes 제거. cross-domain imports 0 violations(Worker-level shared services + local stubs). TDD 6 tests(3 health + 3 cross-domain guards). fx-modules 6/6 + fx-gateway 8/8. Pre-deploy: `wrangler secret put JWT_SECRET --name fx-modules` + `GITHUB_TOKEN` |
| F573 | **Phase 45 · ax-bd-artifacts/discovery 경로 fx-shaping 이전** — Sprint 311 autopilot(F560 scope drift)에서 실제 수행된 작업을 사후 등록. 범위: (a) `core/discovery/routes/ax-bd-{artifacts,discovery}.ts` 7 routes → `packages/fx-shaping/src/routes/` 이동, (b) `discovery-x-ingest.service.ts` 신규, (c) fx-gateway에 discovery-stage-runner/graph/shape-pipeline 7개 MAIN_API 명시 라우팅 추가, (d) cross-domain import 0건 확증. 본 F-item은 F560 원 범위와 별개(ax-bd-* 경로는 Shaping 파이프라인의 일부) (FX-REQ-616, P2) | Sprint 311 | ✅ | **PR #640 MERGED** (2026-04-19, +677/-2, Match 95%). F560 scope drift 사후 분리 등록 |
| F574 | **wiki-sync 자동 경로 2종 버그 fix** — C86 dogfood에서 발견된 webhook `pullFromGit()` D1 INSERT fail 구조적 결함. **근본 원인 2종**: (A) `wiki_pages.slug` UNIQUE 제약 부재로 `INSERT ... ON CONFLICT(slug) DO UPDATE` SQLite reject(`SQLITE_ERROR: ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`), (B) `DEFAULT_PROJECT_ID = "default"` 하드코딩(wiki.ts:19, wiki-sync.ts:53) vs 실측 FK `proj_default` 불일치로 FOREIGN KEY violation. **범위**: (a) D1 migration 신규 `CREATE UNIQUE INDEX idx_wiki_pages_slug ON wiki_pages(slug)` + Drizzle schema `.unique()` 병기, (b) wiki.ts/wiki-sync.ts `'default'` → **`'proj_default'` 하드코딩 치환**(S313 결정 — env var 외부화는 별도 C-track), (c) webhook redelivery E2E 테스트 — C86 기존 row 삭제 후 `docs/wiki/decode-x-spec.md` 재-push 시 자동 sync PASS 확인, (d) **Phase Exit Reality** — 기존 4개 row(intro/ai-foundry-os-*) 가 POST /api/wiki 경로로만 생성됐는지 재검증 + webhook 경로도 정상화 확인. **C87 승계** (FX-REQ-624, P2) | Sprint 319 | ✅ | **PR #690 MERGED** (S313, `ef4f47ad`, Match **100%**). F572 번들. D1 migration `0139_wiki_pages_slug_unique.sql` UNIQUE INDEX + Drizzle `.unique()`. `DEFAULT_PROJECT_ID` `'default'` → `'proj_default'` 하드코딩 치환(env var 외부화는 별도 C-track). wiki-sync 3/3 tests PASS. webhook redelivery 경로 정상화 확증 |

<!-- fx-task-orchestrator-backlog -->
### Task Orchestrator Backlog (B/C/X)

> F-track은 Sprint/Phase 전용. Task Orchestrator는 B(Bug)/C(Chore)/X(Experiment)만 발급.
> 상태: PLANNED → IN_PROGRESS(GitHub Issue) → DONE / CANCELLED / CLOSED_LEARNED

| ID | Type | 제목 | REQ | Sprint | 상태 | 비고 |
|----|------|------|-----|--------|------|------|
| C1 | C | discovery analysis process audit | — | — | CANCELLED | C2와 중복 |
| C2 | C | discovery analysis process audit | — | — | DONE | completed |
| X1 | X | task-orchestrator s-beta heartbeat signal | — | — | DONE | lib.sh write_signal/check_liveness/update_heartbeat |
| C3 | C | claude-code 200k context token optimization | — | — | DONE | CLAUDE.md 82% 압축 (#427) |
| C4 | C | discovery e2e workflow test biz-ai | — | — | DONE | PR #430 merged |
| F498 | F | E2E workflow shard parallelization F490 TD-03 | FX-REQ-482 | — | DONE | PR #432 merged |
| C5 | C | task orchestrator spec sync and skill docs update | — | — | DONE | PR #431 merged |
| X2 | X | self-healing watch agent diagnostic and pattern fix | — | — | DONE | PR #434 merged |
| C6 | C | E2E workflow walkthrough item-discovery-report-offering-PRD-prototype | — | — | DONE | PR merged |
| C7 | C | Task-REQ-Sprint-Phase 거버넌스 매핑 자동화 | — | — | DONE | 본 task |
| C8 | C | Marker.io 피드백 자동처리 점검 및 보강 | — | — | DONE | PR merged |
| C9 | C | Offering PPTX 생성 품질 보강 (FX-REQ-490) | — | DONE | PR #445 merged |
| X3 | X | puppeteer-mcp 설치 및 워크플로우 통합 (FX-REQ-491) | — | DONE | PR #444 merged |
| X4 | X | RFP 기반 제안서 Agent PRD 작성 (FX-REQ-492) | — | DONE | PR #443 merged |
| C10 | C | CC 다중 구독 계정 환경 통합 — HOME 경로 정규화 + 스킬/플러그인/standards/rules 공유 체계 구축 (FX-REQ-493, P1) | 세션 252 | ✅ | scripts/cc-{setup-shared,verify,status}.sh, 개인 .claude/ canonical, 회사 symlink (standards/rules/CLAUDE.md/settings.json), bashrc CLAUDE_WT_BASE 절대경로 고정 |
| C11 | C | Sprint 자동 Monitor+Merge 파이프라인 — Sprint WT 생성 후 Master에서 signal 감시→권한승인→review→merge→cleanup 자동 실행 체인 구축. 현재: merge-monitor.sh/sprint-watch 존재하나 자동 트리거 부재. 해결: (A) /loop Signal 폴링 (B) sprint-pipeline Phase 4c 강제 수행 (C) Hook 기반 자동 트리거 (FX-REQ-494, P1) | — | 📋 → **F500 승격** | Sprint 244 배정. C10과 독립 |
| C12 | C | prd-v8 governance compliance rename and frontmatter (FX-REQ-504) | — | DONE | task orchestrator |
| C13 | C | sprint-pipeline-monitor and post-session docs ax-plugin (FX-REQ-505) | — | DONE | cross-repo: ax-plugin `99446f7 docs(sprint-pipeline): 런타임 지원 스크립트 섹션 추가`. Foundry-X PR #467 metadata-only. |
| X5 | X | long term backlog reevaluation F112 F117 F118 F245 (FX-REQ-506) | 2026-04-11 | DONE | [[FX-PLAN-X5]] — 4건 처분 (DEFER/UPGRADE/ARCHIVE/CLOSE) |
| X6 | X | multi account ax plugin sync symlink hub extension (FX-REQ-507) | — | ✅ | work HOME `plugins/{marketplaces,cache}/ax-marketplace` → main HOME symlink. 사전 drift 3건 보정(버전 경로, 구버전 잔존, skill 내용차). Backup: `/tmp/backup-axmarket-S256.tar.gz`, 이전본: `/tmp/old-ax-{market,cache}-work-S256`. Smoke test: task skill 노출·SKILL.md byte-identical 확인 |
| C14 | C | E2E smoke A — lib.sh + task-start.sh static checks (FX-REQ-508) | — | CLOSED_EMPTY | S257 dogfood: tmux 3.4 pane segfault로 워커 파일 미작성 빈 PR 머지. S258에서 tmux 3.5a 후 재시도 |
| C15 | C | E2E smoke B — task-complete + daemon guard checks (FX-REQ-509) | — | CLOSED_EMPTY | S257 dogfood: 동일 tmux 3.4 pane 소멸. S258에서 재시도 |
| C16 | C | E2E smoke A retry — lib.sh + task-start checks (FX-REQ-510) | — | CLOSED_EMPTY | S257b: prompt 상향 fix로 재시도했으나 실제 원인은 tmux 3.4 → 또 pane 소멸 빈 PR. S258 tmux 3.5a 후 재시도 |
| C17 | C | E2E smoke B retry — task-complete + daemon guards (FX-REQ-511) | — | CLOSED_EMPTY | S257b 동일 원인: tmux 3.4 pane 소멸. S258에서 재시도 |
| C18 | C | E2E smoke 통합 단건 — lib.sh/task-start/task-complete/daemon guard full pipeline dogfood (tmux 3.5a 회귀 검증) (FX-REQ-512) | — | DONE | S258 dogfood ✅ — tmux 3.5a fx35a socket에서 **C19(FX-REQ-513, PR #482)로 실행 완료**. task-start.sh가 C18 IN_PROGRESS 감지 후 C19 자동 발급(ID forward). Assertion 4/4 PASS: ①additions=127 (`docs/dogfood/C18-tmux35a-smoke.md`) ②pane 2개 dead=0 (%0 master, %1 worker) ③daemon auto-merge 성공 ④S257 fix 4종 회귀(FX_SIGNAL_DIR SSOT / AUTONOMY_RULE / task-complete fail-fast / daemon PRESERVED guard) 전부 정상. dmesg tmux segfault 0건. **결론**: tmux 3.4 → 3.5a 업그레이드로 근본 원인 해소 확정. 후속 defense-in-depth: C20/C21 |
| C19 | C | C18 smoke 통합 단건 dogfood (FX-REQ-513) | — | DONE | task orchestrator (PR #482, additions=127, daemon auto-merge, 21:43 lifecycle 완료) |
| C20 | C | task-complete.sh meta-only empty commit 감지 + signal 거부 (FX-REQ-514) | — | DONE | **C22(FX-REQ-516, PR #484)로 실행 완료** — task-start.sh가 C20 PLANNED 감지 후 다음 가용 번호(C22) 자동 발급(ID forward). C20 row는 "등록 의도" 기록, 실제 구현은 C22 커밋에서 확인 |
| C21 | C | phase_recover worker inactivity → retry queue (FX-REQ-515) | — | DONE | **C23(FX-REQ-517, PR #486)로 실행 완료** — task-start.sh ID forward(C21→C23). C21 row는 "등록 의도" 기록, 실제 구현은 C23 커밋에서 확인 |
| C22 | C | task-complete.sh empty commit 감지 + signal 거부 (FX-REQ-516) | — | DONE | task orchestrator (PR #484, 21:54 lifecycle 완료) — C20 row의 실제 구현체. `git diff --numstat HEAD~1 HEAD` 판정 + exit 22 (EMPTY_COMMIT_REJECTED) + signal 미작성 + task rollback 로직 추가 |
| C23 | C | phase_recover worker inactivity → retry queue (FX-REQ-517) | — | DONE | task orchestrator (PR #486, 22:06 lifecycle 완료) — C21 row의 실제 구현체. `scripts/task/task-daemon.sh` phase_recover 개선 +93/-17 + 신규 `scripts/task/task-retry.sh` +107(retry queue 관리) + 단위 테스트 `scripts/task/test-daemon-retry.sh` +151. 총 additions=351. `/tmp/task-retry/{TASK}.json` 재시도 큐 + daemon log `❗ retry` 이벤트 + non-empty diff 기존 경로 유지 |
| C24 | C | task-start.sh `--reuse-id` flag — ID forward 함정 우회 (FX-REQ-518) | — | DONE | S259 dogfood. `scripts/task/lib.sh` `lookup_backlog_row()` 추가(SPEC row에서 REQ+상태 정규식 추출), `task-start.sh` `--reuse-id <ID>` flag + 재사용 경로 분기 + `warn_reuse_risk()` 정책 함수(PLANNED silent / CLOSED_EMPTY warn+pass / IN_PROGRESS·DONE family는 `FX_REUSE_FORCE=1` 없으면 abort) + SPEC row/REQ 재사용(기존 row 유지) + 재사용 경로에서 push 단계 skip. 유닛 테스트 6/6 PASS. Learning mode에서 사용자가 `warn_reuse_risk()` 정책 직접 승인 |
| C25 | C | board-sync-spec.sh gh project scope preflight 체크 강화 (FX-REQ-519) | — | DONE | S259 C24 dogfood 대상. `scripts/board/_common.sh` `board::require_projects()`가 `gh auth status` scope 감지 부족 시 actionable 에러(`gh auth refresh -s read:project,project` 안내) 출력하도록 강화. `--reuse-id C25` 경로로 실행 |
| C26 | C | sprint-merge-monitor dead git -C cleanup (FX-REQ-520) | — | DONE | task orchestrator |
| C27 | C | task-daemon phase_cleanup pre-evict tmux panes rooted in WT (FX-REQ-521) | — | DONE | task orchestrator |
| C28 | C | task-start.sh HOME propagation + inject --model sonnet for WT workers (FX-REQ-522) | — | DONE | task orchestrator |
| C29 | C | task-daemon pre-evict primary exclusion + extras metric (FX-REQ-523) | — | DONE | task orchestrator |
| C30 | C | task-complete daemon auto-restart hook on daemon/lib.sh merge (FX-REQ-524) | — | DONE | task orchestrator |
| C31 | C | inject bracket paste workaround — split text and Enter send-keys (FX-REQ-525) | — | DONE | task orchestrator |
| C32 | C | deploy.yml concurrency + paths filter meta-aware 개선 (FX-REQ-527) | — | DONE | task orchestrator |
| C33 | C | eslint no-explicit-any cleanup — packages/cli/src/ui/render.tsx:52 + packages/cli/src/harness/lint-rules/require-zod-schema.ts:32 (FX-REQ-528) | — | DONE | 1분 수정, deploy.yml run에서 경고로 노출 중, S260 /ax:daily-check 후속 |
| C34 | C | GitHub Actions Node 24 migration — dorny/paths-filter@v3→v4 업그레이드 (FX-REQ-529) | — | DONE | PR #512. 나머지 actions(checkout/setup-node/pnpm-setup/wrangler-action)는 현재 major 내 node24 호환 확인 |
| C35 | C | ~~task-start.sh worker git author 분리~~ (FX-REQ-530) | — | CLOSED | 재평가(S263): repo local config + C28 HOME 전파로 로컬 커밋은 해소 완료. squash merge author는 gh auth 계정 의존이며 개인계정 유지 결정 |
| C36 | C | sidebar에 `/work-management` 링크 추가 — admin-core 그룹, Kanban 아이콘 (FX-REQ-531) | — | DONE | PR #513. sidebar.json + navigation-loader.ts Kanban 아이콘 등록 |
| C37 | C | autopilot Gap% 측정 범위 확장 — Step 5b E2E audit 자동 호출 추가 (FX-REQ-532) | — | DONE | sprint-autopilot SKILL.md Step 5b 신설. Playwright 있을 때 `/ax:e2e-audit coverage` 자동 실행, 갭 시 WARN (중단 안 함) |
| C38 | C | work-unit-taxonomy 용어사전 + 승격기준 + Milestone정리 (FX-REQ-539) | — | DONE | task orchestrator |
| C39 | C | task-daemon 자동 감지 근본 개선 — 세션 간 지속성 + 완료 감지 다중화 (FX-REQ-540) | — | DONE | task orchestrator |
| C40 | C | daemon 3중 감지 dogfood 검증 (FX-REQ-541) | — | DONE | task orchestrator |
| C41 | C | daemon path2-path3 실전 검증 (FX-REQ-542) | — | DONE | S265 실전 검증 완료. Issue #527 close |
| C42 | C | sprint-monitor + task-daemon 통합 — unified-daemon (FX-REQ-543) | — | DONE | task orchestrator |
| C43 | C | sprint monitor 2중 실행 해소 — bashrc _sprint_ensure_monitor→daemon 교체 + post-merge health check | — | DONE | S267. bashrc 직접편집 + daemon 코드 |
| C44 | C | Changelog 마크다운 렌더링 + Roadmap 미래 계획 — react-markdown 적용, GET /api/work/roadmap 추가, SPEC §3 직접 파싱으로 Phase 데이터 개선 | — | DONE | S268 PR #534. 구 C43(중복 ID) + C44 통합 |
| C45 | C | Backlog C/B/X-track 파싱 + 작업 분류 UX — parseBacklogItems() 추가, ClassifyTab "사용 방법" 안내 | — | DONE | S268 PR #534 |
| C46 | F | ~~Backlog 인입 파이프라인~~ → F516 승격 | — | → F516 | S268. Phase 37 Sprint 267 |
| C47 | F | ~~Changelog 트레이서빌리티~~ → F517 승격 | — | → F517 | S268. Phase 37 Sprint 268 |
| C48 | C | ~~Work Management Ontology~~ → F518 승격 | — | → F518 | S268. Phase 37 Sprint 269 |
| C49 | C | 작업 현황 Kanban 데이터 보강 — parseSpecItems F+Backlog 통합 반환, fetchSpecText 캐시 | — | DONE | S268 PR #534 |
| C50 | C | req-interview 스코어카드 채점 메커니즘 개선 — severity 가중치(flaw×3+gap+risk) + 라운드 간 품질 비교 + breakdown 표시 | — | DONE | S273. ax-plugin `4005237` |
| C51 | C | Monitor lifecycle 관리 — sprint skill done/merge에 Monitor stop 추가 + signal에 MONITOR_TASK_ID 기록 + session-start stale 감지 (FX-REQ-559) | — | DONE | ax-plugin eab6d00 |
| C52 | C | 콘텐츠 drift 해소 — landing/footer/README Sprint-Phase 수치 갱신 (FX-REQ-560) | — | DONE | task orchestrator |
| C53 | C | MSA 원칙 하드닝 (W+1~W+7) — PRD: `docs/specs/fx-msa-roadmap-v2/prd-final.md` (FX-REQ-567) | — | → **C54+C55 실행** | id-allocator가 C54 발급 (C20→C22 선례 동일 ID forward). req-interview R1=82/R2=70 Conditional, v2→final |
| C54 | C | MSA 원칙 ESLint 룰 구현 (FX-REQ-568) | — | DONE | PR #567. no-cross-domain-import + no-direct-route-register 룰 2종 + CLAUDE.md 원칙 섹션 + SKU 경계 테이블 + eslint.config.js 연입. 기존 230건 위반은 C55로 분리 |
| C55 | C | git-aware ESLint 신규파일 검증 (FX-REQ-569) | — | DONE | PR #570. scripts/lint-new-files.sh + .github/workflows/msa-lint.yml — `git diff --diff-filter=AM origin/master...HEAD`로 PR 신규/수정 파일만 lint. 통합 검증 3종 통과 (skip/detect/block). PRD Open Issue #1 해결 |
| C56 | C | **Phase 44: D1 격리 실행 (옵션 B)** — 도메인별 테이블 접근 ESLint 룰 + migration 분리 태깅. 범위: (a) `packages/api/src/eslint-rules/no-cross-domain-d1.ts` 신규 — discovery 도메인은 discovery_* 테이블만 접근, shaping은 shaping_*, offering은 offering_* 한정, (b) D1 migration 파일명에 도메인 태그 규칙(`0NNN_discovery_*.sql` / `0NNN_shaping_*.sql`), (c) CI msa-lint 확장 + 자가 PR 통과 | — | 📋(ready) → **C75 실행** | planning row, id-allocator가 C75 발급 (C56→C75 ID forward 패턴). 실제 구현 추적은 C75 row (task/C75-d1-eslint-c56-no-cross-domain-d1). 기존 149개 migration rename 금지(remote drift 위험) — (b)는 forward-only 적용 |
| C57 | C | **Phase 44: shared 슬리밍** — Discovery 전용 타입을 fx-discovery 내부로, Shaping 전용 타입을 fx-shaping 내부로. shared 27 → 15 파일 목표. 범위: (a) `packages/shared/src/types/discovery-*.ts` → fx-discovery/src/types, (b) `shaping-*.ts` → fx-shaping/src/types(F540 완료 후), (c) web/cli 소비자 재경로 + tsc 통과, (d) contract만 shared 유지 | — | 📋(ready) | F540/F541 완료 후 착수 권장 — 선행 시 shaping/offering 타입 이동 범위 불명확. 병렬 시 cross-domain import 깨짐 위험 |
| C69 | C | **배포 파이프라인 preflight 자동화** — feedback_msa_deploy_pipeline_gaps 5항목 자동 검증. 범위: (a) `scripts/msa-deploy-preflight.sh` — deploy.yml path filter와 신규 도메인 경로 정합성 + wrangler devDependency 확인 + `pnpm --filter deploy --dry-run` 실행 + obsolete test 파편 grep + smoke-check curl, (b) PR CI `msa-lint` workflow에 preflight step 추가 — 신규 `packages/fx-*/` 추가 시 자동 trigger, (c) 자가 검증: 본 C-item PR이 자기 자신 preflight 통과. 근거: Sprint 293 F538 배포 연쇄 5건 | — | 🔧 **(부분)** | 초판 `scripts/msa-deploy-preflight.sh` 작성 완료 (2026-04-15 15:57). Self-test: PASS=4/FAIL=3 — 실제 리스크 식별(fx-discovery shared 선빌드 필요 / test 파편 6건 잔여 / wrangler 접근 check false positive). 후속 (i) shared 선빌드 로직 추가, (ii) wrangler check 로직 개선(deploy.yml 실제 패턴 매칭), (iii) CI workflow 통합. 본 초판은 Master pane 수동 호출용. F540 Shaping 착수 전 리스크 가시화 목적 달성 |
| C58 | C | Phase 44: Service Binding latency 벤치마크 — k6로 p99 측정 + 결과에 따른 Go/No-Go 판정. F538/F539 전 선행 필수 | — | → **F543 실행** | 승격 (Sprint 291, 2026-04-14). WT 필요 + 3파일+ 예상으로 task-promotion 기준 4 충족. id-allocator가 F번호 재배정 가능 (C20→C22 선례 동일 ID forward) |
| C59 | C | Phase 44: CI/CD 분리 — deploy.yml paths-filter 적용, 도메인별 선택적 배포. F540/F541과 병행 | — | 📋(idea) | Idea. Phase 44 중반 예상 |
| C60 | C | Phase 44: 서비스 간 통신 계약 표준 — Worker 간 Service Binding 인터페이스 + 에러 핸들링 표준 문서화 | — | 📋(idea) | Idea. Phase 44 후반 문서 작업 |
| C61 | C | task-daemon post-merge 강화 (FX-REQ-570) | — | DONE | task orchestrator |
| C62 | C | task-list stale display 조사 (FX-REQ-572) | — | DONE | task orchestrator |
| C63 | C | msa-lint workflow shared 선빌드 — `pnpm --filter @foundry-x/api build` 실행 시 `@foundry-x/shared` 모듈 미빌드로 tsc 실패. F536/F537/F542/F543/F544 5연속 동일 실패 관찰. `.github/workflows/msa-lint.yml`에 `pnpm --filter @foundry-x/shared build` 선행 단계 추가 | — | DONE | PR #586 (`022a2aa4`). 3 lines. 이 PR 자체는 core/**.ts 미변경으로 msa-lint 미트리거(셀프 검증 불가) → 후속 MSA PR(F538 등)에서 해소 확인 |
| C64 | C | task-daemon signal FAILED 오판 수정 — PR-only non-required 체크(msa-lint)의 FAILURE를 `STATUS=FAILED`로 예단하여 Sprint MERGED 상태임에도 Master에 FAILED 신호. PR `state=MERGED` 또는 deploy.yml conclusion을 우선 판정하도록 `scripts/task/task-daemon.sh` 시그널 판정 로직 보정. Sprint 290에서 첫 관찰 | — | DONE | PR #587 (`17212ca1`). 3회 재발(S290/S291/S292) 후 hotfix: (a) `sprint_sig_get`에 `\|\| true` — pipefail grep no-match 시 set -e abort 방지(F_ITEMS 등 선택 key 누락 signal이 phase_sprint_signals 전체 silent 중단 시키던 숨은 버그 동시 해소), (b) `sprint_pr_state()` 신설 + CI wait 전 PR state 선체크(auto-merge 감지), (c) CI wait 실패 후 PR state 재확인(post-merge deploy 초회 실패 방어), (d) `__debug-phase-sprint-signals` 테스트 진입점. TDD Red→Green + 기존 daemon 테스트 3종 회귀 PASS |
| C65 | C | F536 autoTriggerMetaAgent 저장 누락 — Graph 완료 시 hook이 발동되지만 `agent_improvement_proposals`에 저장되지 않음. Sprint 290 Dogfood 실측(graph-bi-koami-001-1776147547955): auto-trigger 후 0건, 이후 manual `/api/meta/diagnose`는 6건 정상 저장. 원인 후보: ① hook이 호출 자체 안 됨(이벤트 구독 실패), ② 호출되나 proposals[] 반환 없이 조용히 리턴, ③ 저장 경로가 manual과 분기. agent-graph-run-service 로깅 + hook trace 검증 필요 | — | → **F544 실행** | 승격 (Sprint 292, 2026-04-14). task-promotion 기준 2+4 충족. id-allocator가 F번호 재배정 가능 (C58→F543 선례 동일 ID forward) |
| C66 | C | E2E 4-shard 결정론적 실패 근본 조사 — PR #575/#578/#579 3회 연속 동일 shard(4/4)가 전면 실패. 결정론적 실패 = 인프라 regression 확정. 조사 범위: (a) `.github/workflows/*.yml` e2e shard 분할 로직 + shard index=4 특이성, (b) `playwright.config.ts` shardIndex 조건부 설정, (c) Sprint 287~290에 도입된 새 E2E 시나리오 중 shard 4 할당 테스트 식별, (d) deploy.yml paths-filter 영향 여부, (e) Local 4-shard 재현 테스트. 결론에 따라 F 승격 또는 hotfix 진행 | — | DONE | PR #584 (`92fc2a25`). 조사 리포트 `docs/dogfood/C66-e2e-shard4-investigation.md`. 근본 원인: shard 4에 할당된 시나리오 3종에서 strict mode locator violation(같은 text 매치 복수). hotfix 3건 적용 후 Local 4-shard 재현 PASS |
| C67 | C | velocity 메트릭 누락 — `scripts/velocity/record-sprint.sh`가 Master에 존재하지 않는 `.sprint-context`만 참조하여 F_ITEMS/MATCH_RATE/TEST_RESULT/CREATED 전부 빈 값으로 `docs/metrics/velocity/sprint-NNN.json` 기록. Sprint 293 실측: `{"f_items":"","match_rate":null,"duration_minutes":0,"test_result":"unknown"}`. 원인: `.sprint-context`는 WT 생성 시 WT 내부에만 생성됨, Master에서 sprint-merge-monitor가 호출하는 시점엔 부재. 수정: signal 파일 fallback 추가 (glob `${SIGNAL_DIR}/*-${SPRINT_NUM}.signal`). task-promotion 기준 미충족(1파일, 내부 메트릭) → Backlog C-track | — | DONE | PR #593 (`3b82844b`). task-daemon auto-complete로 ba662682 commit → 자동 push → PR open → merge 일련 자동화. sprint-293.json 소급 재기록: `f_items=F538`, `match_rate=95`, `test_result=pass` 반영. 테스트 `scripts/test-velocity-signal-fallback.sh` 3 case 9/9 PASS. PR #594(duplicate, timestamp-only diff)은 close. **관찰**: task-start.sh의 auto-complete hook이 완료 직후 두 번째 auto-commit(`chore(C67): auto-commit on task complete`)을 만들어 PR 중복 생성 — 다음 세션 개선 후보 |
| C68 | C | Hook 경로 WT에서 실패 — `.claude/settings.json`의 5개 hook command(pre-bash-guard, heartbeat-hook, post-edit-format, post-edit-typecheck, post-edit-test-warn)가 `bash .claude/hooks/*.sh` 상대경로로 등록되어 WT cwd가 repo root가 아닐 때 `No such file or directory`. Sprint 294 autopilot 로그에서 반복 관측. autopilot 진행 자체는 영향 없으나 반복 에러 로그 노이즈. 수정: 5곳 전부 `bash "$CLAUDE_PROJECT_DIR/<path>"` 접두어 교체. task-promotion 기준 미충족(1파일, 내부 인프라) → Backlog C-track | — | DONE | settings.json 5 hook commands에 `$CLAUDE_PROJECT_DIR` 접두어 적용. meta-only 변경 → master 직접 push |
| B2 | B | **MSA Worker secret sync 자동화** — B1에서 fx-discovery/fx-shaping에 JWT_SECRET 누락이 수동 등록으로 해소된 건에 대한 재발 방지. 범위: (a) `scripts/msa-secret-sync.sh` — MAIN_API에 등록된 secret 목록 읽고, fx-discovery/fx-shaping/fx-gateway 중 필요 항목(JWT_SECRET 필수, ANTHROPIC_API_KEY 선택)이 누락된 Worker에 자동 등록 안내 or 실행(--apply 플래그), (b) C69 preflight에 secret 목록 diff 체크 추가, (c) deploy.yml post-deploy step에 `wrangler secret list` 비교 검증, (d) feedback memory `feedback_msa_worker_secret_sync.md` 작성. 우선순위 P1 (reality incident 재발 방지) | — | OPEN | Sprint 299 또는 인프라 정비 타이밍 |
| B1 | B | **Google 로그인 후 dashboard 튕김 — 2개 하위 버그 중첩** — (1) **라우팅 충돌**: F539c에서 fx-gateway `/api/biz-items/:id` → DISCOVERY 라우팅 시 정적 경로 3개(summary/portfolio-list/by-artifact)가 `:id`에 매칭되어 fx-discovery 오유입. (2) **JWT_SECRET 누락**: fx-discovery/fx-shaping Worker에 JWT_SECRET secret 미등록 → `auth.ts`가 `"dev-secret"` fallback → MAIN_API(prod secret)가 서명한 JWT 검증 실패 → 401 → api-client refresh 후에도 다시 401 → `window.location.href = "/login"`로 튕김. 해법: (1) 정적 경로 3개 fx-discovery 이전(옵션 B) + PortfolioService 2개 메서드 포팅 + TDD Red→Green 6 tests. (2) wrangler secret put JWT_SECRET=ktds11!@ 등록(`../api/node_modules/.bin/wrangler`). | — | DONE | **(1)** PR #602 (`d0dbacf6`, +388/-0, 4 files). TDD 6/6 PASS, turbo test+typecheck+lint 12 packages PASS. deploy.yml 24483297868 success. **(2)** fx-discovery + fx-shaping 양쪽 `wrangler secret put JWT_SECRET` 수동 실행 (Sinclair 직접). 검증: 브라우저 Google 로그인 → dashboard 머무름 확인 (2026-04-16 09:00경). 근본 원인은 F539c Phase Exit drift "KOAMI Smoke Reality P2 미실측"의 잠복 영향 — authenticated fx-discovery 호출 dogfood 부재로 JWT_SECRET 누락이 배포까지 감지 안 됨 |
| B3 | B | Dashboard 파이프라인 원 안 숫자 모호성 해소 (FX-REQ-586) | — | DONE | task orchestrator |
| B4 | B | **Ontology 그래프 의미 명확화** — 리허설 D-1 dogfood에서 발견: F549 KG XAI 뷰어가 force simulation 기반 배치로 12노드가 중앙 밀집 + 의미 전달 부족. 범위: (a) `packages/web/src/routes/ai-foundry-os/ontology.tsx` force simulation 제거 → 타입별 Swimlane 5열 레이아웃(① 누가/② 어디서/③ 어떻게/④ 판단/⑤ 남겨야), (b) 엣지 라벨 항상 표시, (c) 3가지 경로 토글(성공/불가/부분 취소) + 기본 하이라이트, (d) 내러티브 패널 + 5-tile 통계. 우선순위 P1 (대표 보고 리허설 4/17). | — | DONE | 변경: 1 file(+235/-182). typecheck PASS. PR TBD (fix/ontology-swimlane-layout). Reality: fx.minu.best/ai-foundry-os/ontology 배포 후 dogfood 재검증 필요 |
| C77 | C | **본부장 시연 준비 (fx-boss-demo)** — 4/17 오전 대표 보고 연계 15분 시연. 온누리상품권 LPON Spec(Decode-X) 기반 "전자화폐상품권 플랫폼" 신규 시나리오 발굴→형상화→Prototype 시연. 사전 생성 + 라이브(`bun test`) 혼합. PRD: `docs/specs/fx-boss-demo/prd-final.md`. 준비: 슬라이드 5장 + 시나리오 사전 생성 + Q&A 15개 + 리허설 (formerly C69, renumbered S302 due to ID collision with C69 preflight) | — | DONE | S305 시연 완료(4/17 오전). 기존 자산: F545~F549 데모 페이지 + Decode-X working-version. S302 ID collision 해소로 C69→C77 재번호 |
| C70 | C | wiki: AI Foundry OS 경험자산화+3대자산 등록 (FX-REQ-592) | — | DONE | task orchestrator |
| C71 | C | **모델 SSOT ESLint 룰 신규** (`foundry-x-api/use-model-ssot`) — `packages/api/src/eslint-rules/use-model-ssot.js` 신규 작성 + `index.js` 등록 + 단위 테스트. 규칙: 활성 코드에서 `claude-(sonnet\|haiku\|opus)-[0-9]+-[0-9]+` 리터럴 하드코딩을 `error`로 차단하고 `@foundry-x/shared/model-defaults`의 `MODEL_SONNET`/`MODEL_HAIKU`/`OR_MODEL_*` import 강제. 면제: `packages/api/src/db/migrations/*.sql`, `**/__tests__/`, `packages/web/e2e/fixtures/`, `archive/`. 전제: 없음 — C72 착수 전 선행 권장(룰 도입 후 B3~B6 수정 시 즉시 강제). S300 feedback_model_version_ssot 후속. task-promotion 기준 2 충족 / 기준 3 불충족으로 C-track 유지 (FX-REQ-594, P2) | — | 📋(idea) → **C73 실행** | planning row, id-allocator가 C73 발급 (C71→C73 ID forward 패턴). 실제 구현 추적은 C73 row (task/C73-use-model-ssot-eslint) |
| C72 | C | **packages/* 모델 literal → SSOT 참조 전환** (B3~B6 일괄) — (B3) `packages/api/src/core/agent/specs/{reviewer,infra,security,architect,qa}.agent.yaml` 5개 `claude-haiku-4-5-20251001` → yaml loader에서 SSOT 참조 또는 런타임 치환 패턴. (B4) `packages/gate-x/src/services/llm/providers/anthropic.ts:11` → `import { MODEL_SONNET } from '@foundry-x/shared/model-defaults'`. (B5) `packages/api/src/services/llm.ts:80` → `MODEL_HAIKU` import. (B6) `packages/fx-shaping/src/agent/services/{model-router,openrouter-runner}.ts` OpenRouter 경로 → `OR_MODEL_SONNET` 참조. 총 9+ 파일. typecheck + 기존 단위 테스트 회귀 PASS 필수. 전제: C73 완료 권장(자동 검증 용도, C71→C73 ID forward). S300 feedback_model_version_ssot 후속. 내부 리팩토링으로 C-track (FX-REQ-595, P2) | — | 📋(idea) → **C74 실행** | planning row, id-allocator가 C74 발급 (C72→C74 ID forward 패턴). 실제 구현 추적은 C74 row (task/C74-packages-literal-ssot-c72-b3-b6). C73(use-model-ssot 룰) merge됨(PR #628) — lint 자동 검증 가능 |
| C73 | C | use-model-ssot ESLint 룰 (FX-REQ-596) | — | DONE | task orchestrator |
| C74 | C | packages/* 모델 literal → SSOT 참조 (C72 B3~B6) (FX-REQ-597) | — | DONE | task orchestrator (PR #630, +39/-18) |
| C75 | C | D1 격리 ESLint 룰 (C56 no-cross-domain-d1) (FX-REQ-598) | — | DONE | task orchestrator (PR #632, +333/-0) |
| C76 | C | task-monitor SPEC DONE 마킹 복원 (daemon orphan gap) (FX-REQ-599) | — | DONE | task orchestrator |
| C78 | C | MSA preflight 후속 — shared 선빌드 + wrangler check 개선 + CI workflow 통합 (FX-REQ-600) | — | → **C97 실행** | task orchestrator. S309 C97로 완결 (PR #676 `40ceec77` MERGED, +473/-0 7 files) — task orchestrator id-allocator가 C97 발급 (C78→C97 ID forward). 산출물: scripts/preflight/{lib.sh, check-lint.sh, check-wrangler-auth.sh, run-all.sh} + deploy.yml/deploy-gate-x.yml 통합 |
| C79 | C | task-start auto-complete 이중 commit 개선 (C67 관찰 해소) (FX-REQ-601) | — | ✅ | S305 **완결** (2026-04-21). PR #652 merged (`d8c43fb6`) — `scripts/task/task-complete.sh` Step 2 분기 추가: `git add -u` + untracked filter add 이후 `git diff --cached --quiet` 체크하여 stage 비어있으면 `auto-commit skip` 로그만 출력하고 `chore(TASKID): auto-commit on task complete` 커밋 시도 생략. Red→Green TDD: `test-task-complete-empty.sh` Scenario 4 (C79 auto-commit skip marker assertion, 3 assertions PASS). 이중 PR 패턴(#593/#594, #644/#645 빈 squash) 차단. **자기 dogfood 관찰**: PR #652 자체는 수동 경로라 스크립트 dogfood 아님 — 다음 `/ax:task start` 경로에서 실측 필요 |
| C80 | C | daemon post-merge cleanup gap — WT 자동 `git worktree remove` + signal 파일 archive + 로컬 브랜치 -d + tmux kill-session 일괄. 현재: Sprint 310/311 MERGED 후 WT/signal 잔존(2회 관찰), `phase_sprint_cleanup` 또는 `phase_sprint_signals` 후속 블록 부재. 해결: task-daemon `phase_post_merge_cleanup()` 신규 + STATUS=MERGED 진입 시 is_protected() 체크 후 정리. **F560/F573 Sprint 311 dogfood 관찰**에서 재발 (FX-REQ-617, P2) | — | ✅(partial) | task orchestrator. C76 mark_spec_done_row의 자매 문제. **S309 검증**: Sprint 312/313 clean 확증 (WT/branch/signal/tmux removed). C93 부산물로 WT/branch/tmux 자동 cleanup. **잔존 gap 2종**: (1) signal 파일 archive 미자동화 (daily-check 2h stale cleanup 지연), (2) autopilot PROJECT=$(basename pwd) 파싱 오류로 **dual signal file** 생성(예: `sprint-315-315.signal`). 후속 C99+ 승격 후보 |
| C81 | C | autopilot scope drift 감지 — SPEC F-item 범위 vs 실제 커밋 파일 변경 대조로 drift 자동 경고. Sprint 311 F560 dogfood에서 autopilot이 원 범위("core/discovery→fx-discovery 이동 + proxy 제거") 대신 ax-bd-* → fx-shaping 이전을 수행했으나 Match 95% 산출 — aligner가 Design을 실제 구현에 맞춰 역동기화했을 가능성. 해결: (a) Phase 5b(autopilot) scope-drift-check 훅 — SPEC F-item 비고 (a/b/c) 항목 vs `git diff --name-only` 경로 grep 대조, (b) drift ≥ 50% 시 autopilot 경고 + Gap Analysis 재실행. 근본 보강 방안 (FX-REQ-618, P2) | — | → **C98 실행** | task orchestrator. F573 등록으로 1차 대응 완료, C81은 재발 방지. S309 C98로 완결 (PR #679 `0af571ad` MERGED, +282/-1 3 files) — task orchestrator id-allocator가 C98 발급 (C81→C98 ID forward). 산출물: scripts/preflight/{check-scope-drift.sh, __tests__/test-scope-drift.sh, run-all.sh 통합} |
| C82 | C | GOV-001 frontmatter drift 일괄 정비 — `/ax:gov-doc check` (S302+ 2026-04-19) 결과 docs/ 452개 중 PASS 127(28%) / FAIL 275(61%) / NOFM 50(11%). **근본 원인 (S302+ 인사이트)**: autopilot이 SPEC.md `f_items`/`req_codes` 등록은 수행하지만 GOV frontmatter 8필드는 미주입 — "기능은 동작하지만 거버넌스는 sub-skill 외부에 있음" 패턴. 즉 sprint-* 파일 생성 시점에 `/ax:gov-doc new RPRT` 호출이 빠져 있어 표준 외부 경로로 신규 문서가 계속 누적됨. 주요 패턴: (a) `sprint-{N}.{plan,design,analysis,report}.md` ~200건 — autopilot 자동 생성 시 8필드 미주입, (b) `docs/specs/{project}/review-history.md` ~30건 — ✅ S302+ INDEX.md §"specs/ 디렉토리 구조 가이드" 주석에 비대상 명시 완료(interview-log.md 포함 3종), (c) INDEX.md stale — sprint-77/94 features/ 경로 표시되지만 실제 파일 없음. **해결 우선순위 (실행 순서)**: 1순위 (a) autopilot Plan/Design/Report Phase에 GOV frontmatter 자동 주입 훅 — 신규 누적 차단(상류 fix), 2순위 (b) 기존 drift 일괄 보정 스크립트 `scripts/governance/fix-frontmatter.sh` (sprint-* 패턴 → SSOT 추출 후 8필드 주입) — 1순위 완료 후에만, 3순위 (c) `/ax:gov-doc index` 재생성 — **2순위 완료 후에만 실행** (현재 상태에서 실행 시 frontmatter 없는 325 파일이 INDEX에서 사라져 추적 단절 위험). 최소 수정 1차 대응: FX-RPRT-309/310 2건만 표준 보정 완료(이번 세션) + INDEX.md 비대상 명시 (FX-REQ-619, P2) | — | PLANNED | gov-doc orchestrator. infra-selfcheck C8 통과 후 GOV-001 본격 적용 단계. **순서 위반 시 INDEX 데이터 손실** 주의 |
| C83 | C | MSA worker 필수 secret preflight — S303 로그인 루프 사고(fx-offering JWT_SECRET 미주입 → 모든 /api/offerings·/methodology·/bdp 경로 401 → api-client 강제 /login 리다이렉트 루프) 재발 방지. 근본 원인: 신규 worker 배포 시 JWT_SECRET 주입을 수동 의존했고 검증 장치 부재. **범위**: (a) `scripts/preflight/check-worker-secrets.sh` — packages/fx-*/wrangler.toml 순회 + `wrangler secret list` 결과에서 필수 키(`JWT_SECRET` 공통, worker별 추가 목록) 존재 확인, (b) C78 preflight에 편승(shared 선빌드/wrangler check 뒤에 secret check 삽입), (c) deploy.yml pre-deploy step 추가 — 누락 시 fail-fast. **필수 secret 목록**: foundry-x-api(JWT_SECRET, GOOGLE_CLIENT_ID, ANTHROPIC_API_KEY 등 기존 유지) / fx-discovery·fx-shaping·fx-offering(JWT_SECRET 공통) / fx-gateway(secret 불필요, service binding only). 검증 지표: 새 worker 추가 dogfood에서 누락 감지 시 CI fail (FX-REQ-620, P1) | — | ✅ | S303 회고 **완결** (2026-04-20). `628d4abc` PR #644 merged — 4 files/277 lines: `scripts/preflight/check-worker-secrets.sh` + `__tests__/*` + `required-secrets.json` + `deploy.yml` "Secret Preflight Check (C83)" step. Red→Green TDD(7/7 tests PASS). Deploy run 24652412761 success. **관찰**: task-complete.sh 이중 실행으로 PR #644 + #645 둘 다 merged(content 동일, #645는 빈 squash) — C67→C79 이중 commit 패턴 재발 → **C79 개선안 S305에 이행**(PR #652 `d8c43fb6`, auto-commit 조건부 skip) |
| C84 | C | api-client 401 redirect UX 개선 — S303 로그인 루프의 증상 측면(`window.location.href = "/login"` 강제 리다이렉트로 인해 사용자가 어느 페이지에서 튕겼는지 알 수 없고 spinner 없이 멈춘 것처럼 보임). **범위**: (a) `packages/web/src/lib/api-client.ts:73-81` 401 분기 개편 — 404/403과 명시 구분, (b) 현재 URL을 `sessionStorage.setItem("postLoginRedirect", location.pathname)` 저장 후 /login 이동, (c) login 성공 후 ProtectedRoute가 postLoginRedirect 존재 시 해당 경로로 navigate, (d) 401 토스트 노출("세션이 만료됐어요. 다시 로그인해 주세요") 후 1~2초 지연 redirect — 사용자가 원인 인지. **Out-of-scope**: refresh token 로직 변경(현행 유지), SW/iframe 인증은 별도. 검증: /discovery/items/X 진입 후 401 유발 시 토스트 → /login → 재로그인 → /discovery/items/X 자동 복귀 (FX-REQ-621, P2) | — | ✅ | S303 근본 증상 완화 **완결** (2026-04-20). `68de9a4a` PR #648 merged — 5 files/190+L: `api-client.ts`(sessionStorage + 401 분기) + `routes/login.tsx`(postLoginRedirect consume) + `components/ToastProvider.tsx` **신규**(CustomEvent `app:toast` 리스너) + `main.tsx`(Provider mount) + `__tests__/api-client-401.test.ts`. Red→Green TDD. **이중 PR 재발 ❌** — 단일 PR/단일 merge로 완결(C83 dogfood 대비 개선 없음에도 미재현) |
| C85 | C | deploy-verifier agent에 MSA secret smoke 체크 추가 — S303 회고 후속. 현재 deploy-verifier는 Workers/Pages/D1 migration 정합성만 확인하는데, secret 누락은 배포 성공(return 200)과 런타임 auth fail(401) 사이 사각지대라 기존 verifier에 안 잡힘. **범위**: (a) `.claude/agents/deploy-verifier.md` 확장 — "필수 secret 매트릭스" 섹션 추가 (C83 결과물 재사용), (b) 각 MSA worker에 대해 `wrangler secret list` JSON 파싱 + 매트릭스 대조, (c) 누락 시 deploy-verifier 보고서에 경고 + 복구 CLI 스니펫 동봉 (`curl PUT .../secrets` 예시). **선행 조건**: C83 완료 후 매트릭스 파일(`scripts/preflight/required-secrets.json` 등) 존재. **Out-of-scope**: secret 값 자체 검증(불가, 존재 여부만) (FX-REQ-622, P2) | — | ✅ | S303 회고 **완결** (2026-04-20). `9f0dab48` PR #650 merged — `.claude/agents/deploy-verifier.md` +33/-1: description에 "+ MSA secret matrix" 추가 / 검증 항목 6 "MSA Secret Matrix"(SSOT 매트릭스 인라인 + `bash scripts/preflight/check-worker-secrets.sh` 호출 명시 + `CLOUDFLARE_API_TOKEN`/`CF_ACCOUNT_ID` env var 문서화 + 복구 스니펫 2종: `curl PUT` + `npx wrangler secret put`) / 출력 포맷에 "Secret Matrix: ✅/⚠️" 행 추가. **Dogfood**: 로컬 `check-worker-secrets.sh` 실행 → PASS=4/SKIP=1(fx-gateway) 확인. **이중 PR 미재현** (C84와 동일, 단일 PR/merge). S303 재발 방지 3축(C83 preflight + C84 UX + C85 verifier) **전부 완결** |
| C86 | C | Decode-X Spec v1.1 레퍼런스 Wiki 등록 — `docs/specs/Decode-X_Spec_정의-구분-사례_보고서_v1.1.html` (1,657 lines, Executive Summary + 7장 본문 + 부록 A/B) 전문을 Foundry-X `/wiki`로 복제. **범위**: (a) pandoc HTML→GFM 변환 + 잔여 div/span 정리 + frontmatter 주입 → `docs/wiki/decode-x-spec.md` (977 lines, 148 tables), (b) master merge 시 webhook `pullFromGit()` 자동 실행 → D1 `wiki_pages` INSERT (slug=`decode-x-spec`), (c) 후속 D1 UPDATE로 title을 `Decode-X Spec — 정의·구분·사례 v1.1`로 교체(자동 생성 title "Decode X Spec" 대체), (d) https://fx.minu.best/wiki 에서 렌더링 확인. **근거**: Decode-X 기획 자산을 Foundry-X 내 레퍼런스로 상시 노출, Plumb 소비 루프(§4.3) 문서화 정합성. **Out-of-scope**: Decode-X 기획서 v1.0/v1.2 별도 등록, HTML 양방향 sync(원본 유지 정책) (FX-REQ-623, P3) | — | ✅ | PR #647 merged (`b5ef4b5d`). wiki-sync 자동 경로 **웹훅 fail** → `/tmp/wiki-insert.sql` 직접 INSERT fallback (`proj_default`, slug=`decode-x-spec`, content 43,095 bytes, ownership_marker=`git`). D1 row ✅ 확인. **웹훅 fail 2종 근본원인 → C87 승계**: (1) `wiki_pages.slug` UNIQUE 제약 없어 `ON CONFLICT(slug)` SQLITE_ERROR, (2) `DEFAULT_PROJECT_ID='default'` 하드코딩이 실측 FK `proj_default`와 불일치(wiki.ts:19, wiki-sync.ts:53) |
| C87 | C | wiki-sync 자동 경로 2종 버그 fix — C86 dogfood에서 발견된 wiki-sync.ts/wiki.ts 구조적 결함. **증상**: `docs/wiki/*.md` master push → webhook `pullFromGit()` 실행되지만 D1 INSERT fail (C86 live delivery 로그: `{"synced":0,"errors":["docs/wiki/decode-x-spec.md: D1_ERROR: ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint: SQLITE_ERROR"]}`). **근본 원인 2종**: (A) Drizzle schema `wikiPages.slug: text("slug").notNull()` — UNIQUE 제약 부재로 `INSERT ... ON CONFLICT(slug) DO UPDATE` 쿼리가 SQLite에서 reject됨. 기존 4개 row는 POST /api/wiki 경로(ON CONFLICT 없이 raw insert)로 생성되어 숨어있던 버그. (B) `DEFAULT_PROJECT_ID = "default"`(wiki.ts:19) + `VALUES (?, 'default', ...)` (wiki-sync.ts:53) 하드코딩이 실측 FK 값 `proj_default`와 불일치 → INSERT 시 FOREIGN KEY 위반. **범위**: (a) D1 migration 신규 — `CREATE UNIQUE INDEX idx_wiki_pages_slug ON wiki_pages(slug)` + Drizzle schema `.unique()` 추가, (b) wiki.ts/wiki-sync.ts `'default'` → `'proj_default'` 일괄 치환 또는 env var로 외부화, (c) webhook redelivery 테스트 — 기존 C86 row 삭제 후 `docs/wiki/decode-x-spec.md` 재-push 시 자동 sync PASS 확인. **task-promotion 판정**: D1 migration 포함 → F-item 승격 대상. C87은 "관찰 + 해결 경로 제시" 단계, 실제 구현은 **F574 승격 후 Sprint 단위로 진행 권장** (FX-REQ-624, P2) | — | → **F574 승격** (S306) | C86 dogfood 부산물. 우선순위 P2. **S306 (2026-04-21) F574로 승격** + Sprint 319 배정. 구현 세부는 F574 row 참조 |
| C88 | C | task-daemon 자체 crash loop cap + recovery hard stop — S305 등록(C79 분석 중 인접 발견). **증상 2계층**: (1) **Task-level recovery bypass** (S303 관측, attempts=36) — `TASK_MAX_RETRY=5` cap이 있지만 특정 failure mode(stale pane / orphan signal)에서 `phase_recover`가 cap을 무시하고 무한 반복하여 daemon CPU 스파이크. (2) **Daemon-level crash loop cap 부재** (구조적) — `task-daemon.sh` 자체가 SIGSEGV/SIGKILL 또는 unbounded exception loop에 빠질 경우 `task-start.sh`의 `--bg` 재기동이 idempotent 보장뿐이라 supervisor 없이 crash→재시작 반복 시 감지/차단 장치 없음. systemd/tmux 재시작 단위의 보호만 존재. **근거 데이터**: (a) S303 `daemon-Foundry-X.log` 에서 `attempts=36` 관측, (b) S307 교훈 `TASK_MAX_RETRY=5` 추가 이력 존재(task-daemon.sh:43) — **최초 도입됐지만 우회 경로 존재 확증**, (c) `DAEMON_HEARTBEAT` 파일은 있으나 heartbeat stale 감지 + 재시작 카운트 로직 부재. **범위 제안**: (a) `phase_recover` 호출 전 `attempts >= TASK_MAX_RETRY` 선제 체크 강화(현재 enqueue_retry 내부만 검사) + `retry_attempts_total` counter ndjson 로깅, (b) `DAEMON_RESTART_COUNTER` 파일 — 1시간 윈도우 내 재시작 횟수 트래킹, 3회 초과 시 `task-start.sh` 자동 재기동 거부 + 수동 intervention 안내, (c) daemon 시작 시 이전 heartbeat가 `now - 120s` 이내에 마지막 업데이트됐으면 crash loop 의심으로 warn 로그, (d) `task-doctor.sh`에 crash loop 진단 블록 추가 — restart counter + retry attempts 요약 출력, (e) **🔴 필수 — Terminal state guard (사용자 S305 승격)**: `phase_recover` + `phase_watch` + `phase_signals` **모든 복구 진입점**에서 `tasks-cache.json` status 필드 early return 체크 (`[ "$status" = "merged" ] || [ "$status" = "failed" ] || [ "$status" = "cancelled" ] && continue`). 현재 구조는 pane 필드만 보고 복구 시도하여 terminal state task도 영구 loop에 빠뜨림(S305 Live dogfood에서 C79 MERGED 후 attempts=36 확증). **(a) attempts cap과 독립된 축** — (e)는 진입 차단, (a)는 진입 후 cap. 둘 다 구현. **Out-of-scope**: systemd 도입, 외부 supervisor(PM2 등) 이관 — Foundry-X는 tmux-local 실행이라 추가 의존성 도입 지양. **검증**: 인위적 crash(`kill -SEGV`) 주입 후 3회 재시작 → task-start가 차단하는지 dogfood (FX-REQ-625, P2) | — | ✅(partial) | S303 관측 + C79 분석 중 인접 발견. **S305 Live dogfood (2026-04-21 08:05~08:08)**: C79 MERGED 후 pane `%12`(tmux 3.5a segfault 소실) stale 복구 루프 attempts=22→36 포착 → daemon `--stop` + cache pane/wt clear 회피책. 근본 해결은 **(e) Terminal state guard** 승격. **S307 (2026-04-21) (e) 완료** — PR #655 MERGED (`fc8d4bb1`). `is_terminal_state()` 공통 헬퍼 + 3 진입점 guard (phase_recover/watch/signals). Red→Green TDD 4 scenario(`test-daemon-terminal-guard.sh`) + 기존 4 daemon 테스트 회귀 PASS. **Live dogfood 확증**: master daemon(PID 2357054) 재기동 후 terminal cache(`status=merged`) + signal 주입 → 2 tick(15초 간격)에서 `phase_signals_terminal_skip` 이벤트 2건 + signal 파일 보존 확인. **Ghost discovery**: legacy `task-monitor.sh` (PID 266585, since 2026-04-19) ghost process 여전히 가동 중 — daemon 통합 주석과 달리 guard 없는 legacy code가 signal 경쟁. 첫 dogfood에서 signal을 먼저 먹어 guard 우회. → **후속 C-track 후보 (C88-ghost)**: task-monitor.sh deprecate or guard 이식. (a)/(b)/(c)/(d)는 후속 세션 scope |
| C90 | C | deploy-verifier D1 binding preflight — S307 F561 배포 gap 후속. 현재 C85(secret matrix) 확장은 secret 존재 여부만 검사. F561이 `DISCOVERY_DB` binding을 새로 추가했으나 prod 계정에 해당 D1 DB가 **미생성** 상태라 deploy-msa job의 `wrangler deploy`가 `[code: 10181] D1 binding references database ... not found`로 실패 (Deploy run #24726011482). C83 secret preflight + C85 agent matrix와 동일 패턴으로 D1 binding 존재 검증 축 추가 필요. **범위**: (a) `scripts/preflight/check-d1-bindings.sh` 신규 — wrangler.toml 파싱해서 모든 `[[d1_databases]]` binding 수집 → Cloudflare API `GET /accounts/.../d1/database/{id}` 로 존재 확인, (b) `.github/workflows/deploy.yml`에 preflight 스텝 추가(C83 옆), (c) `.claude/agents/deploy-verifier.md` 검증 항목 7 "D1 Binding Matrix" 추가. **선행 조건**: 없음 (C83 패턴 재사용). **Out-of-scope**: DB 자동 생성(선제 차단만) (FX-REQ-627, P2) | — | → **C91 실행** | S307 실사고 후 등록. F561 배포 gap의 재발 방지축. S308 C91로 완결 (PR #661 `1f4bb32b` MERGED) — task orchestrator id-allocator가 C91 발급 (C90→C91 ID forward) |
| C89 | C | `scripts/content-sync-check.sh` expected 계산 버그 — **3회 반복 관찰**(S303/S306/S307)로 C-track 승격. **증상**: script가 `expected=319` / `expected='Phase 46'`로 drift 9건 보고하지만 실제 **완료된 max Sprint = 313** / **완료된 max Phase = 45**. 사용자가 landing content를 실제 313으로 맞춰도 script는 여전히 319 기대로 drift 표시 → false positive. **근본 원인**: script의 expected 계산 로직이 SPEC §5 f-items 테이블에서 **모든 row**의 Sprint 번호/Phase 번호 중 max를 취함. 그러나 F574(Sprint 319, PLANNED) 또는 Phase 46(PLANNED) 같은 **미래 계획 row**도 포함됨. **올바른 expected**: status가 `✅` 인 row만 필터링한 max Sprint/Phase. **범위**: (a) `scripts/content-sync-check.sh`의 expected 계산 쿼리에 `grep '✅'` 또는 awk 조건 필터 추가, (b) 단위 테스트(`scripts/__tests__/test-content-sync-check.sh` 신규) — fixture SPEC.md로 완료/미래 row 혼합 시나리오 PASS 검증, (c) 본 세션(S307)에서 manual 업데이트한 landing 3 파일 + README가 script에 의해 자동 유지되는지 dogfood. **task-promotion 판정**: 2파일 내 변경(script + test) → **Backlog 유지**(F 승격 기준 미충족). **Out-of-scope**: expected 계산 알고리즘 전면 재설계 — 현재 버그만 fix (FX-REQ-626, P2) | — | → **C92 실행** | S303/S306/S307 3회 관찰 후 memory-lifecycle rules §1 "정착 트리거 2회 이상" 충족하여 승격. 본 세션(S307)에서 hero.md/landing.tsx/footer.tsx/README.md manual update 수행 — script fix 후 이 패치가 자동 유지됨을 확증해야 함. Sprint 313 MVP M2 완료 후 하드닝 Sprint에서 처리 권장 |
| C91 | C | C90 deploy-verifier D1 binding preflight (FX-REQ-628) | — | DONE | task orchestrator |
| C92 | C | C89 content-sync-check.sh expected 계산 버그 fix (FX-REQ-629) | — | DONE | task orchestrator |
| C93 | C | C88 잔여 (a)(b)(c)(d) — task-daemon hardening (FX-REQ-630) | — | DONE | task orchestrator |
| C95 | C | C88-ghost — legacy `scripts/task/task-monitor.sh` deprecate (daemon Phase 1 흡수 후 잔존). **증상**: task-daemon.sh가 Phase 1 (signal 처리)를 C93(a~d) + C88(e) terminal guard까지 포함하여 흡수 완료했으나, (1) `scripts/task/task-monitor.sh` 파일 잔존(5,030 bytes, C88(e) guard **미이식**), (2) `scripts/task/agent-loop.sh:126` + `scripts/task/agent-manage.sh:161` 두 legacy launcher가 `ensure_daemons()`/`health_check()` 경로에서 여전히 `nohup bash task-monitor.sh --interval 30`으로 재소환. 누군가 `agent-loop`/`agent-manage`를 직접 실행하면 guard 없는 task-monitor ghost가 부활하여 daemon과 signal 경쟁. S307 C88 Live dogfood 1차에서 legacy PID 266585 (since 2026-04-19)가 signal을 먼저 소비하여 terminal guard 우회 사건 실증. **범위**: (a) `scripts/task/task-monitor.sh` 파일 삭제(git rm), (b) `agent-loop.sh:122-141 ensure_daemons()`의 monitor 스폰 블록 제거(daemon이 담당), (c) `agent-manage.sh:158-169 health_check()`의 task-monitor 체크/재시작 블록 제거, (d) 주석 참조(`task-daemon.sh:8`, `lib.sh:318`)는 이력 성격으로 보존, (e) Red 테스트: `test-agent-loop-no-monitor-spawn.sh` — `agent-loop.sh` 호출 시 `task-monitor.sh` 프로세스 미생성 확인, (f) Dogfood: `ensure_daemons()` 수동 호출 후 `pgrep task-monitor` 0건 확인. **task-promotion 판정**: 3~4파일 변경(경계 기준 2 충족) + 내부 인프라 deprecate → **Backlog C-track 유지**(F 승격 불필요). **Out-of-scope**: task-monitor.sh의 process_signal() 로직 이식/보존(daemon이 이미 동등+우월 커버리지, C88(e) guard 포함) (FX-REQ-632, P2) | — | → **C96 실행** | S309 등록. S307 C88 dogfood 부산물. MEMORY "다음 작업" C88-ghost 등재 항목. id-allocator가 C96 발급 (C95→C96 ID forward 선례 동일). 구현 추적은 C96 row 참조 |
| C94 | C | **✅ msa-lint workflow harness-kit 선행 빌드 gap fix — PR #670 MERGED** (`f29fb844`). S309 진단 완결. 8회 연속 fail(4/16~4/21) 근본 원인 = `msa-lint.yml` Step 8 `pnpm --filter @foundry-x/api build`가 `packages/api` tsc를 호출하지만 `packages/api`는 `@foundry-x/harness-kit` workspace dep 사용(`packages/api/src/routes/proxy.ts:7-8`) → harness-kit dist 미생성 상태에서 `TS2307 Cannot find module '@foundry-x/harness-kit'`로 fail → Step 9 MSA lint skip → PR CI blocking. **turbo.json**에 `typecheck.dependsOn: ["^build"]`가 이미 있으나 `pnpm --filter` 직접 호출은 turbo 우회하므로 미적용. **범위**: (a) `.github/workflows/msa-lint.yml` Step 8을 `pnpm --filter '@foundry-x/api...' build`로 변경(`...` = workspace dep 전체 포함 — harness-kit 자동 선행 빌드), (b) `.github/workflows/deploy-gate-x.yml` test job 2 스텝 동일 패턴 적용(`@foundry-x/gate-x...`) — deploy-gate-x도 5회 연속 fail(4/7~) 동일 원인으로 판정되나 gate-x dormant 상태라 별건 축에서 치유, (c) local reproduce: `rm -rf packages/harness-kit/dist && pnpm --filter @foundry-x/api build` → TS2307 재현 → `pnpm --filter '@foundry-x/api...' build`로 해결. **확증 데이터**: gh run view 24728578200 jobs JSON — Step 6/7(shared-contracts/shared build) success, Step 8(api build) failure, Step 9 skipped. **Out-of-scope**: F569 harness-kit 표준화 완결, deploy-gate-x 복구(dormant 해소는 별건), turbo 경유 전환(cache 스코프 검증 필요) (FX-REQ-631, P1) | — | DONE | S309 등록. msa-lint PR blocking 해소 최우선. **PR #670 MERGED** (`f29fb844`) — workflow 2 파일(msa-lint + deploy-gate-x) `...` syntax 전환 |
| C96 | C | C95 legacy task-monitor.sh deprecate (FX-REQ-633) | — | DONE | task orchestrator |
| C97 | C | C78 MSA preflight 후속 하드닝 (shared 선빌드 + wrangler check + lint pre-check + preflight 정합성) (FX-REQ-634) | — | DONE | task orchestrator |
| C98 | C | C81 autopilot scope-drift-check (SPEC vs git diff 대조 + autopilot Phase 4 훅) (FX-REQ-635) | — | DONE | task orchestrator (PR #679 `0af571ad` MERGED, S309) |
| C99 | C | **fx.minu.best DNS NXDOMAIN 복구** — Sprint 319 deploy run 24869887551 smoke-test FAIL의 직접 원인. 마지막 성공 deploy(2026-04-22 PR #685) 이후 fx.minu.best DNS 해결 불가 상태(GitHub runner + WSL 양쪽에서 NXDOMAIN). foundry-x-web.pages.dev(Pages 기본 URL) 200 정상 → Cloudflare custom domain 바인딩 또는 minu.best DNS zone 이슈. **범위**: (a) Cloudflare dashboard custom domain `fx.minu.best` 상태 점검, (b) minu.best zone DNS records 점검(CNAME/A → Pages target), (c) Pages project `foundry-x-web` custom domain re-bind 또는 DNS record 복구, (d) 복구 후 smoke-test green 재확증, (e) **재발 방지 옵션**: smoke-test.sh에 fallback URL(foundry-x-web.pages.dev) 추가 + WEB_URL 1차 실패 시 fallback로 재시도. **task-promotion 판정**: 인프라/DNS 작업 + 1~2 스크립트 변경 → **Backlog 유지**(F 승격 불필요, 외부 DNS 의존). **우선도 P1** — 사용자 외부 노출 도메인 미동작. S313 등록 (FX-REQ-636, P1) | — | OPEN | S313 등록. Sprint 319 fx-modules MSA 배포는 이슈 없음(직접 + gateway 경유 모두 200 확증), 본 항목은 fx.minu.best 단독 DNS 이슈 |
| C100 | C | **fx-modules secret matrix + autopilot Phase Exit P1 강화** — Sprint 319 deploy 3연속 사고(test job lint / fx-discovery workspace deps / fx-modules deploy step 누락) 회고 기반 재발 방지. **범위**: (a) `scripts/preflight/required-secrets.json`에 `fx-modules: ["JWT_SECRET", "GITHUB_TOKEN"]` 추가(C83 매트릭스 확장), (b) **사용자 수동 작업 필요**: `wrangler secret put JWT_SECRET --name fx-modules` + `GITHUB_TOKEN` (Cloudflare secret 등록), (c) autopilot Phase Exit P1 체크리스트에 `eslint.config.js` 존재 확증 + 신규 Worker `packages/{worker}/eslint.config.js` 자동 생성 템플릿 추가, (d) deploy.yml에 신규 Worker 추가 시 `Deploy {worker} Worker` step + `paths-filter msa` 등록 자동 점검 스크립트(`scripts/preflight/check-worker-deploy-step.sh`), (e) autopilot Phase Exit P3 강화: api/__tests__/ 이관 누락 grep — `packages/api/src/__tests__/`에서 fx-* 도메인 route 호출 잔존 0건 확증. **task-promotion 판정**: 3~4 파일 + autopilot 자체 개선 → **Backlog C-track 유지** (FX-REQ-637, P1) | — | OPEN | S313 등록. Sprint 319 reality-gap 분석 — Match 100% reported but 3 deploy hotfixes (PR #691/#692/#693) needed. autopilot 자체 진화 필요 |
<!-- /fx-task-orchestrator-backlog -->

## §6 Sprint 실행 계획 (아카이브)

> Sprint 1~263 실행 계획 (체크리스트) 전체:
> **[docs/archive/spec-execution-plan-2026-04.md](archive/spec-execution-plan-2026-04.md)**
>
> 현행 Sprint 계획: [ROADMAP.md](ROADMAP.md) + `docs/01-plan/features/sprint-{N}.plan.md`

## §7 기술 스택

> 상세: [BLUEPRINT.md](docs/BLUEPRINT.md) §2 + `.claude/rules/coding-style.md`

## §8 Tech Debt

> 해소 완료 (TD-01~TD-03). TD-03은 F490으로 승격 후 해소.

## §9 변경 이력

> 전체 이력: [docs/archive/spec-changelog-archive-2026-04.md](archive/spec-changelog-archive-2026-04.md)

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 5.81 | 2026-04-12 | **SPEC.md 추가 경량화** — Phase 1~29 F-items 935줄 → `docs/archive/spec-fitems-phase1-29.md`. §7~§9 압축. 1174→목표 200줄대 |
| 5.83 | 2026-04-15 | **F539 3분할 등록** — Sprint 294 착수 준비 완료. 기존 F539(idea) → F539a(k6 재측정, FX-REQ-576, Sprint 294 📋plan) / F539b(fx-gateway 프로덕션 전환, FX-REQ-577, Sprint 295) / F539c(7 라우트 Service Binding, FX-REQ-578, Sprint 296). PRD Final `docs/specs/fx-gateway-cutover/prd-final.md` Round 1 95/100 + Ambiguity 0.11 Ready. F540/F541은 Sprint 297/298로 1 스프린트씩 이동. 선제 체크리스트 5항목(feedback_msa_deploy_pipeline_gaps.md) 반영 |
| 5.84 | 2026-04-15 | **MSA 2단계 고도화 Pipeline 등록** (/ax:todo plan) — Phase 44 완결 + 운영 preflight. F540 Shaping(FX-REQ-579, Sprint 297) + F541 Offering(FX-REQ-580, Sprint 298) 순차. C56 D1 격리(ready, F540 직전 선행) + C57 shared 슬리밍(ready, F541 이후) + C69 신규 배포 preflight 자동화(F540 전 완료 권장). F540/F541은 PRD `docs/specs/fx-msa-roadmap-v2/prd-final.md` 재사용(신규 PRD 미작성). 병렬 Master pane에서 C56/C57/C69 진행 |
| 5.85 | 2026-04-15 | **F539c ✅ 부분 + Phase 44 F539 전체 MERGED** — PR #597 (S296 Match 95%). 7 routes fx-discovery 이전(Group A+B 단일 PR 통합) + CLI scripts 7개 URL 전환 + fx-discovery 11 new tests. Phase Exit drift 3건: (a) 2 PR 분할 미이행(단일 PR), (b) KOAMI Smoke P2 미실측, (c) Retrospective 누락. packages/cli는 API URL 하드코딩 없음 확인(N/A). 후속: Smoke + Retrospective 별도 task, C69 preflight 선행 후 Sprint 297 F540 |
| 5.86 | 2026-04-15 | **F540 ✅(부분) Shaping 분리 MERGED** — PR #598 (S297 Match 96%, 93 files, 1h 55m). fx-shaping Worker 신규(13 routes/22 services/15 schemas) + fx-gateway SHAPING Service Binding + `/api/shaping/*` + `/api/ax-bd/*` 라우팅. 초기 deploy.yml 24445867997 failure(ESLint 8 errors): autopilot `pnpm lint` 누락 → test job FAIL → deploy 3 job skip. **Hotfix `2efc06f3`**: 7 수동 + 1 auto-fix → deploy.yml 24446036032 success. Phase Exit P1 확증(smoke 200×2). P2 KOAMI는 F541 이월. C69 2차 개선 대상(lint pre-check) |
| 5.88 | 2026-04-18 | **모델 버전 SSOT 정비 + C71/C72 등록** (S300) — PR #625 (`bbbe06d7`) B1/B2/B7 정리: `task-start.sh` `--model sonnet` alias 전환 / `model-defaults.ts` SSOT 주석 보강(업그레이드 절차+소비자 목록) / scripts/ stale `20250514` → `4-6`. 자동 반영 메커니즘: ax-plugin `502caef` daily-check Step 6e "모델 버전 Drift" 추가 + `9ba4468` sprint/SKILL.md alias 문구. MEMORY `feedback_model_version_ssot` 신규 — 2중 SSOT(CLI alias / SDK model-defaults.ts) 원칙. 차기 Sprint C71(use-model-ssot ESLint 룰, FX-REQ-594, P2) + C72(packages/* literal → SSOT 전환 B3~B6, FX-REQ-595, P2) 등록 |
| 5.89 | 2026-04-19 | **모델 SSOT + D1 격리 + daemon orphan fix 4단 체인** (S301) — C-track 4건 연속 완결: **C73**(FX-REQ-596, PR #628) `foundry-x-api/use-model-ssot` ESLint 룰 신규 — `claude-(sonnet\|haiku\|opus)-\d+-\d+` literal 차단 + SSOT import 강제. **C74**(FX-REQ-597, PR #630 +39/-18) packages/* literal → SSOT 전환: agent yaml 5개 + gate-x/api/fx-shaping 4 파일. **C75**(FX-REQ-598, PR #632 +333/-0) `foundry-x-api/no-cross-domain-d1` ESLint 룰 신규 — domain별 D1 table 접근 차단(discovery_*/shaping_*/offering_*). **C76**(FX-REQ-599, PR #634 +187/-32) daemon orphan SPEC mark fix — task-monitor가 signal 선처리+삭제하여 daemon 164-194 블록 skip되는 구조적 간극을 `lib.sh mark_spec_done_row` 공통 함수로 해소. C76 자체가 dogfood 통과(`📝 C76 SPEC backlog → DONE` 자동). ID forward 3건: C71→C73 / C72→C74 / C56→C75. ESLint 4축 체계 완성(no-cross-domain-import / no-direct-route-register / use-model-ssot / no-cross-domain-d1). MEMORY `feedback_daemon_orphan_spec_mark` 2회 관찰 → 수동 승격(3-strike 예외) → resolved |
| 5.92 | 2026-04-20 | **로그인 루프 hotfix + 회고 C-track 3건 등록** (S303) — 사용자 보고 2건 진단: (a) Google OAuth 후 login 카드 멈춤, (b) `/discovery/items/:id` 클릭 시 /login 루프. 근본 원인 = **fx-offering worker JWT_SECRET 미주입** → MAIN_API 발급 토큰이 dev-secret fallback으로 signature 검증 실패 → 401 → `api-client.ts:79` `window.location.href = "/login"` 강제 리다이렉트 + localStorage wipe. 해결: Cloudflare REST API `PUT .../fx-offering/secrets`로 `ktds11!@` 주입 → 모든 영향 경로(`/api/offerings`, `/methodology`, `/bdp`, `/biz-items/:id/business-plan` 등) 401 → 200 복구. 기존 토큰 유지(회전 아님). 회고 C-track 3건 등록: **C83**(FX-REQ-620, P1) MSA worker 필수 secret preflight — C78 편승, deploy.yml fail-fast / **C84**(FX-REQ-621, P2) api-client 401 redirect UX 개선 — postLoginRedirect sessionStorage + toast 안내 / **C85**(FX-REQ-622, P2) deploy-verifier agent secret matrix 확장 — C83 dependency |
| 5.93 | 2026-04-20 | **C83 MSA secret preflight ✅ MERGED** (S304) — `--reuse-id C83`로 기존 PLANNED row 소비(ID forward 방지). WT pane %25, Sonnet 4.6 세션에서 Red→Green TDD로 구현: `scripts/preflight/check-worker-secrets.sh`(137L) + `__tests__/check-worker-secrets.test.sh`(120L, 7/7 PASS) + `scripts/preflight/required-secrets.json`(worker별 필수 secret 매트릭스) + `.github/workflows/deploy.yml`에 "Secret Preflight Check (C83)" step 추가. 필수 매트릭스: foundry-x-api(JWT/GOOGLE/ANTHROPIC/WEBHOOK/GITHUB/OPENROUTER/GOOGLE_SECRET) / fx-discovery·shaping·offering(JWT_SECRET 공통) / fx-gateway(skip, binding-only). **검증**: master commit `628d4abc` PR #644 + Deploy run 24652412761 success. **사이드 관찰**: task-complete.sh가 2회 실행되어 PR #644 merged(실 내용) + PR #645 merged(빈 squash) — C67→C79 이중 commit 패턴이 C83 dogfood에서 재발, C79 개선안(task-start.sh auto-complete hook fix) 후속 Sprint 배정 필요. 추가 관찰: task-daemon의 C79 dead pane 복구 경로가 `TASK_MAX_RETRY=5` cap을 무시하고 무한 루프(attempts=36 관측) — 별도 C-track 후보 |
| 5.94 | 2026-04-20 | **C84 api-client 401 redirect UX ✅ MERGED** (S304 이어서) — `--reuse-id C84`. WT pane %26, Sonnet 4.6 세션, 12분. 5 files / +190 / -4 (`68de9a4a` PR #648). 구현: `api-client.ts` 401 분기 재작성(`sessionStorage.setItem("postLoginRedirect", window.location.pathname)` 후 `location.href = "/login"`, 403/404는 throw만), `routes/login.tsx` 로그인 성공 후 `sessionStorage.getItem("postLoginRedirect")` 소비 + navigate, `components/ToastProvider.tsx` **신규 50L**(window CustomEvent `app:toast` listener — 선례 `VersionBadge.tsx` 대신 전역 dispatch 채택), `main.tsx` Provider mount, `__tests__/api-client-401.test.ts` Red (122L). **이중 PR 패턴 재발 ❌** — C83에서 관찰된 2-PR 이슈가 C84에서는 미재현(완료 감지 타이밍/watch-loop 상태 차이 추정). Deploy run 24653980459 in_progress(master C84+C86+C87 혼합). 후속: C85(deploy-verifier secret matrix) 착수 가능 — C83 dependency 해소됨 |
| 5.95 | 2026-04-20 | **C85 deploy-verifier secret matrix ✅ MERGED + S303 3축 완결** (S304 마무리) — `--reuse-id C85`. WT pane %27, Sonnet 4.6, 2분(markdown-only). `.claude/agents/deploy-verifier.md` +33/-1 (`9f0dab48` PR #650): description `+ MSA secret matrix` / 검증 항목 6 "MSA Secret Matrix"(SSOT 매트릭스 인라인 + `bash scripts/preflight/check-worker-secrets.sh` 호출 + `CLOUDFLARE_API_TOKEN`/`CF_ACCOUNT_ID` env var 문서화 + 복구 스니펫 2종 `curl PUT`/`npx wrangler secret put`) / 출력 포맷에 "Secret Matrix: ✅/⚠️" 행. **Dogfood**: `check-worker-secrets.sh` 로컬 실행 → PASS=4/SKIP=1(fx-gateway) ✅. **이중 PR 미재현** 단일 PR/merge. **S303 재발 방지 3축 전체 완결** (C83 preflight + C84 UX + C85 verifier) — 운영 관점: (1) 신규 worker 추가 시 deploy.yml이 fail-fast로 누락 secret 차단, (2) 런타임 401 발생 시 사용자는 postLoginRedirect로 원래 경로 복귀, (3) 배포 후 agent가 matrix 대조로 사후 검증 가능. S303 사고가 관찰·분석·재발방지까지 단일 세션(~3h)에 완결된 최초 사례 |
| 5.96 | 2026-04-21 | **C88 신규 등록 — task-daemon crash loop cap + recovery hard stop** (S305 착수) — 사용자 요청 "C79 이중 PR 근본 fix + task-daemon recovery cap 이슈 신규 C-track 등록" 2건 분기 처리 중. C79(auto-commit 조건부 skip) 방식 확정 — 다음 커밋에서 PR 생성 예정. C88은 S303 관측(attempts=36 recovery 무한 루프) + 구조적 daemon-self crash loop cap 부재를 통합 scope로 등록(FX-REQ-625, P2). 해결 방안 제안 4종: (a) `phase_recover` 선제 cap 체크 강화, (b) `DAEMON_RESTART_COUNTER` 1h window, (c) heartbeat stale 재시작 시 warn, (d) `task-doctor.sh` crash loop 진단 블록. C79 완료 후 일괄 하드닝 Sprint에서 처리 권장 |
| 5.97 | 2026-04-21 | **C79 ✅ MERGED — task-complete.sh auto-commit 조건부 skip** (S305 마무리) — PR #652 merged (`d8c43fb6`). 기존 C79 worktree(`C79-task-start-auto-complete-commit-c67`) 재사용 + origin/master rebase 후 Red→Green TDD 수행. `scripts/task/task-complete.sh` Step 2 분기 추가(10 lines) + `scripts/task/test-task-complete-empty.sh` Scenario 4 회귀 보호(36 lines). 핵심 로직: `git add -u` + untracked filter add 이후 `git diff --cached --quiet` 체크하여 stage empty 시 `auto-commit skip` 로그만 출력하고 `chore(TASKID): auto-commit on task complete` 커밋 시도 생략. C83 dogfood 재발 패턴(#644+#645 빈 squash) 차단. 4 scenarios / 12 assertions 전부 PASS. **자기 dogfood 제한**: PR #652 자체는 수동 gh pr 경로라 task-complete.sh dogfood 아님 — 다음 `/ax:task start` 경로에서 실측 필요. **S303 후속 재발 방지축 완결**: C83 preflight(누락 방지) + C84 UX(증상 완화) + C85 verifier(사후 감시) + C79(orchestrator 하드닝) 4축 |
| 5.98 | 2026-04-21 | **F574 신규 등록(C87 승격) + Decode-X 219 handoff Foundry-X 측 긴급 랜딩** (S306) — Session-start 점검에서 **uncommitted 3파일** 감지(`packages/api/src/{app.ts, env.ts, core/harness/routes/internal-prototype-jobs.ts}`, 10:07 mtime). Decode-X Sprint 219(81c9805, PR #22, 01:18Z merged)의 "Foundry-X 통합 완결" 선언이 실제로는 Foundry-X 리포 측 파일만 생성되고 commit 누락 상태로 10시간 방치 → prod `/api/internal/prototype-jobs` 404 위험. PR #651 `feat(api): F355b Decode-X 219 handoff endpoint` 긴급 랜딩(`475acad1` → squash `f91ee587`, deploy run 24717113571 success, smoke-test PASS). **단, `DECODE_X_HANDOFF_SECRET` prod 미등록** — endpoint live지만 401 리턴 상태. Decode-X 측 `env.FOUNDRY_X_SECRET`(handoff.ts:244)과 **동일 값 필수**. 사용자 직접 등록 예정(후속 Task). **F574 승격** — C87 본문이 "F574 승격 권장" 명시하여 task-promotion.md 기준 1(D1 migration 포함) 충족. Sprint 319 배정(Phase 45 F560~F572 완주 후 첫 하드닝). FX-REQ-624 승계. 병렬 작업 배려: %19(S305) 완료 후 master push 충돌 회피 (ScheduleWakeup 270s) |
| 6.00 | 2026-04-21 | **Sprint 313 착수 — F561+F562 🔧(design) 전환 + F563/F564 shift** (S307 시작) — 사용자 지정 스코프 확정: Sprint 313 = F561(D1 Option A PoC, MVP M2) + F562(shared-contracts). F563 Sprint 313→314, F564 Sprint 313→315 shift. Phase 45 MVP M2 집중을 위한 스코프 축소 — Sprint 311/312에서 관찰된 scope drift(Match 95%로 녹아 탐지 어려움) 재발 방지 목적. 다음 단계: `bash -i -c "sprint 313"` WT + Full Auto autopilot + **SCOPE LOCKED** 주입(F561 범위 5항목 + F562 범위 5항목 명시). S306 F560 성공 패턴 재사용 |
| 6.02 | 2026-04-21 | **Sprint 313 F561+F562 ✅ MERGED + MVP M2 달성 + hotfix 2건 + C90 등록** (S307 마무리) — **(a) Sprint 313 PR #656 MERGED** (`fc9751c1`, Match 100%). Full Auto + SCOPE LOCKED 주입(F561 5항목 + F562 5항목 + out-of-scope F563/F564 명시) 재사용. 실행 21분. `packages/fx-discovery/wrangler.toml`에 DISCOVERY_DB binding + DISCOVERY_DB_MODE=shadow 추가. `packages/shared-contracts/` workspace 신규(Discovery↔Shaping DTO/Event v1.0). packages 11→13. **(b) Hotfix PR #657** — Sprint 313 E2E 4 shards + deploy 전부 FAIL. 근본: `pnpm --filter @foundry-x/shared build`가 workspace dep `@foundry-x/shared-contracts`를 선행 빌드하지 않아 tsc TS2307. 수정: deploy.yml(3) + e2e.yml + msa-lint.yml 총 5곳에 `pnpm --filter @foundry-x/shared-contracts build` 선행 스텝. **(c) Hotfix PR #658** — fx-discovery deploy `[code: 10181] D1 binding 'DISCOVERY_DB' references database ... not found`. 근본: Sprint 313 autopilot가 sinclairseo@gmail.com PoC 계정에만 D1 생성, ktds.axbd@gmail.com prod 계정 전환 미실행(wrangler.toml 주석은 명시돼 있었음). 조치: Cloudflare API로 prod 계정에 `foundry-x-discovery-db` 생성(UUID `f129f04b-0149-4ff4-bc2f-382b025130b9`) + 13 migration 로컬 적용 + wrangler.toml top-level DB ID 교체 + deploy.yml에 discovery-db migration 자동 적용 스텝 추가. env.dev 블록은 PoC ID 유지(dev용). **Deploy run #24726406766 success**. **Phase 45 MVP M2 달성** — Gap 2(D1 Option A PoC) + Gap 3(shared-contracts) 동시 해소. **C90 신규 등록**(FX-REQ-627, P2) — deploy-verifier에 D1 binding 존재 preflight 추가(C83 secret / C85 matrix 확장). S307 F561 배포 gap 재발 방지축 |
| 6.01 | 2026-04-21 | **C88 (e) Terminal state guard ✅ MERGED** (S307 병렬) — PR #655 `fc8d4bb1` squash merged. Master pane(Opus) 직접 TDD로 진행 (Sprint 313 worktree와 병렬, daemon stale code 함정 통제). `scripts/task/task-daemon.sh`에 `is_terminal_state()` 공통 헬퍼 + 3 진입점 guard(phase_recover line 465 / phase_watch line 294 / phase_signals line 68) 추가 +57L. `scripts/task/test-daemon-terminal-guard.sh` 신규 +180L — 4 scenario(A: recover+merged / B: recover+failed / C: recover+cancelled / D: signals+failed). **Live dogfood 확증**: master daemon(PID 2357054) 재기동 후 terminal cache + signal 주입 → `phase_signals_terminal_skip` 이벤트 2건(15초 간격) + signal 파일 보존 ✅. **Ghost discovery**: legacy `task-monitor.sh` (PID 266585, since 2026-04-19) ghost process 여전히 가동 — daemon 통합 주석과 달리 guard 없는 legacy가 signal 경쟁해서 첫 dogfood에서 guard 우회. **후속 C-track 후보(C88-ghost)** 등록 예정: task-monitor.sh deprecate or guard 이식. C88 row: `PLANNED → ✅(partial)` — (a)/(b)/(c)/(d)는 후속 세션 scope. Sprint 313 병렬 pane과 working tree 충돌 2회 발생(SPEC.md "File has been modified since read" / master에 Red commit 잘못 들어가 task branch로 복구) — `git update-ref refs/heads/master origin/master` plumbing 복구 사용 |
| 5.99 | 2026-04-21 | **F560 ✅ MERGED + MVP M1 달성 + 부가 3건** (S306 마무리) — PR #654 `b64cc59b` squash merged. Full Auto 경로 + **SCOPE LOCKED** 프롬프트 주입(원 범위 3항목 — core/discovery/routes/* 이동 + app.ts 등록 제거 + fx-gateway proxy 제거)으로 Sprint 311 scope drift 재발 차단. Match 95%, daemon 자동 PR merge + cleanup + velocity 기록 정상. 수동 patch 필요했던 velocity JSON created literal은 feedback_velocity_created_literal.md로 관찰 기록(재관찰 시 C-track 승격). **Phase 45 Gap 1 해소 = MVP M1 달성**. **부가 작업 3건**: (a) Decode-X handoff prod secret 2쌍 등록(`DECODE_X_HANDOFF_SECRET`/`FOUNDRY_X_SECRET` 동일 값, 사용자 직접) — smoke test 401 리턴 확인 = endpoint + auth layer 정상, (b) preflight matrix PR #653 merged, (c) ax-plugin daily-check **Step 6f 추가**(`4d7a91e`) — `claude --print --output-format json` 응답의 `modelUsage` key로 CLI alias 실측 → SSOT 비교 → 신 minor 등장 시 3단계 업그레이드 안내. Sonnet 4.7 미release 상태에서 자동 감지 메커니즘 확보. **부가 feedback 정착**: `feedback_monitor_tail_n1_antipattern.md` — Monitor script에서 `tail -n 1` poll은 last line을 반복 emit하는 안티패턴(S306 Sprint 312 Monitor에서 재현), `tail -F` + `LAST_KEY` 대조로 해소 |
| 6.03 | 2026-04-21 | **Sprint 314 착수 — F563 🔧(design) 전환 + F565/F567 Sprint 315 shift** (S308 시작) — 사용자 지정 스코프 확정: Sprint 314 = F563(fx-shaping E2E + KOAMI P2 완결, P0) 단일. F565(SDD Triangle CI)/F567(Multi-hop latency) Sprint 314→315 shift. Phase 45 MVP 연계 집중 + scope drift 재발 차단 목적 — S306/S307 연속 성공 패턴(F560 Match 95% / F561+F562 Match 100%) 재사용. F563 범위 4항목 명시: (a) Shaping 13 routes 순수 이관(F540 proxy 제거), (b) KOAMI P2 deferred 케이스 사전 분석 + 구현, (c) Shaping Graph E2E(bi-koami-001 proposals ≥ 1건), (d) Phase Exit P1~P4 Smoke Reality. Out-of-scope: F565/F567/C89/C90 하드닝/다른 도메인(Offering/Agent) 이전. 다음 단계: `bash -i -c "sprint 314"` WT + Full Auto autopilot + **SCOPE LOCKED** 주입(F563 4항목 + out-of-scope 명시). 리스크 R5(KOAMI P2 deferred 난이도) — F542 Rubric 튜닝 후 재착수 조건 충족 확인 |
| 6.05 | 2026-04-22 | **C95 신규 등록 — C88-ghost legacy task-monitor.sh deprecate** (S309 세션 시작) — 사용자 요청 "`/ax:session-start C88-ghost (legacy task-monitor.sh deprecate)`"로 MEMORY "다음 작업" 최상위 항목 착수. SPEC §5 C95 row 추가(FX-REQ-632, P2, PLANNED). 범위 (a)~(f): task-monitor.sh 파일 삭제 + agent-loop.sh:126 monitor 스폰 블록 제거 + agent-manage.sh:161 health_check 블록 제거 + Red 테스트 `test-agent-loop-no-monitor-spawn.sh` + dogfood(pgrep 0건). 근거: S307 C88 (e) Live dogfood 1차에서 legacy PID 266585가 signal을 먼저 먹어 terminal guard 우회 실증. 해소 시 legacy 재소환 경로 완전 차단. **병렬 pane 관찰**: 다른 master pane이 직전에 C94 신규(msa-lint harness-kit 선행 빌드 gap fix, FX-REQ-631) 선점 → C95로 shift 적용. 다음 단계: `/ax:task start C C95` WT + Red→Green TDD |
| 6.12 | 2026-04-22 | **Sprint 316 F567+F568 ✅ MERGED — Phase 45 Batch 2 완결** (S310) — PR #678 MERGED (`3f426727`, Match 98%, +1182/-0, 13 files). Full Auto + SCOPE LOCKED(F567 4항목 + F568 4항목 + out-of-scope F565/F570/F571/F572/C78/C80/C89/C90 명시 + Phase Exit P1~P4) 재사용. **F567 Multi-hop latency**: k6 3-hop benchmark script + p50/p95/p99 측정 + **SLO p95 < 300ms 확정**(F543 1-hop baseline 기반) + `docs/dogfood/sprint-316-latency.md`. **F568 EventBus PoC**: D1 Event Table vs Cloudflare Queue vs Durable Object 3종 비교 → **D1 선정** + `docs/specs/fx-eventbus/decision.md`(근거 문서) + `StagePublisher` + `DiscoveryTrigger` PoC + Discovery→Shaping FORMALIZATION 트리거 1 flow. **Tests 83/83 PASS** (fx-discovery 35 + fx-shaping 40 + k6 validation 8). Phase Exit P1~P4 전부 충족. **S306~S310 연속 성공 패턴**(Match 95%→100%→100%→97%→98%) — SCOPE LOCKED prompt + autopilot이 일관되게 작동. 병렬 pane C98(C81 autopilot scope-drift-check) 별건 착수 중 — Sprint와 독립. **Phase 45 진행**: MVP M1+M2+M3 + F563 + Batch 2(F567+F568) 완결. 남은 Batch: F565(Sprint 317) / F570(Sprint 318) / F572+F574(Sprint 319) / F571(Sprint 320) |
| 6.11 | 2026-04-22 | **Sprint 316 착수 — F567+F568 🔧(design) 전환** (S310) — 사용자 확정 Batch 2 스코프: Sprint 316 = F567(Multi-hop latency k6 benchmark + SLO p95 < 300ms, Gap 9) + F568(EventBus PoC D1/Queue/DO 3종 비교 + Discovery→Shaping 1 flow, Gap 10). 두 F-item 독립성 높음 — 병렬 구현 가능. 선행 충족: F562 shared-contracts MERGED / F569 harness-kit EventBus client MERGED. 다음 단계: `bash -i -c "sprint 316"` WT + Full Auto autopilot + **SCOPE LOCKED** 주입(F567 4항목 + F568 4항목 + out-of-scope F565/F570/F571/F572 명시). S306~S309 연속 성공 패턴(SCOPE LOCKED + autopilot Match 95~100%) 재사용 |
| 6.21 | 2026-04-27 | **Sprint 319 fx-modules prod LIVE — 3 hotfix 체인 종결 + C99/C100 등록** (S313 마무리) — 추가 hotfix 2건: **PR #692** `6dd318b7` MERGED — `Apply fx-discovery D1 migrations` step에 `packageManager: pnpm` 1줄 추가. cloudflare/wrangler-action@v3가 wrangler 미감지 시 `npm i wrangler@3.90.0` fallback → `@foundry-x/harness-kit: workspace:*` deps 인식 불가 → `npm error EUNSUPPORTEDPROTOCOL "workspace:"`. S307 패턴 재현. **PR #693** `?` MERGED — `Deploy fx-modules Worker` step 추가 + `msa` paths-filter에 `packages/fx-modules/**` + `packages/harness-kit/**` 추가. F572 autopilot이 `fx-gateway/wrangler.toml`에 MODULES Service Binding은 추가했으나 `deploy.yml`에 fx-modules deploy step 누락 → fx-gateway가 미존재 worker 참조하여 Cloudflare API error. 결과: deploy run **24869887551 deploy-msa SUCCESS**. **fx-modules prod 실측 PASS**: `https://fx-modules.ktds-axbd.workers.dev/api/{portal,gate,launch}/health` 3/3 HTTP 200, `https://fx-gateway.ktds-axbd.workers.dev/api/{portal,gate,launch}/health` 3/3 HTTP 200 (gateway routing 정상). **smoke-test FAIL 2건**: fx.minu.best DNS NXDOMAIN(Pages custom domain 이슈, F572 무관, 마지막 성공 2026-04-22 PR #685). foundry-x-web.pages.dev 기본 URL은 200 정상. **C99 신규 등록(P1)**: fx.minu.best DNS 복구 — 사용자 외부 노출 도메인 미동작. **C100 신규 등록(P1)**: fx-modules secret matrix + autopilot Phase Exit P1 강화(eslint.config.js 존재 / deploy step 자동 등록 / api/__tests__ 이관 누락 grep). **S313 총 결산**: PR #690 (autopilot, Match 100%) + PR #691 (lint+dead test 정리) + PR #692 (workspace deps wrangler-action) + PR #693 (fx-modules deploy step) = 4 PR 체인. **교훈 재확인**: autopilot Match 100% ≠ Production Reality (S311 패턴 4세션 연속 재현 — F540/F562/F563/F572). **C100을 통한 autopilot 자기진화 시점 도달** |
| 6.20 | 2026-04-24 | **Sprint 319 deploy hotfix PR #691 MERGED** (S313 후속) — Sprint 319 PR #690 MERGED 후 deploy run 24865659216 test job FAIL(@foundry-x/fx-modules#lint exit 2) → prod 미배포 사고. **근본 2종**: (1) `packages/fx-modules/eslint.config.js` 누락 — ESLint 10.0.3 flat config 형식 요구, fx-shaping 패턴 복사 + 15 errors auto-fix + 12 hand-fix(unused vars `_` prefix 또는 import 제거), (2) `packages/api/src/__tests__/` 18 파일 / 115 dead tests — fx-modules로 이동된 portal/gate/launch route 대상 테스트가 잔존하여 404 전면 실패 → 전수 삭제(feedback-*, metrics-routes, inbox-routes, slack-config, onboarding-*, org*, wiki*, project-overview, webhook-*, kpi-weekly, middleware, simple-routes). **PR #691** `1fff1575` +577/-? MERGED, 약 8분. turbo 40/40 PASS(api 3174 tests, fx-modules 6 tests). **교훈**: autopilot Match 100% ≠ Production 동작 증명 재확인(S311 Sprint 316 패턴 재현). **후속 C-track 제안**: (C98) fx-modules 테스트 커버리지 재구축 — 삭제된 115 tests 시나리오 fx-modules에 재작성, (C99) C83 preflight 매트릭스에 fx-modules 추가(JWT_SECRET + GITHUB_TOKEN), (C100) autopilot Phase Exit P1에 `eslint.config.js` 존재 확증 + `packages/api/src/__tests__/` 이관 누락 grep 체크 추가. S306~S313 Match 100% pattern은 유지되나 deploy Reality gap 연속 발생 — autopilot 자체 개선 필요 |
| 6.19 | 2026-04-24 | **Sprint 319 F572+F574 ✅ MERGED — Phase 45 Batch 5 완결 (S313 마무리)** — PR #690 MERGED (`ef4f47ad`, Match **100%** 24/24, +18690/-141, 162 files). F572+F574 번들 PR. autopilot 약 60분 완결(08:23 inject → 09:23 DONE → 09:23 MERGED, CI 대기 7초). **F572 fx-modules Worker**: `packages/fx-modules/` 신규 Hono Worker(34 routes + 45 services), prefix 라우팅 `/api/portal/*`·`/api/gate/*`·`/api/launch/*`, wrangler.toml 1개, harness-kit JWT middleware 재사용. fx-gateway MODULES Service Binding + catch-all 단일화. api/app.ts portal/gate/launch routes 제거. cross-domain imports **0 violations**(Worker-level shared services + local stubs 패턴). TDD 6 tests(3 health + 3 cross-domain guards). **F574 wiki-sync fix**: D1 migration `0139_wiki_pages_slug_unique.sql` UNIQUE INDEX + Drizzle `.unique()` + `DEFAULT_PROJECT_ID` `'default'` → `'proj_default'` 치환. wiki-sync 3/3 PASS. **병렬 S312 Sprint 318 F570 ✅ MERGED**(PR #687, Match 97%) — Phase 45 Batch 4+5 동일 세션 연속 완결. **S306~S313 7세션 연속 성공**: Match 95→100→100→97→98→100→97→**100%**. SCOPE LOCKED + autopilot 패턴 재확증. 다음: F571 Sprint 320(최후 Batch 6, Agent 도메인 62 services). **Pre-deploy secrets 필요**: `wrangler secret put JWT_SECRET --name fx-modules` + `GITHUB_TOKEN` (C83 preflight 매트릭스에 fx-modules 추가 필요 후속) |
| 6.17 | 2026-04-24 | **Sprint 319 착수 — F572+F574 🔧(design) 전환** (S313) — Phase 45 Batch 5. 사용자 확정 스코프: Sprint 319 = **F572**(modules/portal·gate·launch 통합 분리, P2) + **F574**(wiki-sync webhook 2종 버그 fix, P2, C87 승계). 3대 아키텍처 결정: (1) 단일 Sprint 번들 PR(S309/S310 패턴 재사용), (2) **fx-modules 단일 Worker**(prefix `/api/portal/*`·`/api/gate/*`·`/api/launch/*`로 분리, wrangler.toml 1개, F560/F563 Discovery/Shaping 패턴 재사용), (3) F574 `'proj_default'` 하드코딩 치환(env var 외부화는 별도 C-track). **병렬 실행**: S312 pane이 Sprint 318 F570 동시 진행 중 — system-version 2개 병기. 다음 단계: `/ax:sprint 319` Full Auto + SCOPE LOCKED 주입(F572 7항목 + F574 4항목 + out-of-scope F565 중복/F570 영역/F571/D1 wiki schema 확장/env var 외부화 명시) |
| 6.04 | 2026-04-22 | **Sprint 314 F563 ✅ MERGED + deploy hotfix PR #668 + S308 마무리** — **(a) Sprint 314 PR #663 MERGED** (`6583b1fe`, Match 97%). Full Auto + SCOPE LOCKED(F563 4항목 + out-of-scope 7건) 재사용, autopilot 25분 24초. fx-shaping 34/34 tests PASS(shaping-d1-insert 6 + shaping-routes-functional 17 + 기존 11). dead code `core/shaping/routes/*` ×13 제거 + dead test 5건 삭제/4건 정리. bi-koami-001 D1 INSERT 실측 확인(rawDb 직접 조회). better-sqlite3 devDep 추가. (a) 원 범위 "순수 이관"은 F540에서 완료 상태 → autopilot이 정확히 파악하여 dead code 제거로 재해석(Design §5 gap 명시). **(b) Hotfix PR #668 (`cd9d499a`)** — deploy run #24728582298 test job FAIL: `@foundry-x/web#typecheck` 가 `@foundry-x/shared-contracts` 모듈을 못 찾아 exit 2 → deploy 5 jobs skip → prod 미배포. 근본: F562 hotfix(PR #657)의 deploy-api/web/msa + e2e + msa-lint 5곳 커버리지에 **deploy.yml test job 누락**. 수정: `pnpm --filter @foundry-x/shared-contracts build` 1 스텝 추가. **(c) 병렬 pane 부산물**: C91 deploy-verifier D1 binding preflight PR #661 `1f4bb32b` (C90 구현), C93 task-daemon hardening PR #666 `e13f22e2` (a)(b)(c)(d) 완결 — crash loop cap + restart counter + heartbeat warn + doctor --crash-loop. **(d) C80 사실상 해소** — C93 daemon hardening 부산물로 Sprint 314 완료 후 WT/tmux/signal/branch 전부 자동 cleanup 확증(수동 정리 불필요). **(e) 장기 관찰**: msa-lint 8회 연속 fail(4/16~) — `@foundry-x/harness-kit` 선행 빌드 누락, F563 무관. 후속 C-track 승격 대상. Phase 45 진행: MVP M1(F560) + MVP M2(F561+F562) + F563 완결. 남은 MVP M3(F564 Sprint 315) 대기 |
| 6.19 | 2026-04-29 | **E2E Audit 회복률 41% — `/ax:e2e-audit` 5건 fix + 9 신규** (S313) — PR #695 MERGED (`55a5e980`, +309/-42, 7 files). 391 tests / fail 54→32 (-22), pass 305→332 (+27). 패턴별 근본원인 5종 식별·수정: (a) fixture destructure 누락 `{page}`→`{authenticatedPage:page}` 12곳 ProtectedRoute 통과, (b) tabs 전환 누락(default tab `info` → analysis 클릭 후 검증), (c) `page.request` ≠ `page.route` mock(strangler-gateway → `page.evaluate(fetch)`), (d) fx-gateway root 404(prod/smoke `/api/discovery/health` 200 공개 endpoint로 변경), (e) 옛 IA(`/collection/*`, `/gtm/*` dead route) → 신 IA(`/discovery/items/:id`) + offering API 이관(`/api/offering-packs` → `/api/offerings`). 신규 spec 2개: lpon-demo(F547 대표 시연 5 tests) + prd-management(F454/F455 4 tests). Coverage 75%(라우터 92 라우트 중 69 cover), redirect 16개 모두 검증됨 확인(redirect-routes.spec.ts 17/17 PASS). 잔여 32 fails는 setup-guide(9)/onboarding-flow(5)/pipeline-dashboard(4)/discovery-detail-advanced(3) 등 sprint 단위 spec 재작성 필요(차기 후보) |
| 6.18 | 2026-04-24 | **Sprint 318 F570 ✅ MERGED — Phase 45 Batch 4 완결** (S312 마무리) — PR #687 MERGED (`4c12cd84`, Match 97%, +340/-12089 87 files). Full Auto + SCOPE LOCKED(F570 4항목 + out-of-scope 8종 + Fail criteria 3+3종) 재사용, autopilot 34분 완결(08:20 CREATED → 08:54 MERGED). **범위 완결**: (a) `offeringPacksRoute`(Sprint 81 F236/F238/F240 Offering Pack + MVP Tracking + IR Bottom-up) + `core/offering/` + `__tests__/` offering 20+건 MAIN_API → fx-offering 이관, (b) fx-gateway F541 proxy 12줄(biz-items business-plan/methodology + offerings + bdp + methodologies) 제거 + catch-all 단일화, (c) `grep -rn "core/offering"` fx-*/api 0건 확증, (d) Phase Exit P1~P4 Smoke Reality 통과. MAIN_API 대량 삭제 확증 — +340/-12089 = legacy offering 정리(87 files). 후속 post-merge cleanup: WT/tmux/local branch/remote branch/signal 전부 자동(C93 daemon hardening 재확증). **S306~S312 7 세션 연속 성공**(Match 95→100→100→97→98→100→97%) — SCOPE LOCKED prompt + autopilot 일관성 재확증. **병렬**: S313 pane Sprint 319 F572+F574 🔧(design) 동시 진행 중(STATUS=CREATED, 09:21 KST 확인), system-version 2축 병기. Phase 45 남은: F572+F574 Sprint 319(S313 in-progress) / F571 Sprint 320 📋 |

## §10 버전 정책

### 프로젝트 마일스톤
- **Sprint N**: 프로젝트 진행 상태 추적 (Sprint 32부터 적용)
- 이전(Sprint 1~31): v0.1~v2.5 형식 사용 (이력 보존, 소급 수정 안 함)

### 패키지 버전 (Independent SemVer 2.0)

| 패키지 | 현재 버전 | 배포 대상 | 버전 증가 기준 |
|--------|----------|----------|--------------|
| packages/cli | 0.5.0 | npm registry | CLI 기능 변경 |
| packages/api | 0.1.0 | Cloudflare Workers | API endpoint 변경 |
| packages/web | 0.1.0 | Cloudflare Pages | UI 기능 변경 |
| packages/shared | 0.1.0 | 내부 전용 | 타입/인터페이스 변경 |

### 0.x 기간 버전 증가 규칙

| 변경 유형 | 버전 증가 | 예시 |
|----------|----------|------|
| 하위 비호환 변경 | 0.MINOR.0 | 0.1.0 → 0.2.0 |
| 새 기능 추가 | 0.minor.PATCH | 0.1.0 → 0.1.1 |
| 버그 수정 | 0.minor.PATCH | 0.1.1 → 0.1.2 |

### 1.0.0 전환 기준

패키지별 독립 판단. 아래 두 조건 **모두** 충족 시 승격:
1. 외부 사용자가 프로덕션에서 실제 사용 중
2. 공개 API 하위 호환성 정책 수립 + API 문서화 완료
