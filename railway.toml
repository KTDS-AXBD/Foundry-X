[build]
builder = "DOCKERFILE"
dockerfilePath = "prototype-builder/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
